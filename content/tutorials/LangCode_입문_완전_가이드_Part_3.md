---
title: "LangCode 입문 완전 가이드 - Part 3: 고급 기능과 최적화"
created: '2025-11-16'
last_modified: '2025-11-16'
tags:
  - LangCode
  - 고급/기능
  - Router/최적화
  - Deep-Mode
  - 자동화
status: "진행중"
type: "입문가이드"
priority: "high"
---

# LangCode 입문 완전 가이드 - Part 3: 고급 기능과 최적화

## 📋 목차

1. [[#스마트_Router_이해하기]]
2. [[#Deep_Mode_vs_ReAct_Mode]]
3. [[#Priority_전략]]
4. [[#Interactive_Launcher_마스터하기]]
5. [[#프로젝트_별_설정_파일]]
6. [[#성능_최적화_팁]]
7. [[#실전_워크플로우]]

---

## 스마트 Router 이해하기

### 🔀 Router란?

**정의:** 상황에 맞게 가장 적절한 AI를 자동으로 선택해주는 기능

**비유:**
- 택시 부르면 기사가 상황에 맞게 길을 선택하는 것처럼
- LangCode가 상황에 맞게 Claude나 Gemini를 선택

### 🆚 Router 없을 때 vs 있을 때

#### Router 없을 때 (수동 선택)

```bash
# 간단한 질문 → Claude 선택 (오버스펙)
langcode chat --llm claude
"Python 시작하는 방법?"
# 비용: $0.003 / 결과: 우수 / 속도: 2초

# 간단한 질문에 비싼 AI를 썼음 💸
```

#### Router 있을 때 (자동 선택)

```bash
# Router 활성화
langcode chat --router

# LangCode가 자동으로 판단:
# "이건 간단한 질문이니 Gemini 써야겠다"
# 비용: $0.0001 / 결과: 좋음 / 속도: 1초

# 저렴하고 빠름! 🚀
```

### 📊 Router가 고르는 방식

```
질문의 복잡도 분석
    ↓
┌─────────────────────────────┐
│ 간단함                복잡함  │
│   ↓                    ↓    │
│ Gemini              Claude  │
│ (빠름, 저렴)       (깊음, 정교) │
└─────────────────────────────┘
```

### 🎯 Router 실제 사용법

#### 기본 문법

```bash
langcode [명령어] --router --priority [우선순위]
```

#### 우선순위 옵션

| 우선순위 | 의미 | 상황 |
|---------|------|------|
| **cost** | 돈을 아껴라 | 학생 / 개인 프로젝트 |
| **speed** | 속도를 중시 | 긴급 작업 |
| **quality** | 품질 중시 | 중요한 프로젝트 |
| **balanced** | 균형잡힘 | 기본값 |

#### 실전 예제

**예제 1: 비용 우선**

```bash
# 학생이 프로젝트 하는데 돈이 없음
langcode feature "로그인 기능 만들어" --router --priority cost

# Router가 생각:
# "복잡한 기능이지만, cost 우선이니 Gemini 써야겠다"
# 비용: 저 / 속도: 중간 / 품질: 좋음
```

**예제 2: 속도 우선**

```bash
# 회사에서 긴급하게 버그를 고쳐야 함
langcode fix --log crash.log "서버 다운됨" --router --priority speed

# Router가 생각:
# "어려운 문제고 시간이 없으니 Claude 써야겠다"
# 비용: 높음 / 속도: 빠름 / 품질: 우수
```

**예제 3: 품질 우선**

```bash
# 중요한 금융 프로젝트
langcode feature "결제 시스템 구현" --router --priority quality

# Router가 생각:
# "매우 중요하니까 Claude로 깊게 생각하자"
# 비용: 높음 / 속도: 느림 / 품질: 최상
```

### 💡 Router 사용 팁

#### Tip 1: 학습 중일 때는 --router 끄기

```bash
# Router 없음 (교육용 - 일관성 있음)
langcode chat --llm claude
# 계속 Claude를 쓰니까 일관성 있음

# Router 있음 (자동 선택 - 다양함)
langcode chat --router
# Gemini와 Claude를 번갈아 쓸 수 있음
```

**이유:** 처음 배울 때는 같은 AI를 쓰는 게 좋습니다.

#### Tip 2: 프로젝트 시작 시 Router 평가

```bash
# 프로젝트의 예상 비용 계산
langcode analyze "이 프로젝트의 복잡도" --router

# Router가 제시한 AI를 보고
# 그 AI의 가격 확인 후 프로젝트 진행 여부 결정
```

---

## Deep Mode vs ReAct Mode

### 🤔 두 가지 생각 방식

```
┌──────────────────────┬──────────────────────┐
│   ReAct Mode (빠름)   │   Deep Mode (깊음)    │
├──────────────────────┼──────────────────────┤
│ 빠르게 행동           │ 천천히 생각           │
│ 간단한 문제용        │ 복잡한 문제용        │
│ 1-2초                │ 10-30초              │
│ 전형적인 답변        │ 창의적인 답변        │
└──────────────────────┴──────────────────────┘
```

### 📝 ReAct Mode (기본값)

**정의:** "생각하고 행동한다" (Reasoning + Acting)

**특징:**
- 빠르게 결정
- 즉각적인 행동
- 반복적 개선

**언제 쓸까?**
- 간단한 질문
- 빠른 프로토타입
- 일상적인 작업

```bash
# ReAct Mode (기본값)
langcode chat

# vs 명시적으로
langcode chat --mode react
```

**예제:**

```bash
langcode chat --mode react

질문: "Python 리스트 만드는 방법?"

결과:
# 즉시 답변 (2초)
my_list = [1, 2, 3]
print(my_list[0])  # 1
```

### 🧘 Deep Mode

**정의:** 깊게 생각하고 단계적으로 접근

**특징:**
- 천천히 분석
- 다양한 관점 고려
- 최적의 해답 추구

**언제 쓸까?**
- 복잡한 알고리즘
- 성능 최적화
- 아키텍처 설계

```bash
# Deep Mode 활성화
langcode chat --mode deep

# 또는 대화형으로
langcode
# 메뉴에서 Deep Mode 선택
```

**예제:**

```bash
langcode feature "성능을 최적화한 정렬 알고리즘" --mode deep

결과:
# 단계별 분석 (15초)

1단계: 문제 정의
- 시간 복잡도: O(n log n) 이상
- 공간 복잡도: O(1) 선호

2단계: 알고리즘 선택
- Quicksort vs Mergesort vs Heapsort

3단계: 구현
def optimized_sort(arr):
    # 최적화된 구현
    ...

4단계: 성능 측정
# 벤치마크 결과
```

### 🎯 모드 선택 가이드

| 상황 | Mode | 이유 |
|-----|------|------|
| "이게 뭔가요?" | ReAct | 빠른 답변 필요 |
| "어떻게 최적화할까?" | Deep | 깊은 분석 필요 |
| "급하다!" | ReAct | 속도 중시 |
| "중요한 프로젝트" | Deep | 품질 중시 |

### 💡 Mode 사용 팁

#### Tip 1: Deep Mode 자동화

```bash
# Deep Mode로 자동 실행 (물어보지 말고)
langcode feature "복잡한 기능" --mode deep --auto

# 순서:
# 1. AI가 계획 수립
# 2. AI가 코드 구현
# 3. 자동으로 적용
# (사람이 개입 안 함)
```

#### Tip 2: Mode 조합

```bash
# Chat은 ReAct, Feature는 Deep
langcode chat --mode react  # 빠른 이해
langcode feature "..." --mode deep  # 깊은 구현
```

---

## Priority 전략

### 🎯 Priority의 4가지 옵션

```
langcode [명령어] --priority [balanced|cost|speed|quality]
```

### 📊 Priority별 특징

#### 1️⃣ Balanced (기본값)

```
좋음 ├─────────────┤
     │   균형잡힘  │
     └─────────────┘
```

**특징:**
- 비용, 속도, 품질 균형
- 대부분의 상황에 추천
- 의사결정 안 해도 됨

```bash
langcode chat --priority balanced
# 또는 생략 (기본값)
langcode chat
```

#### 2️⃣ Cost (비용 우선)

```
비용 ├──┤
속도 │  │
품질 │  │
```

**특징:**
- 가장 저렴한 AI 선택
- 큰 프로젝트에 적합
- 많은 API 호출 필요할 때

```bash
langcode feature "..." --router --priority cost

# Router 없으면 Gemini, 있으면 더 저렴한 모델 자동 선택
```

#### 3️⃣ Speed (속도 우선)

```
비용 │  │
속도 ├──┤
품질 │  │
```

**특징:**
- 가장 빠른 AI 선택
- 긴급 상황용
- 비용이 높을 수 있음

```bash
langcode fix "긴급 버그" --priority speed

# 빠르게 해결하는 걸 최우선
```

#### 4️⃣ Quality (품질 우선)

```
비용 │  │
속도 │  │
품질 ├──┤
```

**특징:**
- 가장 나은 결과 추구
- 중요한 프로젝트용
- 비용이 높고 시간이 오래 걸림

```bash
langcode feature "금융 시스템" --priority quality

# 가장 좋은 결과만 원함
```

### 💡 Priority 실전 예제

#### 상황 1: 학생 프로젝트

```bash
# 비용이 최고 우선
langcode feature "숙제 프로젝트" --router --priority cost
```

**선택:**
- Claude: $0.003 (토큰당)
- Gemini: $0.0001 (토큰당)
→ **Gemini 선택** ✅

#### 상황 2: 회사 웹서비스

```bash
# 품질이 최고 우선
langcode feature "결제 기능" --priority quality

# 또는
langcode analyze "아키텍처 검토" --llm claude --mode deep
```

**선택:**
- Claude: 깊고 정교함
- Gemini: 빠르지만 얕을 수 있음
→ **Claude 선택** ✅

#### 상황 3: 긴급 버그

```bash
# 속도가 최고 우선
langcode fix --log crash.log "서버 다운" --priority speed
```

**선택:**
- 가장 빠른 응답
- 비용은 무시
→ **가장 빠른 AI 선택** ✅

---

## Interactive Launcher 마스터하기

### 🚀 Interactive Launcher란?

**정의:** 명령어 없이 메뉴에서 선택하는 방식

```bash
langcode
# 메뉴가 나타남 ↓
```

### 📋 메뉴 항목 완전 해석

#### 1. Command 선택

```
┌─────────────────────────┐
│ 1. Chat                 │
│ 2. Feature              │
│ 3. Fix                  │
│ 4. Analyze              │
│ 5. Settings             │
└─────────────────────────┘
```

**각각의 의미:**
- **Chat**: 대화형 질문 (Part 2 참고)
- **Feature**: 새 기능 구현 (Part 2 참고)
- **Fix**: 버그 수정 (Part 2 참고)
- **Analyze**: 코드 분석 및 리포트
- **Settings**: 설정 변경

#### 2. Engine 선택

```
React (기본): 빠르고 반복적
Deep: 깊고 사려 깊음
```

**권장:**
- 배우는 중 → React
- 중요한 작업 → Deep

#### 3. Smart Routing

```
Enable Smart Routing? (y/n)
```

**의미:**
- YES → Router 활성화 (상황에 맞게 AI 선택)
- NO → 직접 AI 선택

#### 4. Priority 선택

```
Priority (if routing enabled):
1. Balanced (기본)
2. Cost
3. Speed
4. Quality
```

**상황에 맞게 선택!**

#### 5. LLM 선택

```
Available LLMs:
1. Claude (Anthropic)
2. Gemini (Google)
3. OpenAI
4. Ollama (로컬)
```

**Smart Routing OFF일 때만 나타남**

### 🎬 Interactive Launcher 실전

**예제: 웹 프로젝트 로그인 기능 구현**

```bash
$ langcode

✓ Command: Feature
✓ Engine: Deep
✓ Smart Routing: Enable
✓ Priority: Quality
✓ Project Dir: /Users/me/web-project

Feature description: "Add secure user authentication with JWT tokens"

# 아래에서 자동 진행
# 1. AI가 설계안 제시
# 2. 코드 구현
# 3. 테스트 실행
# 4. 결과 제시
```

---

## 프로젝트별 설정 파일

### 📁 설정 파일의 역할

**문제점:**
```bash
# 매번 옵션을 다 입력해야 함
langcode feature "..." --llm claude --mode deep --priority quality

# 너무 길다! 😫
```

**해결책:**
```bash
# 설정 파일에 저장하면 자동으로 적용
langcode feature "..."

# 끝! 설정이 자동 적용됨 ✨
```

### 🔧 설정 파일 만들기

#### 단계 1: 설정 폴더 생성

```bash
mkdir -p .langcode
```

#### 단계 2: 설정 파일 작성

```bash
# .langcode/langcode.md 파일 생성
cat > .langcode/langcode.md << 'EOF'
# LangCode Configuration

## Default Settings

- **LLM**: claude
- **Mode**: deep
- **Priority**: quality
- **Project Type**: web-application

## Project Context

이것은 Python Django 웹 애플리케이션입니다.
- 데이터베이스: PostgreSQL
- 프론트엔드: React
- API: REST

## Instructions

모든 코드는 PEP 8 스타일 가이드를 따릅니다.
EOF
```

#### 단계 3: 확인

```bash
# 설정이 자동으로 적용됨
langcode feature "사용자 인증 추가"

# 클로드 + Deep Mode + Quality 자동 선택!
```

### 📋 MCP 설정 (고급)

```bash
# .langcode/mcp.json 파일
cat > .langcode/mcp.json << 'EOF'
{
  "servers": {
    "github": {
      "command": "mcp-github"
    },
    "web": {
      "command": "mcp-web-search"
    }
  }
}
EOF
```

**효과:**
- GitHub 데이터 자동 접근
- 웹 검색 자동 실행
- 더 똑똑한 AI!

---

## 성능 최적화 팁

### ⚡ 속도 최적화

#### Tip 1: 로컬 LLM 사용 (Ollama)

```bash
# 인터넷 요청 없이 로컬에서 실행
langcode chat --llm ollama

# 장점:
# - 빠름 (네트워크 지연 없음)
# - 무료 (API 비용 없음)
# - 프라이버시 (로컬에서만)

# 단점:
# - 품질 낮을 수 있음
# - 컴퓨터 리소스 많이 씀
```

#### Tip 2: ReAct Mode 사용

```bash
# 빠른 답변 원할 때
langcode chat --mode react

# Deep Mode보다 10배 빠름
```

#### Tip 3: 간단하고 명확한 지시

```bash
# 나쁜 예 (AI가 헷갈림 → 오래 걸림)
"뭔가 코드를 만들어줄래"

# 좋은 예 (AI가 바로 이해 → 빠름)
"Python 더하기 함수 만들어"
```

### 💰 비용 최적화

#### Tip 1: Gemini 사용

```bash
# 저렴함 (Claude의 1/10)
langcode chat --llm gemini --priority cost

# 간단한 작업은 Gemini로 충분
```

#### Tip 2: Router + Cost Priority

```bash
# 모든 상황에 최적의 AI 선택
langcode feature "..." --router --priority cost

# 자동으로 가장 저렴한 옵션 선택
```

#### Tip 3: 토큰 사용 최소화

```bash
# 나쁜 예 (토큰 많이 씀)
langcode chat
"제 전체 프로젝트를 분석해주고 개선안을 주세요"
# 전체 코드를 올려야 함 (비용 ↑)

# 좋은 예 (토큰 적게 씀)
langcode chat
"이 함수의 성능을 개선하는 방법은?"
# 함수만 보냄 (비용 ↓)
```

### 🎯 품질 최적화

#### Tip 1: Deep Mode + Claude

```bash
# 최고 품질
langcode feature "중요한 기능" --mode deep --llm claude

# 시간: 오래 걸림
# 품질: 최상
# 비용: 높음
```

#### Tip 2: Context 충분히 제공

```bash
# 좋은 지시
"Django REST API 프로젝트에서
사용자 인증을 JWT로 구현해줘"

# AI가 Django와 JWT를 알고 있으니 더 좋은 코드 생성
```

#### Tip 3: 테스트 명령어 제공

```bash
langcode feature "..." --test-cmd "pytest -q" --mode deep

# AI가:
# 1. 코드 생성
# 2. 테스트 코드도 생성
# 3. 자동으로 테스트 실행
# 4. 오류 수정
```

---

## 실전 워크플로우

### 📋 일반적인 개발 흐름

```
1️⃣ Chat으로 이해
   ↓
2️⃣ Feature로 구현
   ↓
3️⃣ Fix로 오류 수정
   ↓
4️⃣ Analyze로 검토
   ↓
✅ 완성!
```

### 🎯 예제: 웹 API 개발

**프로젝트:** 사용자 관리 REST API

#### Step 1: 설계 논의 (Chat)

```bash
langcode chat --llm claude

질문: "사용자 관리 API 설계에서
이메일 중복 확인은 어떻게 해야 할까?"

결과: 여러 방법 제시 및 권장안
```

#### Step 2: 구현 (Feature)

```bash
langcode feature "POST /users 엔드포인트 구현:
- 이메일 중복 체크
- 패스워드 해싱
- 데이터베이스 저장" \
  --test-cmd "npm test" \
  --mode deep
```

#### Step 3: 테스트 실패 수정 (Fix)

```bash
langcode fix \
  --log test_output.log \
  "이메일 검증 테스트가 실패한다" \
  --test-cmd "npm test"
```

#### Step 4: 코드 리뷰 (Chat)

```bash
langcode chat --llm claude

질문: "구현한 인증 로직을 검토해줄래?"

# 코드 붙여넣기

결과: 개선안 제시
```

#### Step 5: 최적화 (Feature)

```bash
langcode feature "데이터베이스 쿼리 최적화" \
  --mode deep \
  --priority quality
```

---

## 💡 Advanced Tips

### Tip 1: Slash Commands

```bash
# Chat 중에 특수 명령어 사용 가능

/clear          # 대화 히스토리 삭제
/select         # 메뉴로 돌아가기
/exit           # 종료
/help           # 도움말
```

### Tip 2: Environment Variables

```bash
# 기본값 설정
export LANGCODE_DEFAULT_LLM=claude
export LANGCODE_DEFAULT_MODE=deep
export LANGCODE_PROJECT_DIR=~/my_project

# 그 다음부터는 자동 적용
langcode chat
```

### Tip 3: Batch Processing

```bash
# 여러 파일에 같은 작업
langcode feature "모든 Python 파일에 타입 힌트 추가" \
  --apply

# --apply가 자동으로 변경사항 적용
```

---

## 🎓 결론: Part 3 마무리

이제 배운 것들:

✅ Router로 자동 AI 선택
✅ Deep Mode로 깊은 분석
✅ Priority로 상황에 맞는 최적화
✅ Interactive Launcher로 쉬운 사용
✅ 설정 파일로 프로젝트 자동화
✅ 성능, 비용, 품질 최적화

**다음:** Part 4에서는 실제 프로젝트에 적용하는 법을 배웁니다!

