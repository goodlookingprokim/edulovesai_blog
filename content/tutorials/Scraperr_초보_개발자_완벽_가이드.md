---
title: Scraperr - 노코드 웹 스크래핑 도구 완벽 가이드
created: 2025-10-08
last_modified: 2025-10-08
tags:
  - 웹스크래핑/도구
  - 데이터수집/자동화
  - XPath/선택자
  - 노코드/개발
  - 초보자/가이드
  - Scraperr
status: 완료
type: 가이드
priority: high
share_link: https://share.note.sx/lll81942#gQON324o/ZApzwVihUi5FKegADWz9ZwmdUcSK9qYc2Q
share_updated: 2025-10-08T04:54:52+09:00
---

# Scraperr - 노코드 웹 스크래핑 도구 완벽 가이드

## 📋 목차 (내부 링크 활용)
1. [[#스토리로 시작하기 - 연구원 지훈의 고민]]
2. [[#Scraperr란 무엇인가? (파인만 기법)]]
3. [[#핵심 개념 쉽게 이해하기]]
4. [[#실전 단계별 예제]]
5. [[#실무 활용 시나리오]]
6. [[#문제 해결 가이드]]
7. [[#고급 활용 패턴]]
8. [[#용어 사전]]

---

## 스토리로 시작하기 - 연구원 지훈의 고민

### 📖 지훈의 데이터 수집 악몽

지훈은 마케팅 리서치 회사의 주니어 애널리스트입니다. 오늘도 경쟁사 분석 프로젝트가 시작되었는데요...

**🤯 지훈이 마주한 현실:**

```
상사: "지훈씨, 경쟁사 100개 웹사이트에서
      제품 가격 정보 수집해주세요.
      내일까지요!"

지훈: "네... 알겠습니다..." 😰
```

**지훈의 하루:**

```
오전 9시: 첫 번째 웹사이트 열기
→ Ctrl+C, Ctrl+V (복사 붙여넣기)
→ 엑셀에 수동 입력

오전 10시: 다섯 번째 웹사이트...
→ 손목이 아프기 시작
→ 실수로 잘못된 데이터 복사

점심 시간: 15개 웹사이트 완료
→ 85개 남음...
→ 내일까지 불가능! 😱

오후 2시: "파이썬으로 크롤링하면 되지 않을까?"
→ Python 설치
→ BeautifulSoup 튜토리얼 검색
→ 코드 이해 안 됨 😵

오후 5시: 여전히 20개 웹사이트...
→ 야근 확정
→ 주말 출근 예상
```

**😫 지훈의 진짜 고민:**
```
문제 1: 수동 작업 = 너무 느림
- 100개 웹사이트 = 3일 걸림
- 반복 작업 = 손목 터널 증후군

문제 2: 코딩 = 너무 어려움
- Python 배우기 = 시간 없음
- XPath, CSS Selector = 뭔지 모름
- 오류 디버깅 = 불가능

문제 3: 데이터 정리 = 너무 복잡
- 각 웹사이트 구조 다름
- 엑셀 정리 = 또 다른 작업
- 이미지 다운로드 = 일일이 클릭
```

### 😊 Scraperr를 만난 후의 지훈

```
[Scraperr 웹 인터페이스]

1️⃣ URL 입력: competitor1.com
2️⃣ 추출할 데이터 클릭: 제품 이름, 가격
3️⃣ "스크래핑 시작" 버튼 클릭
4️⃣ ☕ 커피 마시며 대기
5️⃣ 📊 데이터 자동 수집 완료!
6️⃣ CSV 다운로드 → 엑셀에서 바로 열림

시간: 100개 웹사이트 = 2시간
코드: 한 줄도 안 씀!
정확도: 100%
```

**지훈의 변화:**
- ⏰ 3일 작업 → 2시간으로 단축
- 💻 코딩 지식 불필요
- 📊 데이터 자동 정리
- 🖼️ 이미지도 자동 다운로드
- 🔄 매일 자동 업데이트 가능
- 🚀 생산성 1500% 향상!

**상사의 반응:**
```
상사: "지훈씨, 벌써 다 했어요?
      데이터도 깔끔하고...
      비결이 뭐에요?"

지훈: "Scraperr라는 도구 사용했습니다!
      이제 매일 자동으로 업데이트할 수도 있어요!"

상사: "우와! 우리 팀 전체가 써야겠는데요!"
```

> **💡 일상 비유:** Scraperr는 마치 "로봇 비서"와 같습니다. 당신이 손으로 일일이 복사 붙여넣기 하던 작업을, 로봇이 24시간 쉬지 않고 대신해주는 똑똑한 도구예요!

---

## Scraperr란 무엇인가? (파인만 기법)

### 5살 아이에게 설명한다면...

"웹사이트에서 정보를 찾는 게임이 있다고 상상해봐. 너는 '빨간 사과' 사진 100개를 찾아야 해. 손으로 하나하나 찾으면 하루 종일 걸리지?

Scraperr는 **마법 로봇**이야! 네가 '빨간 사과 찾아줘'라고 하면, 로봇이 쏙쏙 찾아서 예쁜 앨범에 정리해주는 거야!"

### 중학생에게 설명한다면...

인터넷에는 엄청나게 많은 정보가 있어요. 예를 들어, 신발 쇼핑몰 100개에서 가격 비교를 하고 싶다면?

**수동 방법:**
1. 각 웹사이트 방문
2. 가격 확인하고 메모장에 적기
3. 100번 반복 → 3시간 소요 😰

**Scraperr 방법:**
1. 웹사이트 목록 입력
2. "가격" 위치 지정
3. 버튼 클릭
4. 10분 후 모든 데이터 자동 수집! 🎉

Scraperr는 이런 **반복 작업을 자동화**해주는 도구예요. 코딩 몰라도 괜찮아요!

### 개발자/전문가에게 정확하게 설명하면...

**Scraperr = 셀프 호스팅 웹 스크래핑 플랫폼 (No-Code)**

**핵심 구성:**
```
[아키텍처]
Frontend (Next.js + Tailwind CSS)
    ↕️ REST API
Backend (FastAPI + Python)
    ↕️ 데이터 저장
Database (MongoDB)
    ↕️ 스크래핑 실행
Scrapy/Playwright (엔진)
```

**주요 기능:**
- **XPath 기반 추출**: 정확한 DOM 요소 타겟팅
- **큐 시스템**: 여러 작업 동시 관리 및 우선순위 지정
- **도메인 스파이더링**: 사이트 전체 재귀적 크롤링
- **커스텀 헤더**: User-Agent, Cookie, Auth 설정
- **미디어 다운로드**: 이미지/동영상 자동 수집
- **결과 시각화**: 웹 UI에서 즉시 확인
- **데이터 내보내기**: CSV, Markdown 형식 지원
- **알림 채널**: 작업 완료 시 Slack/Email 등 통지

**기술 스택:**
- **Frontend**: Next.js 13+ (React Server Components), TypeScript
- **Backend**: FastAPI (async), Pydantic 모델
- **Database**: MongoDB (문서 지향 DB)
- **Scraping Engine**: Playwright (브라우저 자동화), Requests
- **Deployment**: Docker Compose, Kubernetes Helm Charts

---

## 핵심 개념 쉽게 이해하기

### 1. 🕷️ 웹 스크래핑 (Web Scraping)

**개념:** 웹사이트에서 자동으로 데이터를 추출하는 기술

**실생활 비유:**
```
도서관에서 책 정보 수집하기:

수동 방법 (사람):
- 책장 앞에 서기
- 책 한 권씩 꺼내기
- 제목, 저자, 출판사 메모
- 100권 = 3시간

자동 방법 (로봇):
- 카메라로 책장 스캔
- AI가 텍스트 인식
- 자동으로 표 작성
- 100권 = 5분

웹 스크래핑 = 로봇 방식!
```

**기술적 동작:**
```python
# 수동 방식 (사람이 하는 일)
1. 브라우저로 웹사이트 열기
2. 원하는 정보 찾기
3. Ctrl+C (복사)
4. Excel에 Ctrl+V (붙여넣기)
5. 다음 웹사이트로 이동
6. 반복...

# 스크래핑 (Scraperr가 하는 일)
1. HTTP 요청으로 HTML 가져오기
2. HTML 파싱 (구조 분석)
3. 원하는 데이터 추출
4. 데이터베이스에 저장
5. 다음 페이지 자동 이동
6. 전체 자동화!
```

**합법적 사용 예시:**
```
✅ 공개된 가격 정보 수집
✅ 뉴스 기사 제목 수집
✅ 제품 리뷰 분석
✅ 부동산 매물 정보
✅ 채용 공고 수집

⚠️ 주의사항:
- robots.txt 확인 필수
- 서비스 약관 준수
- 너무 빠르게 요청 X (서버 부하)
- 개인정보 수집 금지
```

---

### 2. 🎯 XPath (XML Path Language)

**개념:** 웹페이지에서 원하는 요소를 "주소"로 찾는 방법

**5살 아이 설명:**
```
큰 장난감 상자에서 빨간 자동차를 찾고 싶어.

방법 1: 손으로 하나하나 뒤지기 (느림)
방법 2: "2층 3번째 칸 왼쪽" 주소로 바로 찾기 (빠름)

XPath = 주소 시스템!
```

**실생활 비유:**
```
아파트 주소:
서울시 강남구 테헤란로 123 (도로)
→ 대림아파트 (건물)
→ 101동 (동)
→ 505호 (호수)

웹페이지 XPath:
/html (시작)
→ /body (본문)
→ /div[1] (첫 번째 박스)
→ /h1 (제목)
```

**HTML과 XPath:**
```html
<!-- 웹페이지 HTML 구조 -->
<html>
  <body>
    <div class="product">
      <h2>아이폰 15</h2>
      <span class="price">1,200,000원</span>
    </div>
  </body>
</html>
```

**XPath로 가격 찾기:**
```xpath
방법 1 (절대 경로):
/html/body/div/span[@class='price']

방법 2 (상대 경로 - 더 간단):
//span[@class='price']
→ 어디에 있든 class='price'인 span 찾기

방법 3 (텍스트 검색):
//span[contains(text(), '원')]
→ '원'이라는 텍스트 포함한 span 찾기
```

**Scraperr에서 XPath 사용:**
```
[Scraperr 인터페이스]

1️⃣ 웹사이트 열기
2️⃣ 추출하고 싶은 데이터 오른쪽 클릭
3️⃣ "Inspect" (검사) 선택
4️⃣ 개발자 도구에서 요소 오른쪽 클릭
5️⃣ "Copy" → "Copy XPath" 선택
6️⃣ Scraperr에 붙여넣기!

→ 코딩 몰라도 XPath 자동 생성!
```

---

### 3. 📊 DOM (Document Object Model)

**개념:** 웹페이지의 "설계도"

**일상 비유:**
```
집 짓기:

설계도 (DOM):
- 1층: 거실, 주방
- 2층: 침실1, 침실2
- 각 방의 위치와 크기 명시

실제 집 (웹페이지):
- 설계도대로 건설된 집
- 눈으로 보고 만질 수 있음

웹페이지도 동일!
HTML = 설계도 (DOM)
브라우저 화면 = 완성된 집
```

**HTML → DOM 변환:**
```html
<!-- HTML 코드 (텍스트) -->
<div>
  <h1>제목</h1>
  <p>내용</p>
</div>
```

```
DOM 트리 (구조):
div (부모)
├── h1 (자식1) "제목"
└── p (자식2) "내용"

마치 가계도처럼!
할아버지
├── 아빠
│   ├── 나
│   └── 동생
└── 삼촌
```

**Scraperr가 DOM 사용하는 방법:**
```
1. 웹페이지 접속
   → HTML 코드 다운로드

2. HTML → DOM 변환
   → 트리 구조로 파싱

3. XPath로 원하는 노드 찾기
   → //div[@class='price']

4. 텍스트 추출
   → "1,200,000원"

5. 데이터베이스 저장
   → MongoDB에 기록
```

---

### 4. 🔄 큐 시스템 (Queue System)

**개념:** 작업을 순서대로 처리하는 대기열

**일상 비유:**
```
은행 창구:

고객들이 줄 서기:
1번: 김철수 (예금)
2번: 이영희 (대출 상담)
3번: 박민수 (환전)

창구 직원:
- 1번부터 순서대로 처리
- 2번은 1번 끝날 때까지 대기
- 새 고객은 맨 뒤로

Scraperr 큐도 동일!
```

**Scraperr 큐 시스템:**
```
[스크래핑 작업 큐]

대기 중:
1. Amazon 제품 가격 (우선순위: 높음)
2. eBay 판매자 정보 (우선순위: 중간)
3. AliExpress 리뷰 (우선순위: 낮음)

실행 중:
🔄 Walmart 재고 정보 스크래핑...
   진행률: 47% (235/500 페이지)

완료:
✅ Target 할인 정보 (5분 전)
✅ Best Buy 배송비 (10분 전)
```

**큐의 장점:**
```
장점 1: 동시 실행 방지
- 웹사이트에 부담 안 줌
- 서버 과부하 방지

장점 2: 우선순위 관리
- 급한 작업 먼저 처리
- 중요도에 따라 순서 조정

장점 3: 실패 처리
- 작업 실패 시 재시도
- 오류 로그 자동 기록

장점 4: 자원 관리
- CPU, 메모리 효율적 사용
- 동시 작업 수 제한 가능
```

---

### 5. 🎨 CSS Selector vs XPath

**개념:** 웹 요소 찾는 두 가지 방법

**실생활 비유:**
```
친구 집 찾기:

방법 1 (주소 - XPath):
"서울시 강남구 테헤란로 123번길 45"
→ 정확한 위치

방법 2 (특징 - CSS):
"빨간 지붕, 2층집, 정원 있는 집"
→ 특징으로 찾기

둘 다 같은 집을 찾을 수 있음!
```

**CSS Selector:**
```css
/* 클래스로 찾기 */
.price  → class="price"인 요소

/* ID로 찾기 */
#title  → id="title"인 요소

/* 태그로 찾기 */
div  → 모든 div 요소

/* 조합 */
div.product > span.price
→ class="product"인 div 안의
   class="price"인 span
```

**XPath:**
```xpath
/* 클래스로 찾기 */
//span[@class='price']

/* ID로 찾기 */
//h1[@id='title']

/* 태그로 찾기 */
//div

/* 조합 */
//div[@class='product']/span[@class='price']
```

**비교:**
```
[속도]
CSS: ⚡⚡⚡ 빠름
XPath: ⚡⚡ 약간 느림

[표현력]
CSS: 🎯 간단한 선택
XPath: 🎯🎯🎯 복잡한 선택 가능

[호환성]
CSS: 브라우저 최적화
XPath: XML/HTML 모두 지원

[Scraperr 추천]
→ XPath 사용!
   (더 강력하고 유연함)
```

---

### 6. 🤖 헤드리스 브라우저 (Headless Browser)

**개념:** 화면 없이 실행되는 웹 브라우저

**5살 아이 설명:**
```
보통 게임:
- TV 화면 보면서 게임
- 눈으로 캐릭터 확인

헤드리스 (머리 없는) 게임:
- 화면 안 보고 게임
- 로봇이 자동으로 플레이
- 결과만 알려줌

웹사이트도 마찬가지!
```

**일반 브라우저 vs 헤드리스:**
```
[일반 Chrome]
1. 창 열림 👁️
2. 웹사이트 로딩 보임
3. 스크롤, 클릭 눈으로 확인
4. 느림 (화면 그리기 시간)

[헤드리스 Chrome]
1. 창 안 열림 (백그라운드)
2. 웹사이트 로딩 (보이지 않음)
3. 자동으로 스크롤, 클릭
4. 빠름 (화면 안 그려서)
```

**Scraperr에서 사용:**
```python
# Playwright (헤드리스 브라우저)
browser = playwright.chromium.launch(
    headless=True  # 화면 안 띄우기
)

page = browser.new_page()
page.goto('https://example.com')

# JavaScript로 렌더링된 내용도 가져옴!
content = page.content()

# 스크린샷도 가능
page.screenshot(path='page.png')
```

**장점:**
```
장점 1: 속도
- 화면 렌더링 안 해서 빠름
- CPU, GPU 자원 절약

장점 2: 서버 실행
- 화면 없는 서버에서도 작동
- 자동화 작업에 최적

장점 3: JavaScript 처리
- Ajax, React 등 동적 콘텐츠
- 일반 HTTP 요청으로 안 되는 것도 가능

장점 4: 대량 작업
- 여러 브라우저 동시 실행
- 수백 개 페이지 병렬 처리
```

---

### 7. 📦 도커 (Docker)

**개념:** 프로그램을 "상자"에 넣어서 어디서든 똑같이 실행

**일상 비유:**
```
컵라면:

일반 요리:
- 재료 사기 (야채, 면, 육수)
- 조리 도구 준비
- 레시피 따라 요리
- 집마다 맛이 달라질 수 있음

컵라면:
- 모든 재료가 컵에 포장됨
- 뜨거운 물만 부으면 됨
- 어디서든 똑같은 맛
- 실패 확률 0%

Docker = 소프트웨어 컵라면!
```

**전통 방식 vs Docker:**
```
[전통 방식]
설치 과정:
1. Python 3.11 설치
2. Node.js 18 설치
3. MongoDB 설치
4. 환경 변수 설정
5. 패키지 설치 (100개+)
6. 설정 파일 작성
→ 3시간 소요
→ 실패할 수 있음
→ 컴퓨터마다 다름

[Docker 방식]
docker-compose up
→ 5분 소요
→ 100% 작동 보장
→ 어디서든 동일
```

**Scraperr + Docker:**
```bash
# 1. 저장소 클론
git clone https://github.com/jaypyles/Scraperr.git
cd Scraperr

# 2. Docker로 실행 (끝!)
make up

# 내부에서 자동으로:
# - Python 환경 구성
# - Node.js 앱 빌드
# - MongoDB 시작
# - 모든 설정 완료
# - 웹 서버 실행

# 3. 브라우저 접속
http://localhost:3000
```

**Docker의 마법:**
```
[Docker 컨테이너 구조]

컨테이너 1: Frontend
- Next.js 앱
- 포트 3000

컨테이너 2: Backend
- FastAPI 서버
- 포트 8000

컨테이너 3: MongoDB
- 데이터베이스
- 포트 27017

→ 각각 독립적으로 실행
→ 서로 통신 가능
→ 하나 문제 생겨도 다른 건 OK
```

---

## 실전 단계별 예제

### 🌱 예제 1: 기초 - 첫 스크래핑 작업 (초보자용)

**시나리오:** 온라인 서점에서 책 제목과 가격 수집하기

#### 1단계: Scraperr 설치 및 실행

```bash
# 터미널 열기

# 1. 저장소 다운로드
git clone https://github.com/jaypyles/Scraperr.git
cd Scraperr

# 2. Docker로 실행
make up

# 출력:
# Creating network "scraperr_default"...
# Creating scraperr_mongodb_1...
# Creating scraperr_backend_1...
# Creating scraperr_frontend_1...
# ✓ Started successfully!
```

**🤔 Docker가 없으면?**
```bash
# Docker Desktop 설치
# Mac: https://docs.docker.com/desktop/install/mac-install/
# Windows: https://docs.docker.com/desktop/install/windows-install/

# 설치 확인
docker --version
# 출력: Docker version 24.0.0
```

#### 2단계: 웹 인터페이스 접속

```
브라우저 열기:
http://localhost:3000

[Scraperr 홈 화면]
┌─────────────────────────────────┐
│  🕷️ Scraperr                    │
│                                 │
│  [+ New Scraping Job]           │
│                                 │
│  Recent Jobs:                   │
│  (아직 없음)                     │
└─────────────────────────────────┘
```

#### 3단계: 첫 스크래핑 작업 생성

```
[+ New Scraping Job] 클릭

[설정 화면]
┌─────────────────────────────────┐
│  Job Name:                      │
│  [책 가격 수집]                  │
│                                 │
│  Target URL:                    │
│  [https://example-bookstore.com]│
│                                 │
│  XPath Selectors:               │
│  Title: [//h2[@class='book']]   │
│  Price: [//span[@class='price']]│
│                                 │
│  [Start Scraping] 버튼          │
└─────────────────────────────────┘
```

**XPath 찾는 방법:**
```
1. 목표 웹사이트 열기
   https://example-bookstore.com

2. 책 제목에 오른쪽 클릭
   → "검사" (Inspect) 선택

3. 개발자 도구 열림:
   <h2 class="book-title">해리포터</h2>
              ↑ 여기 오른쪽 클릭

4. "Copy" → "Copy XPath" 선택
   → //h2[@class='book-title']

5. Scraperr에 붙여넣기!
```

#### 4단계: 스크래핑 실행 및 결과 확인

```
[Start Scraping] 클릭!

[진행 상황]
┌─────────────────────────────────┐
│  Status: Running... 🔄          │
│  Progress: 15/50 pages          │
│  Time elapsed: 00:02:35         │
│                                 │
│  [Cancel] [View Log]            │
└─────────────────────────────────┘

... 2분 후 ...

[완료!]
┌─────────────────────────────────┐
│  Status: Completed ✅           │
│  Items scraped: 250 books       │
│  Time taken: 00:05:12           │
│                                 │
│  [View Results] [Export CSV]    │
└─────────────────────────────────┘
```

#### 5단계: 결과 확인 및 내보내기

```
[View Results] 클릭

[결과 테이블]
┌─────────────────────────────────────────────┐
│ Title              │ Price      │ URL       │
├────────────────────┼────────────┼───────────┤
│ 해리포터와 마법사의 돌│ 15,000원   │ /book/1  │
│ 반지의 제왕         │ 18,000원   │ /book/2  │
│ 어린왕자            │ 12,000원   │ /book/3  │
│ ...                │ ...        │ ...       │
└─────────────────────────────────────────────┘

[Export CSV] 클릭 → books.csv 다운로드
```

**CSV 파일 내용:**
```csv
title,price,url
해리포터와 마법사의 돌,15000,https://example.com/book/1
반지의 제왕,18000,https://example.com/book/2
어린왕자,12000,https://example.com/book/3
```

**Excel에서 열기:**
```
books.csv 더블클릭
→ Excel 자동 실행
→ 표 형태로 깔끔하게 정리됨!
→ 추가 분석 가능 (평균 가격, 정렬 등)
```

#### 전체 흐름 요약:

```
[데이터 흐름]
1. 웹사이트
   ↓ (HTML)
2. Scraperr Backend
   ↓ (파싱)
3. XPath 매칭
   ↓ (추출)
4. MongoDB 저장
   ↓ (조회)
5. Frontend 표시
   ↓ (다운로드)
6. CSV 파일
```

**🤔 생각해보기:**
- 수동으로 했다면 얼마나 걸렸을까요?
- XPath를 직접 작성하지 않고 복사만 했나요?
- 다른 웹사이트에도 똑같이 적용할 수 있을까요?

**✅ 성공 체크리스트:**
- [ ] Scraperr 설치 및 실행
- [ ] 웹 인터페이스 접속 성공
- [ ] XPath 추출 방법 이해
- [ ] 첫 스크래핑 작업 완료
- [ ] CSV 파일 다운로드 및 확인

---

### 🌿 예제 2: 중급 - 이미지 포함 상품 정보 수집 (실무 적용)

**시나리오:** 이커머스 사이트에서 제품명, 가격, 이미지, 리뷰 점수 수집

#### 프로젝트 목표:
```
수집 대상: 전자제품 쇼핑몰
데이터:
- 제품명
- 가격
- 이미지 URL
- 평점 (별점)
- 리뷰 수

목적: 경쟁사 가격 분석
```

#### 1단계: 복잡한 XPath 작성

**웹사이트 구조 분석:**
```html
<!-- 실제 HTML 예시 -->
<div class="product-card">
  <img src="/images/iphone15.jpg" alt="iPhone 15" />
  <h3 class="product-name">iPhone 15 Pro</h3>
  <div class="price-box">
    <span class="original">1,500,000원</span>
    <span class="sale">1,200,000원</span>
  </div>
  <div class="rating">
    <span class="stars">★★★★☆</span>
    <span class="count">(128)</span>
  </div>
</div>
```

**XPath 설계:**
```xpath
# 제품명
//h3[@class='product-name']/text()
→ "iPhone 15 Pro"

# 할인 가격 (없으면 원가)
//span[@class='sale']/text() | //span[@class='original']/text()
→ "1,200,000원"

# 이미지 URL
//div[@class='product-card']//img/@src
→ "/images/iphone15.jpg"

# 평점 (별 개수 세기)
count(//span[@class='stars']/text()[contains(., '★')])
→ 4

# 리뷰 수 (숫자만 추출)
translate(//span[@class='count']/text(), '()', '')
→ "128"
```

#### 2단계: Scraperr 설정 (고급)

```yaml
# Job Configuration
name: "전자제품 가격 비교"
url: "https://electronics-store.com/products"

selectors:
  product_name:
    xpath: "//h3[@class='product-name']/text()"
    type: "text"

  price:
    xpath: "//span[@class='sale']/text() | //span[@class='original']/text()"
    type: "text"
    transform: "extract_numbers"  # 1,200,000원 → 1200000

  image:
    xpath: "//div[@class='product-card']//img/@src"
    type: "url"
    download: true  # 이미지 자동 다운로드

  rating:
    xpath: "count(//span[@class='stars']/text()[contains(., '★')])"
    type: "number"

  review_count:
    xpath: "translate(//span[@class='count']/text(), '()', '')"
    type: "number"

pagination:
  enabled: true
  next_button: "//a[@class='next-page']"
  max_pages: 10

rate_limit:
  requests_per_second: 2  # 서버에 부담 안 주기
  delay_between_pages: 3  # 3초 대기
```

#### 3단계: 페이지네이션 처리

**여러 페이지 자동 수집:**
```
페이지 1: products?page=1 (20개 제품)
페이지 2: products?page=2 (20개 제품)
페이지 3: products?page=3 (20개 제품)
...
페이지 10: products?page=10 (20개 제품)

→ 총 200개 제품 자동 수집!
```

**Scraperr 페이지네이션 설정:**
```python
# 내부 동작 (참고용)
current_page = 1
max_pages = 10

while current_page <= max_pages:
    # 현재 페이지 스크래핑
    scrape_page(url + f"?page={current_page}")

    # "다음" 버튼 찾기
    next_button = find_element("//a[@class='next-page']")

    if next_button:
        next_button.click()
        current_page += 1
        time.sleep(3)  # 3초 대기
    else:
        break  # 더 이상 페이지 없음
```

#### 4단계: 이미지 다운로드 설정

```
[Scraperr Advanced Settings]

Image Download:
✅ Enable automatic image download
📁 Save location: ./downloads/images/
🏷️ Naming: {product_name}_{timestamp}.jpg

예시 다운로드:
./downloads/images/
├── iPhone_15_Pro_20240108_143022.jpg
├── Galaxy_S24_20240108_143025.jpg
├── MacBook_Pro_20240108_143028.jpg
└── ...
```

#### 5단계: 실시간 모니터링

```
[실행 중 대시보드]
┌──────────────────────────────────────────┐
│  Job: 전자제품 가격 비교                  │
│  Status: Running 🔄                      │
│                                          │
│  Progress: ████████░░ 80% (8/10 pages) │
│  Items: 156 products                     │
│  Images: 142 downloaded                  │
│  Errors: 3 (retrying...)                 │
│                                          │
│  Current: Page 8 - Loading...            │
│  ETA: 00:02:30                           │
│                                          │
│  [Pause] [Cancel] [View Log]            │
└──────────────────────────────────────────┘

[실시간 로그]
14:30:22 - Page 7 completed (20 items)
14:30:25 - Downloading image: iphone15.jpg
14:30:26 - Image saved: ./downloads/iphone15.jpg
14:30:27 - Moving to page 8...
14:30:30 - Page 8 loaded (20 items)
```

#### 6단계: 결과 분석 및 시각화

**수집된 데이터:**
```csv
product_name,price,image_path,rating,review_count
iPhone 15 Pro,1200000,./downloads/iphone15.jpg,4,128
Galaxy S24,1100000,./downloads/galaxys24.jpg,5,95
MacBook Pro,2500000,./downloads/macbook.jpg,4,67
```

**Excel에서 분석:**
```
=AVERAGE(B2:B200)  # 평균 가격
=MAX(D2:D200)      # 최고 평점
=SUM(E2:E200)      # 총 리뷰 수

피벗 테이블:
- 브랜드별 평균 가격
- 평점별 제품 분포
- 가격대별 리뷰 수
```

**대시보드 생성 (선택):**
```python
import pandas as pd
import matplotlib.pyplot as plt

# CSV 읽기
df = pd.read_csv('products.csv')

# 가격 분포 그래프
plt.hist(df['price'], bins=20)
plt.title('제품 가격 분포')
plt.xlabel('가격 (원)')
plt.ylabel('제품 수')
plt.show()

# 평점별 평균 가격
df.groupby('rating')['price'].mean().plot(kind='bar')
plt.title('평점별 평균 가격')
plt.show()
```

#### 실무 활용 팁:

**1. 스케줄링 (자동 실행):**
```bash
# cron으로 매일 아침 9시 실행
0 9 * * * cd /path/to/scraperr && make run-job electronics

# 매주 월요일 자동 업데이트
0 9 * * 1 cd /path/to/scraperr && make run-job electronics
```

**2. 알림 설정:**
```yaml
notifications:
  email:
    enabled: true
    recipients: ["team@company.com"]
    on: ["complete", "error"]

  slack:
    enabled: true
    webhook: "https://hooks.slack.com/..."
    channel: "#price-monitoring"
```

**3. 데이터 변화 감지:**
```python
# 이전 데이터와 비교
old_data = pd.read_csv('products_old.csv')
new_data = pd.read_csv('products_new.csv')

# 가격 변동 감지
price_changes = new_data[new_data['price'] != old_data['price']]

if not price_changes.empty:
    send_alert(f"가격 변동 감지: {len(price_changes)}개 제품")
```

**🤔 생각해보기:**
- 이미지 다운로드가 왜 필요한가요?
- 페이지네이션 없이 수동으로 한다면 얼마나 걸릴까요?
- 매일 자동으로 실행하면 어떤 인사이트를 얻을 수 있나요?

**✅ 중급 체크리스트:**
- [ ] 복잡한 XPath 작성
- [ ] 페이지네이션 설정
- [ ] 이미지 자동 다운로드
- [ ] 실시간 모니터링 경험
- [ ] CSV 데이터 분석
- [ ] 스케줄링 설정 (선택)

---

### 🌳 예제 3: 고급 - 동적 웹사이트 스크래핑 (심화 학습)

**시나리오:** JavaScript로 렌더링되는 SPA(Single Page Application) 스크래핑

#### 문제 상황:

**일반 스크래핑이 안 되는 웹사이트:**
```
문제 웹사이트 특징:
- React, Vue, Angular 등으로 제작
- 페이지 로드 시 빈 HTML
- JavaScript가 데이터 로딩
- 무한 스크롤
- Ajax로 동적 데이터 요청
```

**일반 스크래핑 실패 예:**
```python
# 일반 HTTP 요청
import requests
html = requests.get('https://spa-website.com').text

print(html)
# 출력:
# <div id="root"></div>  ← 비어있음!
# <script src="app.js"></script>

# XPath로 찾으려 해도:
# → 데이터 없음 (JavaScript가 아직 실행 안 됨)
```

#### 해결책: 헤드리스 브라우저 활용

**Scraperr의 Playwright 모드:**
```yaml
# Advanced Configuration
scraping_mode: "browser"  # HTTP 대신 브라우저 사용

browser_options:
  headless: true
  javascript_enabled: true
  wait_for: "networkidle"  # 모든 요청 완료까지 대기
  timeout: 30000  # 30초

page_actions:
  - action: "wait"
    selector: "//div[@class='product-list']"
    timeout: 10000

  - action: "scroll"
    direction: "bottom"
    times: 5  # 무한 스크롤 5번

  - action: "click"
    selector: "//button[@class='load-more']"
    optional: true
```

#### 1단계: 무한 스크롤 처리

**웹사이트 동작:**
```
사용자 스크롤 ↓
→ JavaScript 감지
→ Ajax 요청: /api/products?page=2
→ 새 제품 20개 로딩
→ 화면에 추가

반복...
```

**Scraperr 자동화:**
```python
# 내부 동작 (참고)
from playwright.sync_api import sync_playwright

with sync_playwright() as p:
    browser = p.chromium.launch(headless=True)
    page = browser.new_page()
    page.goto('https://spa-website.com')

    # 초기 로딩 대기
    page.wait_for_selector('//div[@class="product-list"]')

    # 무한 스크롤 시뮬레이션
    for i in range(5):
        # 페이지 끝까지 스크롤
        page.evaluate('window.scrollTo(0, document.body.scrollHeight)')

        # 새 콘텐츠 로딩 대기
        page.wait_for_timeout(2000)

        print(f"스크롤 {i+1}/5 완료")

    # 이제 모든 데이터 로드됨!
    html = page.content()
```

#### 2단계: Ajax 요청 가로채기

**고급 기법: 네트워크 요청 모니터링**
```python
# API 요청 직접 가로채기
page.on('response', lambda response:
    handle_response(response)
    if '/api/products' in response.url
    else None
)

def handle_response(response):
    if response.status == 200:
        data = response.json()
        # API 응답을 직접 파싱
        # HTML 파싱보다 훨씬 깔끔!
        for product in data['products']:
            print(f"{product['name']}: {product['price']}")
```

**Scraperr API 인터셉트 설정:**
```yaml
api_intercept:
  enabled: true
  endpoints:
    - pattern: "/api/products*"
      extract:
        products: "$.data.products[*]"
      fields:
        name: "$.name"
        price: "$.price"
        stock: "$.inventory.quantity"
```

#### 3단계: 로그인 필요한 사이트

**인증이 필요한 경우:**
```yaml
authentication:
  method: "form"  # 또는 "basic", "oauth"

  login_url: "https://website.com/login"

  credentials:
    username_field: "//input[@name='email']"
    password_field: "//input[@name='password']"
    submit_button: "//button[@type='submit']"

  username: "${ENV:SCRAPER_USERNAME}"  # 환경 변수
  password: "${ENV:SCRAPER_PASSWORD}"

  success_indicator: "//div[@class='user-menu']"

  session_management:
    cookies_file: "./cookies.json"
    reuse_session: true  # 세션 재사용
```

**실행 순서:**
```
1. 로그인 페이지 접속
   ↓
2. 이메일 입력
   ↓
3. 비밀번호 입력
   ↓
4. 로그인 버튼 클릭
   ↓
5. 로그인 성공 확인
   ↓
6. 쿠키 저장
   ↓
7. 실제 스크래핑 시작
   ↓
8. 다음 실행 시 쿠키 재사용
```

#### 4단계: CAPTCHA 우회 전략

**주의: 합법적 방법만!**
```yaml
captcha_handling:
  strategy: "manual"  # 자동 우회 X

  on_captcha_detected:
    - action: "pause"
    - action: "notify"
      message: "CAPTCHA 발견 - 수동 해결 필요"
    - action: "wait_for_user"

  retry:
    max_attempts: 3
    delay: 60000  # 1분 대기
```

**권장 방법:**
```
1. 요청 속도 줄이기
   - rate_limit: 1 request / 5 seconds

2. User-Agent 다양화
   - 실제 브라우저처럼 보이기

3. 쿠키/세션 유지
   - 로봇으로 안 보이게

4. 시간대 분산
   - 한꺼번에 많이 요청 X

5. API 사용 고려
   - 공식 API가 있다면 사용
```

#### 5단계: 오류 처리 및 재시도

**강력한 오류 처리:**
```yaml
error_handling:
  on_timeout:
    action: "retry"
    max_retries: 3
    backoff: "exponential"  # 1초, 2초, 4초...

  on_404:
    action: "skip"
    log: true

  on_500:
    action: "retry"
    max_retries: 5
    delay: 30000

  on_network_error:
    action: "retry"
    max_retries: 10

  on_parse_error:
    action: "log"
    save_html: true  # 디버깅용 HTML 저장
    continue: true
```

**실전 예시:**
```
[오류 발생]
14:30:15 - Page 5 loading...
14:30:45 - ERROR: Timeout (30s exceeded)
14:30:45 - Retry 1/3 in 1 second...
14:30:46 - Page 5 loading (retry)...
14:31:16 - ERROR: Timeout again
14:31:16 - Retry 2/3 in 2 seconds...
14:31:18 - Page 5 loading (retry)...
14:31:30 - SUCCESS: Page 5 loaded (23 items)
```

#### 6단계: 대규모 스크래핑 최적화

**병렬 처리:**
```yaml
performance:
  concurrent_jobs: 5  # 동시에 5개 작업

  worker_pool:
    size: 10  # 워커 10개
    distribution: "round-robin"

  caching:
    enabled: true
    ttl: 3600  # 1시간 캐시
    storage: "redis"

  database:
    batch_size: 100  # 100개씩 묶어서 저장
    connection_pool: 20
```

**모니터링:**
```python
# 성능 지표 추적
metrics = {
    "pages_scraped": 1250,
    "items_extracted": 25000,
    "errors": 15,
    "retry_count": 8,
    "avg_page_time": 2.3,  # 초
    "cache_hit_rate": 0.78,  # 78%
    "memory_usage": "450MB",
    "cpu_usage": "35%"
}
```

#### 7단계: 법적/윤리적 고려사항

**체크리스트:**
```yaml
legal_compliance:
  robots_txt:
    check: true
    respect: true
    user_agent: "ScraperBot/1.0 (Educational Purpose)"

  rate_limiting:
    enabled: true
    requests_per_second: 1
    respect_retry_after: true

  terms_of_service:
    acknowledged: true
    purpose: "Research and analysis"

  data_privacy:
    pii_filtering: true  # 개인정보 필터링
    gdpr_compliant: true

  attribution:
    source_url: true
    scrape_date: true
```

**robots.txt 확인:**
```
https://website.com/robots.txt

User-agent: *
Disallow: /admin/        ← 금지 영역
Disallow: /private/
Allow: /products/        ← 허용 영역
Crawl-delay: 5           ← 5초 간격 요청

→ Scraperr가 자동으로 준수!
```

#### 8단계: 실전 프로젝트 구조

**전문가 수준 구성:**
```
scraperr-project/
├── configs/
│   ├── base.yaml              # 공통 설정
│   ├── ecommerce.yaml         # 이커머스 특화
│   ├── news.yaml              # 뉴스 사이트
│   └── social.yaml            # 소셜 미디어
│
├── selectors/
│   ├── amazon.xpath           # 아마존 XPath
│   ├── ebay.xpath             # eBay XPath
│   └── common.xpath           # 공통 선택자
│
├── scripts/
│   ├── preprocess.py          # 전처리
│   ├── postprocess.py         # 후처리
│   └── analyze.py             # 데이터 분석
│
├── data/
│   ├── raw/                   # 원본 데이터
│   ├── processed/             # 가공 데이터
│   └── exports/               # 내보내기
│
├── logs/
│   ├── scraperr.log
│   ├── errors.log
│   └── performance.log
│
└── docker-compose.yml         # 전체 환경
```

**자동화 파이프라인:**
```bash
#!/bin/bash
# run-pipeline.sh

# 1. 환경 시작
docker-compose up -d

# 2. 스크래핑 실행
scraperr run --config configs/ecommerce.yaml

# 3. 데이터 검증
python scripts/validate.py

# 4. 전처리
python scripts/preprocess.py

# 5. 분석
python scripts/analyze.py

# 6. 리포트 생성
python scripts/generate_report.py

# 7. 알림 전송
python scripts/send_notification.py

# 8. 정리
docker-compose down
```

**🤔 생각해보기:**
- 정적 웹사이트와 동적 웹사이트의 차이는?
- API 인터셉트가 HTML 파싱보다 나은 이유는?
- 어떤 상황에서 병렬 처리가 필요한가요?
- 법적 문제를 피하려면 어떻게 해야 하나요?

**✅ 고급 체크리스트:**
- [ ] 동적 웹사이트 스크래핑
- [ ] 무한 스크롤 처리
- [ ] API 인터셉트 경험
- [ ] 로그인 자동화
- [ ] 오류 처리 구현
- [ ] 병렬 처리 최적화
- [ ] 법적 컴플라이언스 확인
- [ ] 전문가 수준 프로젝트 구조

---

## 실무 활용 시나리오

### 시나리오 1: 이커머스 가격 모니터링 - 마케터 수연

**배경:**
- 전자제품 유통 회사 마케팅팀
- 경쟁사 50개 실시간 가격 추적
- 매일 가격 변동 보고서 작성

**Before Scraperr:**
```
업무 프로세스:
1. 아침 9시: 경쟁사 웹사이트 50개 방문
2. 주요 제품 100개 가격 수동 확인
3. Excel 스프레드시트에 입력
4. 어제 가격과 비교
5. 변동 사항 보고서 작성
→ 소요 시간: 3시간
→ 실수: 빈번함
→ 스트레스: 극심함
```

**After Scraperr:**
```yaml
# price-monitoring.yaml
name: "Daily Price Monitor"
schedule: "0 9 * * *"  # 매일 아침 9시

targets:
  - name: "Competitor A"
    url: "https://competitor-a.com/electronics"
    selectors:
      product: "//div[@class='product']"
      name: ".//h3/text()"
      price: ".//span[@class='price']/text()"

  # ... 경쟁사 50개 ...

actions:
  - compare_with_yesterday
  - highlight_changes
  - generate_report
  - send_email

notifications:
  email:
    to: ["team@company.com"]
    subject: "일일 가격 변동 리포트"
    template: "price_report.html"
```

**결과:**
```
자동 실행:
- 09:00: 스크래핑 시작
- 09:15: 모든 데이터 수집 완료
- 09:20: 보고서 자동 생성
- 09:21: 이메일 발송

수연의 새로운 아침:
- 09:00: 출근
- 09:30: 보고서 확인 (이미 완성됨!)
- 10:00: 전략 회의 (3시간 절약!)

효과:
⏰ 3시간 → 15분
📊 정확도 99.9%
📧 자동 알림
💰 인사이트 기반 가격 전략
```

**대시보드 예시:**
```
[실시간 가격 대시보드]
┌─────────────────────────────────────────┐
│ 가격 변동 TOP 10                         │
├─────────────────────────────────────────┤
│ iPhone 15: -50,000원 (↓4.2%)           │
│ Galaxy S24: +30,000원 (↑2.8%)          │
│ MacBook: 변동 없음                       │
│ ...                                     │
├─────────────────────────────────────────┤
│ 알림: Competitor A가 10개 제품 할인!     │
│ 대응 필요: 경쟁력 확보 전략 수립          │
└─────────────────────────────────────────┘
```

---

### 시나리오 2: 부동산 데이터 수집 - 데이터 분석가 준호

**배경:**
- 부동산 투자 분석 스타트업
- 매물 정보 실시간 수집
- AI 가격 예측 모델 학습 데이터

**수집 대이터:**
```
부동산 포털 5개:
- 네이버 부동산
- 직방
- 다방
- 호갱노노
- 피터팬

수집 정보:
- 주소
- 면적
- 가격 (매매/전세/월세)
- 층수
- 방향
- 옵션 (주차, 엘리베이터)
- 사진
- 등록일
```

**Scraperr 설정:**
```yaml
# real-estate-scraper.yaml
targets:
  naver:
    url: "https://land.naver.com/"
    search:
      region: "강남구"
      type: "apartment"
    selectors:
      address: "//div[@class='address']/text()"
      price: "//span[@class='price']/text()"
      area: "//span[@class='area']/text()"
      floor: "//span[@class='floor']/text()"
      images: "//img[@class='photo']/@src"

  jikbang:
    url: "https://www.jikbang.com/"
    api_mode: true  # API 사용
    endpoint: "/api/apartments"

  # 다른 사이트들...

data_pipeline:
  - deduplicate: true  # 중복 제거
  - geocoding: true    # 좌표 변환
  - price_normalize: true  # 가격 정규화
  - image_download: true

export:
  database: "postgresql://localhost/realestate"
  table: "listings"
  update_strategy: "upsert"  # 업데이트 또는 삽입
```

**데이터 활용:**
```python
# 분석 예시
import pandas as pd

# Scraperr가 수집한 데이터 로드
df = pd.read_sql('listings', con=engine)

# 1. 지역별 평균 가격
df.groupby('region')['price'].mean()

# 2. 가격 추세 분석
df.groupby('date')['price'].mean().plot()

# 3. AI 모델 학습
from sklearn.ensemble import RandomForestRegressor
model = RandomForestRegressor()
model.fit(df[features], df['price'])

# 4. 가격 예측
predicted_price = model.predict(new_listing)
```

**실제 효과:**
```
데이터 규모:
- 일일 신규 매물: 5,000건
- 누적 데이터: 500만 건
- 이미지: 2,000만 장

AI 모델 성능:
- 가격 예측 정확도: 92%
- 투자 수익률: +15%
- 시장 트렌드 예측: 85%

비즈니스 임팩트:
- 데이터 수집 자동화
- 실시간 시장 분석
- 투자자 의사결정 지원
```

---

### 시나리오 3: 뉴스 기사 수집 - 미디어 리서처 지영

**배경:**
- 여론 조사 기관
- 일일 뉴스 2,000개 분석
- 키워드 트렌드 분석

**수집 전략:**
```yaml
# news-aggregator.yaml
name: "Daily News Aggregator"

sources:
  - name: "Naver News"
    url: "https://news.naver.com/"
    categories: ["정치", "경제", "사회", "문화"]

  - name: "Daum News"
    url: "https://news.daum.net/"

  # ... 10개 언론사 ...

selectors:
  article:
    title: "//h2[@class='headline']/text()"
    content: "//div[@class='article-body']//text()"
    author: "//span[@class='author']/text()"
    date: "//time/@datetime"
    category: "//span[@class='category']/text()"

filtering:
  keywords: ["AI", "기술", "스타트업"]
  date_range: "today"
  min_length: 500  # 최소 500자

nlp_processing:
  sentiment_analysis: true
  keyword_extraction: true
  entity_recognition: true
  summarization: true
```

**자연어 처리 파이프라인:**
```python
# NLP 분석
from transformers import pipeline

# 감정 분석
sentiment = pipeline("sentiment-analysis")
result = sentiment("이 기사는 긍정적인 내용입니다.")

# 키워드 추출
from keybert import KeyBERT
kw_model = KeyBERT()
keywords = kw_model.extract_keywords(article_text, top_n=10)

# 요약
summarizer = pipeline("summarization")
summary = summarizer(article_text, max_length=150)

# 결과 저장
save_to_database({
    'title': title,
    'content': content,
    'summary': summary,
    'sentiment': result,
    'keywords': keywords
})
```

**대시보드 시각화:**
```
[트렌드 분석 대시보드]
┌─────────────────────────────────────────┐
│ 오늘의 핫 키워드                         │
├─────────────────────────────────────────┤
│ 1. AI          ████████████ (1,234회)  │
│ 2. ChatGPT     ██████████   (987회)    │
│ 3. 반도체      ████████     (756회)    │
│ 4. 스타트업    ██████       (543회)    │
│ 5. 투자        ████         (421회)    │
├─────────────────────────────────────────┤
│ 감정 분석                                │
│ 긍정: 60% ████████                      │
│ 중립: 30% ████                          │
│ 부정: 10% ██                            │
└─────────────────────────────────────────┘
```

---

### 시나리오 4: 학술 논문 메타데이터 수집 - 연구원 민석

**배경:**
- 대학원 박사과정
- 문헌 리뷰를 위한 논문 수집
- 연구 트렌드 분석

**수집 대상:**
```
학술 데이터베이스:
- Google Scholar
- arXiv
- PubMed
- IEEE Xplore
- ACM Digital Library

메타데이터:
- 제목
- 저자
- 초록
- 키워드
- 인용 수
- 출판 연도
- 학술지
- DOI
```

**Scraperr 설정:**
```yaml
# academic-scraper.yaml
name: "Research Paper Aggregator"

search_query: "machine learning healthcare"
date_range: "2020-2024"

sources:
  google_scholar:
    url: "https://scholar.google.com/scholar"
    params:
      q: "${SEARCH_QUERY}"
      hl: "en"
      as_ylo: "2020"
      as_yhi: "2024"

    selectors:
      paper: "//div[@class='gs_ri']"
      title: ".//h3/a/text()"
      authors: ".//div[@class='gs_a']/text()"
      abstract: ".//div[@class='gs_rs']/text()"
      citations: ".//div[@class='gs_fl']//a[contains(text(), 'Cited')]/text()"
      pdf: ".//div[@class='gs_or_ggsm']//a/@href"

  arxiv:
    api: true
    endpoint: "http://export.arxiv.org/api/query"
    params:
      search_query: "${SEARCH_QUERY}"
      max_results: 1000

post_processing:
  - parse_citations
  - download_pdfs
  - extract_references
  - build_citation_network
```

**네트워크 분석:**
```python
import networkx as nx
import matplotlib.pyplot as plt

# 인용 네트워크 구축
G = nx.DiGraph()

for paper in papers:
    G.add_node(paper['id'], title=paper['title'])
    for ref in paper['references']:
        G.add_edge(paper['id'], ref)

# 중요 논문 찾기 (PageRank)
pagerank = nx.pagerank(G)
top_papers = sorted(pagerank.items(), key=lambda x: x[1], reverse=True)[:10]

# 시각화
nx.draw(G, with_labels=True, node_color='lightblue')
plt.show()
```

---

### 시나리오 5: 소셜 미디어 트렌드 분석 - 마케터 혜진

**배경:**
- 브랜드 매니지먼트 회사
- 실시간 브랜드 모니터링
- 경쟁사 소셜 미디어 분석

**수집 데이터:**
```
플랫폼:
- Twitter (X)
- Instagram (공개 계정)
- Reddit
- Product Hunt
- Hacker News

지표:
- 게시물 수
- 좋아요/댓글/공유
- 해시태그 트렌드
- 감정 분석
- 인플루언서 멘션
```

**Scraperr 설정:**
```yaml
# social-media-monitor.yaml
brand: "OurBrand"
competitors: ["CompA", "CompB", "CompC"]

twitter:
  keywords: ["#OurBrand", "@OurBrand"]
  track_mentions: true
  sentiment: true

  selectors:
    tweet: "//article[@data-testid='tweet']"
    text: ".//div[@data-testid='tweetText']/text()"
    likes: ".//button[@data-testid='like']/@aria-label"
    retweets: ".//button[@data-testid='retweet']/@aria-label"

instagram:
  accounts: ["@ourbrand_official"]
  hashtags: ["#OurBrand"]

  metrics:
    - followers_count
    - engagement_rate
    - post_frequency

alerts:
  - condition: "sentiment < -0.5"
    action: "send_urgent_email"
    message: "부정적 멘션 급증!"

  - condition: "mentions > 1000"
    action: "send_notification"
    message: "버즈 발생!"
```

**실시간 대시보드:**
```
[브랜드 모니터링 대시보드]
┌─────────────────────────────────────────┐
│ 오늘의 멘션: 1,234 (+45% vs 어제)        │
│ 감정 지수: 0.65 (긍정적)                 │
│ Top 해시태그: #OurBrand, #Innovation     │
├─────────────────────────────────────────┤
│ 🔥 버즈 알림:                            │
│ - 인플루언서 A님이 제품 리뷰 (10K 좋아요)│
│ - Reddit 인기 게시물 1위!                │
├─────────────────────────────────────────┤
│ ⚠️ 주의 필요:                            │
│ - 고객 불만 트윗 증가 (배송 지연 관련)   │
│ - 경쟁사 B 신제품 출시 반응 긍정적        │
└─────────────────────────────────────────┘
```

**자동 대응:**
```python
# 자동 대응 시스템
if negative_sentiment > 0.7:
    alert_customer_service()
    prepare_response_template()
    track_issue_resolution()

if viral_post_detected:
    notify_marketing_team()
    prepare_engagement_strategy()
    amplify_positive_content()
```

---

## 문제 해결 가이드

### 자주 발생하는 문제와 해결책

#### 문제 1: "XPath가 작동하지 않아요"

**증상:**
```
XPath: //div[@class='price']/text()
결과: 데이터 없음 (빈 배열)
```

**원인 체크리스트:**
```
□ 웹페이지가 JavaScript로 렌더링되나요?
□ 클래스 이름이 정확한가요? (공백, 대소문자)
□ 요소가 iframe 안에 있나요?
□ 동적으로 생성되는 요소인가요?
```

**해결책 1: 브라우저 개발자 도구로 확인**
```
1. 웹페이지에서 F12 (개발자 도구)
2. Elements 탭
3. Ctrl+F (검색)
4. XPath 입력: //div[@class='price']
5. 결과 확인:
   - 0 results → XPath 잘못됨
   - 1 of 5 → 여러 개 있음 (더 구체적으로)
```

**해결책 2: CSS Selector 시도**
```xpath
# XPath (작동 안 함)
//div[@class='price']/text()

# CSS Selector (시도)
div.price

# Scraperr 설정
selector_type: "css"
selector: "div.price"
```

**해결책 3: 브라우저 모드 사용**
```yaml
# JavaScript 렌더링 필요한 경우
scraping_mode: "browser"
wait_for: "//div[@class='price']"
timeout: 10000
```

---

#### 문제 2: "스크래핑이 너무 느려요"

**증상:**
```
100개 페이지 스크래핑
예상 시간: 10분
실제 시간: 2시간 😱
```

**원인:**
```
1. 페이지 로딩 시간이 김
2. 불필요한 리소스 다운로드 (이미지, CSS)
3. JavaScript 실행 대기
4. 단일 스레드 실행
```

**해결책 1: 리소스 차단**
```yaml
browser_options:
  block_resources:
    - "image"
    - "stylesheet"
    - "font"
    - "media"

# 속도 향상: 60% → 80%
```

**해결책 2: 병렬 처리**
```yaml
performance:
  concurrent_workers: 5  # 5개 동시 실행

# 시간 단축: 2시간 → 24분
```

**해결책 3: 캐싱**
```yaml
caching:
  enabled: true
  ttl: 3600  # 1시간

# 반복 실행 시 속도: 10배 향상
```

**해결책 4: HTTP 모드로 전환**
```yaml
# 가능하면 브라우저 대신 HTTP 사용
scraping_mode: "http"  # browser → http

# 속도 향상: 10배
```

---

#### 문제 3: "403 Forbidden 오류"

**증상:**
```
HTTP Error 403: Forbidden
Access Denied
```

**원인:**
```
웹사이트가 봇으로 인식:
- User-Agent가 Python
- 너무 빠른 요청
- 쿠키/세션 없음
- IP 블랙리스트
```

**해결책 1: User-Agent 설정**
```yaml
headers:
  User-Agent: "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36"
  Accept: "text/html,application/xhtml+xml"
  Accept-Language: "ko-KR,ko;q=0.9,en-US;q=0.8,en;q=0.7"
```

**해결책 2: 속도 제한**
```yaml
rate_limit:
  requests_per_second: 1  # 초당 1개
  delay_between_pages: 3   # 3초 대기
```

**해결책 3: 세션 유지**
```yaml
session:
  enabled: true
  cookies_file: "./cookies.json"

# 첫 방문 후 쿠키 저장
# 다음 방문 시 재사용
```

**해결책 4: 프록시 사용**
```yaml
proxy:
  enabled: true
  type: "rotating"
  list:
    - "http://proxy1:8080"
    - "http://proxy2:8080"

# IP 로테이션으로 차단 우회
```

---

#### 문제 4: "데이터가 중복돼요"

**증상:**
```
같은 제품이 여러 번 저장됨:
- iPhone 15 (5회)
- Galaxy S24 (3회)
```

**원인:**
```
1. 페이지네이션 중복
2. 고유 키 없음
3. 재실행 시 중복 저장
```

**해결책 1: 중복 제거 설정**
```yaml
data_processing:
  deduplicate: true
  unique_key: "product_id"  # 또는 "url"
```

**해결책 2: 데이터베이스 UPSERT**
```yaml
export:
  database: "postgresql://..."
  table: "products"
  strategy: "upsert"  # 있으면 업데이트, 없으면 삽입
  conflict_column: "product_id"
```

**해결책 3: 스크래핑 전 확인**
```python
# 커스텀 스크립트
def before_scrape(url):
    # 이미 스크래핑했는지 확인
    if url in already_scraped:
        return "skip"
    return "proceed"
```

---

#### 문제 5: "MongoDB 연결 오류"

**증상:**
```
MongoDBConnectionError: Connection refused
Cannot connect to MongoDB at localhost:27017
```

**원인:**
```
1. MongoDB 서버 실행 안 됨
2. 포트 충돌
3. 인증 실패
4. 방화벽 차단
```

**해결책 1: MongoDB 상태 확인**
```bash
# Docker 컨테이너 확인
docker ps

# 출력에 mongodb 있어야 함
# CONTAINER ID   IMAGE       STATUS
# abc123         mongo:latest   Up 5 minutes

# 없으면 시작
docker-compose up -d mongodb
```

**해결책 2: 연결 정보 확인**
```yaml
# .env 파일
MONGODB_URI=mongodb://localhost:27017/scraperr
MONGODB_USER=admin
MONGODB_PASSWORD=password123

# Scraperr 설정
database:
  type: "mongodb"
  uri: "${MONGODB_URI}"
  username: "${MONGODB_USER}"
  password: "${MONGODB_PASSWORD}"
```

**해결책 3: 방화벽 확인**
```bash
# Mac
sudo lsof -i :27017

# 아무것도 안 나오면 포트 열기
sudo ufw allow 27017
```

---

#### 문제 6: "메모리 부족 오류"

**증상:**
```
MemoryError: Unable to allocate array
Process killed (OOM)
```

**원인:**
```
대량 데이터 한 번에 로딩:
- 10,000개 이미지 동시 다운로드
- 거대한 HTML 파싱
- 메모리에 모든 결과 저장
```

**해결책 1: 배치 처리**
```yaml
data_processing:
  batch_size: 100  # 100개씩 처리

# 10,000개를 100개씩 100번 처리
```

**해결책 2: 스트리밍**
```python
# 한 번에 다 로드 (나쁨)
all_data = load_all()  # MemoryError!

# 스트리밍 (좋음)
for item in stream_data():
    process(item)
    save(item)
    # 메모리 해제
```

**해결책 3: 리소스 제한**
```yaml
resource_limits:
  max_memory: "2GB"
  max_images_in_queue: 50
```

---

#### 문제 7: "인코딩 오류 (한글 깨짐)"

**증상:**
```
제품명: "������ 15"
정상: "아이폰 15"
```

**원인:**
```
인코딩 불일치:
웹페이지: EUC-KR
Scraperr: UTF-8
```

**해결책 1: 인코딩 명시**
```yaml
encoding:
  input: "euc-kr"  # 웹페이지 인코딩
  output: "utf-8"  # 저장 인코딩
```

**해결책 2: 자동 감지**
```yaml
encoding:
  auto_detect: true

# chardet 라이브러리로 자동 감지
```

**해결책 3: 데이터베이스 설정**
```sql
-- PostgreSQL
CREATE DATABASE scraperr
WITH ENCODING 'UTF8'
LC_COLLATE = 'ko_KR.UTF-8'
LC_CTYPE = 'ko_KR.UTF-8';

-- MongoDB
# 자동으로 UTF-8 사용
```

---

#### 문제 8: "Docker 빌드 실패"

**증상:**
```bash
$ make up
Error: Cannot connect to Docker daemon
```

**원인:**
```
1. Docker Desktop 실행 안 됨
2. Docker 권한 없음
3. 포트 충돌
```

**해결책 1: Docker 시작**
```bash
# Mac
open -a Docker

# 또는 Docker Desktop 아이콘 클릭

# 확인
docker ps
# 정상이면 빈 리스트 출력
```

**해결책 2: 권한 설정 (Linux)**
```bash
# 사용자를 docker 그룹에 추가
sudo usermod -aG docker $USER

# 로그아웃 후 재로그인
# 또는 세션 갱신
newgrp docker

# 확인
docker ps
```

**해결책 3: 포트 확인**
```bash
# 3000번 포트 사용 중인지 확인
lsof -i :3000

# 사용 중이면 종료
kill -9 <PID>

# 또는 다른 포트 사용
# docker-compose.yml 수정
ports:
  - "3001:3000"  # 3001로 변경
```

---

## 고급 활용 패턴

### 패턴 1: 증분 스크래핑 (Incremental Scraping)

**개념:** 전체가 아닌 변경 사항만 수집

**사용 시나리오:**
```
일일 뉴스 사이트:
- 전체 스크래핑: 10,000개 기사 (2시간)
- 증분 스크래핑: 신규 200개만 (5분)
```

**구현:**
```yaml
# incremental-scraper.yaml
scraping_strategy: "incremental"

checkpoint:
  enabled: true
  storage: "./checkpoints/last_scrape.json"

  criteria:
    - field: "publish_date"
      operator: ">"
      value: "${LAST_SCRAPE_TIME}"

  - field: "article_id"
      operator: "not in"
      value: "${SCRAPED_IDS}"

on_complete:
  - update_checkpoint
  - save_scrape_time
```

**Python 구현 예시:**
```python
import json
from datetime import datetime

# 마지막 스크래핑 시간 로드
try:
    with open('checkpoint.json', 'r') as f:
        checkpoint = json.load(f)
        last_scrape = checkpoint['last_scrape_time']
except:
    last_scrape = '2000-01-01'  # 처음 실행

# 신규 항목만 필터링
new_items = []
for item in all_items:
    if item['date'] > last_scrape:
        new_items.append(item)

# 처리
process(new_items)

# 체크포인트 업데이트
checkpoint = {
    'last_scrape_time': datetime.now().isoformat(),
    'items_scraped': len(new_items)
}
with open('checkpoint.json', 'w') as f:
    json.dump(checkpoint, f)
```

---

### 패턴 2: 멀티소스 데이터 통합

**개념:** 여러 웹사이트 데이터를 하나로 합치기

**시나리오:**
```
제품 정보 통합:
- 사이트 A: 가격
- 사이트 B: 리뷰
- 사이트 C: 재고
→ 통합 데이터베이스
```

**설정:**
```yaml
# multi-source-integration.yaml
sources:
  price_source:
    url: "https://store-a.com"
    selector: "//span[@class='price']"
    field: "price"

  review_source:
    url: "https://reviews-b.com"
    selector: "//div[@class='rating']"
    field: "rating"

  stock_source:
    url: "https://inventory-c.com"
    api: true
    endpoint: "/api/stock"
    field: "stock"

integration:
  key: "product_id"  # 통합 키
  strategy: "merge"

  output:
    database: "postgresql://..."
    table: "unified_products"
```

**데이터 매핑:**
```python
# 데이터 정규화 및 통합
def integrate_data():
    # 소스 1: 가격
    prices = scrape_source('price_source')
    # [{'id': 'P001', 'price': 10000}, ...]

    # 소스 2: 리뷰
    reviews = scrape_source('review_source')
    # [{'id': 'P001', 'rating': 4.5}, ...]

    # 소스 3: 재고
    stocks = scrape_source('stock_source')
    # [{'id': 'P001', 'stock': 50}, ...]

    # 통합
    integrated = {}
    for price in prices:
        pid = price['id']
        integrated[pid] = {'price': price['price']}

    for review in reviews:
        pid = review['id']
        if pid in integrated:
            integrated[pid]['rating'] = review['rating']

    for stock in stocks:
        pid = stock['id']
        if pid in integrated:
            integrated[pid]['stock'] = stock['stock']

    return list(integrated.values())
```

---

### 패턴 3: 이벤트 기반 스크래핑

**개념:** 특정 조건 발생 시 자동 실행

**트리거 예시:**
```
1. 가격 변동 감지
   → 즉시 스크래핑
   → 알림 발송

2. 새 제품 출시
   → 정보 수집
   → 데이터베이스 추가

3. 재고 부족
   → 경쟁사 재고 확인
   → 가격 조정 제안
```

**설정:**
```yaml
# event-driven-scraper.yaml
events:
  price_drop:
    monitor:
      url: "https://store.com/iphone"
      selector: "//span[@class='price']"
      interval: 3600  # 1시간마다 체크

    trigger:
      condition: "price < 1000000"  # 100만원 미만
      action:
        - scrape_competitor_prices
        - send_notification
        - update_recommendation

  new_product:
    monitor:
      url: "https://store.com/new-arrivals"
      selector: "//div[@class='product-card']"
      interval: 7200

    trigger:
      condition: "count > previous_count"
      action:
        - scrape_product_details
        - analyze_market_position
        - generate_report
```

**Webhook 통합:**
```python
from flask import Flask, request
app = Flask(__name__)

@app.route('/webhook/price-alert', methods=['POST'])
def price_alert():
    data = request.json
    product_id = data['product_id']

    # 즉시 스크래핑 시작
    scraperr.run_job(f'emergency-scrape-{product_id}')

    return {'status': 'triggered'}
```

---

### 패턴 4: AI 기반 데이터 검증

**개념:** 수집한 데이터를 AI로 검증

**검증 항목:**
```
1. 가격 이상치 탐지
   - 평소 10만원 → 갑자기 1억원?
   - 오류 가능성 높음

2. 이미지 품질 확인
   - 너무 작은 이미지
   - 깨진 이미지

3. 텍스트 품질
   - 의미 없는 문자열
   - HTML 태그 포함
```

**AI 검증 파이프라인:**
```python
from sklearn.ensemble import IsolationForest

# 1. 이상치 탐지 모델 학습
prices = [item['price'] for item in historical_data]
model = IsolationForest()
model.fit(prices)

# 2. 신규 데이터 검증
for new_item in scraped_data:
    prediction = model.predict([[new_item['price']]])

    if prediction == -1:  # 이상치
        flag_for_review(new_item)
        notify_admin(f"이상 가격: {new_item['price']}")

# 3. 이미지 검증
from PIL import Image

for item in scraped_data:
    img = Image.open(item['image_path'])

    # 크기 확인
    if img.width < 100 or img.height < 100:
        item['quality_score'] = 'low'

    # 선명도 확인
    if is_blurry(img):
        item['quality_score'] = 'blurry'

# 4. 텍스트 검증
import re

for item in scraped_data:
    text = item['description']

    # HTML 태그 포함 여부
    if re.search(r'<[^>]+>', text):
        text = strip_html(text)

    # 최소 길이
    if len(text) < 50:
        item['quality_score'] = 'too_short'
```

---

### 패턴 5: 분산 스크래핑 아키텍처

**개념:** 여러 서버에서 동시 스크래핑

**시나리오:**
```
대규모 스크래핑:
- 목표: 100만 개 페이지
- 단일 서버: 10일 소요
- 10개 서버: 1일 완료
```

**아키텍처:**
```
[Master Server]
     ↓ 작업 분배
[Worker 1] [Worker 2] [Worker 3] ... [Worker 10]
     ↓ 결과 수집
[Central Database]
```

**구현:**
```yaml
# master-config.yaml
coordinator:
  mode: "master"
  workers: 10

  task_distribution:
    strategy: "dynamic"  # 동적 로드 밸런싱
    queue: "redis://localhost:6379"

# worker-config.yaml
coordinator:
  mode: "worker"
  master: "http://master-server:8000"

  resources:
    cpu: 4
    memory: "8GB"
    concurrent_jobs: 5
```

**Redis 큐:**
```python
import redis
from rq import Queue

# Master: 작업 추가
queue = Queue(connection=redis.Redis())

for url in urls:
    queue.enqueue(scrape_page, url)

# Worker: 작업 처리
from rq import Worker

worker = Worker(['default'], connection=redis.Redis())
worker.work()
```

---

### 패턴 6: 실시간 스크래핑 스트림

**개념:** 데이터를 스트림으로 처리

**사용 사례:**
```
실시간 주식 가격:
- 1초마다 업데이트
- 즉시 분석 필요
- 대시보드 실시간 표시
```

**Kafka 통합:**
```yaml
# streaming-scraper.yaml
output:
  type: "kafka"
  topic: "stock-prices"
  bootstrap_servers: "localhost:9092"

scraping:
  mode: "continuous"
  interval: 1000  # 1초
  targets:
    - url: "https://finance.yahoo.com/quote/AAPL"
      selector: "//span[@data-symbol='AAPL']"
```

**Kafka Consumer:**
```python
from kafka import KafkaConsumer
import json

consumer = KafkaConsumer(
    'stock-prices',
    bootstrap_servers=['localhost:9092'],
    value_deserializer=lambda m: json.loads(m.decode('utf-8'))
)

for message in consumer:
    price_data = message.value

    # 실시간 처리
    analyze(price_data)
    update_dashboard(price_data)
    check_alerts(price_data)
```

---

## 용어 사전

### A

**API (Application Programming Interface)**
```
쉬운 설명: 프로그램끼리 대화하는 방법
일상 비유: 레스토랑 메뉴판

설명:
- 웹사이트가 데이터를 주는 공식적인 방법
- HTML 스크래핑보다 안정적
- 구조화된 데이터 (JSON, XML)

예시:
# API 요청
GET https://api.store.com/products
→ JSON 응답:
{
  "products": [
    {"id": 1, "name": "iPhone", "price": 1000000}
  ]
}

장점:
✅ 빠름
✅ 깔끔한 데이터
✅ 공식 지원

단점:
❌ API 없는 사이트 많음
❌ 요청 제한 있을 수 있음
```

---

### B

**Bot (봇)**
```
쉬운 설명: 자동으로 작동하는 프로그램
일상 비유: 로봇 청소기

종류:
1. 좋은 봇:
   - 검색엔진 크롤러 (Google Bot)
   - 가격 모니터링
   - 데이터 분석

2. 나쁜 봇:
   - 스팸 봇
   - DDoS 공격
   - 티켓 매크로

Scraperr는:
- 좋은 봇
- 합법적 데이터 수집
- 웹사이트 규칙 준수
```

---

### C

**Crawling (크롤링)**
```
쉬운 설명: 웹사이트를 돌아다니며 링크 따라가기
일상 비유: 도서관에서 책 찾기

vs Scraping:
Crawling = 여행 (사이트 탐색)
Scraping = 수집 (데이터 추출)

예시:
1. 메인 페이지 방문
2. 링크 수집
3. 각 링크 방문
4. 더 많은 링크 발견
5. 반복...

Scraperr의 크롤링:
spider_mode: true
max_depth: 3  # 3단계까지
follow_links: "//a[@class='product']"
```

---

**CSS Selector (CSS 선택자)**
```
쉬운 설명: 웹 요소를 찾는 방법 (XPath의 대안)
문법이 더 간단함

기본 문법:
.classname  → class로 찾기
#idname     → id로 찾기
tag         → 태그로 찾기
[attr=val]  → 속성으로 찾기

예시:
HTML:
<div class="price">1,000원</div>

CSS Selector:
.price  또는  div.price

XPath (비교):
//div[@class='price']

언제 사용:
- 간단한 선택
- 브라우저 친화적
- 속도 중요할 때
```

---

### D

**DOM (Document Object Model)**
```
쉬운 설명: 웹페이지의 가계도
일상 비유: 가족 관계도

구조:
document (최상위)
├── html
    ├── head
    │   ├── title
    │   └── meta
    └── body
        ├── div
        │   ├── h1
        │   └── p
        └── div

용어:
- 부모 (Parent): 위 요소
- 자식 (Child): 아래 요소
- 형제 (Sibling): 같은 레벨

왜 중요한가:
스크래핑 = DOM 트리 탐색
```

---

### E

**ETL (Extract, Transform, Load)**
```
쉬운 설명: 데이터 처리의 3단계
일상 비유: 요리 과정

Extract (추출):
- 웹사이트에서 데이터 가져오기
- Scraperr의 역할

Transform (변환):
- 데이터 정제 (HTML 태그 제거)
- 형식 변환 (날짜, 숫자)
- 중복 제거

Load (적재):
- 데이터베이스에 저장
- CSV 파일 생성

전체 흐름:
웹사이트 → 스크래핑 → 정제 → 데이터베이스
```

---

### F

**FastAPI**
```
쉬운 설명: 빠른 Python 웹 프레임워크
Scraperr의 백엔드

특징:
- 매우 빠름 ⚡
- 자동 API 문서
- 비동기 지원
- 타입 체크

Scraperr에서 역할:
프론트엔드 ↔ FastAPI ↔ 스크래핑 엔진
         요청        작업 실행
```

---

### H

**HTML (HyperText Markup Language)**
```
쉬운 설명: 웹페이지의 뼈대
일상 비유: 건물 설계도

구조:
<html>
  <body>
    <h1>제목</h1>
    <p>내용</p>
  </body>
</html>

태그 (Tag):
- <h1>: 큰 제목
- <p>: 문단
- <div>: 구역
- <span>: 작은 텍스트

속성 (Attribute):
<div class="price" id="main">

스크래핑의 목표:
HTML에서 원하는 데이터 추출
```

---

### J

**JSON (JavaScript Object Notation)**
```
쉬운 설명: 데이터를 주고받는 형식
일상 비유: 정리된 메모

형식:
{
  "name": "iPhone 15",
  "price": 1200000,
  "stock": 50,
  "tags": ["스마트폰", "애플"]
}

vs CSV:
JSON:
- 계층 구조 가능
- 배열, 객체 표현
- API에 주로 사용

CSV:
- 표 형태만
- 간단함
- Excel에서 열기 쉬움

Scraperr 사용:
export_format: "json"
```

---

### M

**MongoDB**
```
쉬운 설명: 문서 저장 데이터베이스
일상 비유: 파일 캐비닛

vs 전통적 DB (MySQL):
MySQL: 표 형태 (행, 열)
MongoDB: 문서 형태 (JSON)

장점:
- 유연한 구조
- 스키마 변경 쉬움
- 대용량 데이터

Scraperr에서:
스크래핑 결과 → MongoDB
웹 UI에서 조회
```

---

### N

**Next.js**
```
쉬운 설명: React 기반 웹 프레임워크
Scraperr의 프론트엔드

특징:
- 빠른 페이지 로딩
- SEO 친화적
- 서버 사이드 렌더링

Scraperr에서 역할:
사용자 ↔ Next.js UI ↔ FastAPI
      보기/설정      데이터
```

---

### P

**Pagination (페이지네이션)**
```
쉬운 설명: 여러 페이지로 나뉜 결과
일상 비유: 책의 페이지

형태:
1. 번호: [1] [2] [3] [다음]
2. 무한 스크롤: 계속 로딩
3. "더보기" 버튼

스크래핑 전략:
방법 1: 페이지 번호 증가
products?page=1
products?page=2
...

방법 2: "다음" 버튼 클릭
//a[@class='next']

Scraperr 설정:
pagination:
  type: "numbered"
  max_pages: 10
```

---

**Playwright**
```
쉬운 설명: 브라우저 자동화 도구
일상 비유: 로봇이 브라우저 조작

기능:
- 웹사이트 열기
- 클릭, 입력, 스크롤
- JavaScript 실행
- 스크린샷

vs Selenium:
Playwright: 빠름, 현대적
Selenium: 오래됨, 널리 사용

Scraperr에서:
동적 웹사이트 스크래핑에 필수
```

---

### R

**Rate Limiting (속도 제한)**
```
쉬운 설명: 요청 속도 조절
일상 비유: 천천히 걷기

왜 필요:
1. 서버 부담 줄이기
2. 차단 방지
3. 예의

설정:
rate_limit:
  requests_per_second: 2
  delay_between_pages: 3

의미:
- 초당 2개 요청
- 페이지 사이 3초 대기
```

---

**Regex (정규표현식)**
```
쉬운 설명: 텍스트 패턴 찾기
일상 비유: 검색 마법

예시:
\d+        → 숫자
[0-9]+     → 숫자 (같음)
\w+        → 단어
.+         → 모든 글자

활용:
가격 추출:
"1,200,000원" → 1200000

전화번호:
"010-1234-5678" → 01012345678

Scraperr:
transform: "regex"
pattern: "\d+"
```

---

**robots.txt**
```
쉬운 설명: 웹사이트 크롤링 규칙
일상 비유: "출입금지" 표지판

위치:
https://website.com/robots.txt

내용:
User-agent: *
Disallow: /admin/
Disallow: /private/
Allow: /products/
Crawl-delay: 5

의미:
- admin, private 금지
- products 허용
- 5초 간격 요청

Scraperr:
자동으로 robots.txt 확인
준수하지 않으면 윤리적 문제
```

---

### S

**Scraping (스크래핑)**
```
쉬운 설명: 웹사이트에서 데이터 추출
일상 비유: 신문에서 기사 오리기

vs Crawling:
Scraping: 데이터 수집
Crawling: 사이트 탐색

합법성:
✅ 공개된 정보
✅ robots.txt 준수
✅ 적절한 속도
❌ 개인정보
❌ 로그인 필요 데이터 (허가 없이)
```

---

**Spider (스파이더)**
```
쉬운 설명: 웹사이트를 돌아다니는 봇
일상 비유: 거미줄처럼 연결 따라가기

동작:
1. 시작 URL
2. 페이지 분석
3. 링크 추출
4. 링크 방문
5. 반복

Scraperr의 스파이더:
spider_mode: true
follow_links: true
max_depth: 3
```

---

### U

**User-Agent**
```
쉬운 설명: "나는 이런 브라우저예요" 소개
일상 비유: 명함

예시:
Mozilla/5.0 (Windows NT 10.0) Chrome/120.0

구성:
- OS: Windows 10
- 브라우저: Chrome 120

왜 중요:
서버가 봇인지 판단하는 기준
Python 기본 User-Agent = 차단될 수 있음

Scraperr 설정:
headers:
  User-Agent: "Mozilla/5.0..."
```

---

### W

**Webhook**
```
쉬운 설명: 자동 알림
일상 비유: 초인종

동작:
이벤트 발생 → Webhook 호출
예: 스크래핑 완료 → Slack 메시지

Scraperr 사용:
notifications:
  webhook:
    url: "https://hooks.slack.com/..."
    on: ["complete", "error"]
```

---

### X

**XPath (XML Path Language)**
```
쉬운 설명: 웹 요소 주소
일상 비유: 우편 주소

문법:
//      모든 곳에서 검색
/       직접 자식만
[@]     속성 조건
text()  텍스트만

예시:
//div[@class='price']/text()
→ class='price'인 div의 텍스트

축약:
//div[@class='price']//span
→ price div 안의 모든 span

Scraperr의 핵심:
모든 데이터 추출의 기본
```

---

### Y

**YAML (YAML Ain't Markup Language)**
```
쉬운 설명: 설정 파일 형식
일상 비유: 레시피

특징:
- 읽기 쉬움
- 들여쓰기로 구조 표현
- 주석 가능 (#)

예시:
name: "My Scraper"
targets:
  - url: "https://example.com"
    selector: "//div[@class='item']"

Scraperr에서:
모든 설정을 YAML로 작성
```

---

## 마무리하며

### 🎓 핵심 요약

**Scraperr를 한 문장으로:**
"코딩 없이 클릭만으로 웹사이트 데이터를 자동 수집하고, 깔끔하게 정리해주는 셀프 호스팅 도구"

**꼭 기억할 5가지:**
1. **노코드 스크래핑** → 개발자 아니어도 OK
2. **XPath 자동 생성** → 복사만 하면 됨
3. **대량 데이터 처리** → 수천 페이지 자동화
4. **다양한 내보내기** → CSV, JSON, DB
5. **윤리적 사용** → robots.txt 준수 필수

---

### 🚀 지금 바로 시작하기

**10분 퀵 스타트:**
```bash
# 1. 다운로드
git clone https://github.com/jaypyles/Scraperr.git
cd Scraperr

# 2. 실행 (Docker 필요)
make up

# 3. 브라우저 접속
http://localhost:3000

# 4. 첫 스크래핑 작업 생성!
```

**첫 주말 목표:**
- [ ] Scraperr 설치 및 실행
- [ ] 간단한 웹사이트 스크래핑 성공
- [ ] CSV 파일로 다운로드
- [ ] XPath 사용법 이해
- [ ] 실무 프로젝트 아이디어 구상

---

### 💬 도움 받기

**공식 자료:**
- [GitHub Repository](https://github.com/jaypyles/Scraperr)
- [Documentation](https://scraperr-docs.pages.dev/)
- [Discord Community](https://discord.gg/89q7scsGEK)

**학습 로드맵:**
```
Week 1: 기초
- Scraperr 설치
- 첫 스크래핑 작업
- XPath 기본

Week 2: 실무
- 페이지네이션
- 이미지 다운로드
- 데이터 정제

Week 3: 고급
- 동적 웹사이트
- API 인터셉트
- 자동화 스케줄링

Week 4: 마스터
- 분산 스크래핑
- AI 검증
- 프로덕션 배포
```

---

### 🎯 실전 체크리스트

**데이터 수집 자동화 체크:**
- [ ] 수동 작업 시간 50% 이상 감소
- [ ] 정확도 95% 이상
- [ ] 법적/윤리적 문제 없음
- [ ] 스케줄 자동 실행 설정
- [ ] 오류 처리 구현
- [ ] 데이터 검증 파이프라인
- [ ] 팀과 지식 공유 완료

**모두 체크되면 Scraperr 마스터! 🎉**

---

### 🌟 다음 단계

**관련 도구 학습:**
- **BeautifulSoup**: Python 스크래핑 라이브러리
- **Selenium**: 브라우저 자동화
- **Scrapy**: 프로페셔널 스크래핑 프레임워크
- **Apache Airflow**: 워크플로우 오케스트레이션

**심화 주제:**
- 대규모 분산 스크래핑
- 머신러닝 데이터 파이프라인
- 실시간 데이터 스트리밍
- 스크래핑 법률 및 윤리

---

## 연결된 노트
- [[웹 스크래핑 기초]]
- [[XPath 완벽 가이드]]
- [[데이터 수집 자동화]]
- [[Docker 컨테이너 관리]]
- [[FastAPI 백엔드 개발]]
- [[MongoDB 데이터베이스]]
- [[데이터 분석 파이프라인]]

---

**마지막 조언:**
> "Scraperr는 단순한 도구가 아닙니다. 데이터 수집의 민주화입니다.
>
> 개발자가 아니어도, 복잡한 코드를 몰라도, 누구나 웹의 방대한 데이터를 활용할 수 있게 됩니다.
>
> 하지만 강력한 도구인 만큼, 책임감 있게 사용해야 합니다. robots.txt를 존중하고, 서비스 약관을 준수하며, 개인정보를 침해하지 마세요.
>
> 윤리적으로 사용한다면, Scraperr는 당신의 업무를 혁신하고 인사이트를 제공하는 최고의 파트너가 될 것입니다!"

**Happy Scraping! 🕷️✨**