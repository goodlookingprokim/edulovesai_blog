---
title: HTTP 상태 코드 - 초보자를 위한 완벽 가이드
created: 2025-10-12
last_modified: 2025-10-12
tags:
  - HTTP
  - 상태코드
  - 웹개발
  - 초보자가이드
  - API
  - 백엔드
  - 프론트엔드
  - 네트워크
status: 완료
type: 가이드
priority: high
share_link: https://share.note.sx/6086v3lh#K6CXxSYPIDbCjla6XRmyxbm7ArFxQEK2GaDMbtKdaEk
share_updated: 2025-10-12T12:48:52+09:00
---

# 🌐 HTTP 상태 코드 - 초보자를 위한 완벽 가이드

> **이 노트에서 배울 내용**: 웹 개발의 언어, HTTP 상태 코드를 스토리와 함께 쉽고 재미있게 마스터해봅시다!

---

## 📋 목차 (클릭하면 해당 섹션으로 이동)

### 🎬 시작하기
1. [[#시작하기 전에 - 지은이의 하루]]
2. [[#HTTP 상태 코드란 무엇인가요]]
3. [[#왜 HTTP 상태 코드가 중요한가요]]

### 📚 상태 코드 5가지 카테고리
4. [[#1xx Information - 정보 응답]]
5. [[#2xx Success - 성공 응답]]
6. [[#3xx Redirect - 리다이렉션]]
7. [[#4xx Client Error - 클라이언트 에러]]
8. [[#5xx Server Error - 서버 에러]]

### 💡 실전 활용
9. [[#초급 예제 - 첫걸음]]
10. [[#중급 예제 - 실무 적용]]
11. [[#고급 예제 - 심화 학습]]
12. [[#실전 시나리오 - 에러 해결 스토리]]

### 🔧 문제 해결
13. [[#자주 발생하는 에러 TOP 10]]
14. [[#디버깅 체크리스트]]
15. [[#상태 코드 치트시트]]

### 📖 부록
16. [[#용어 설명]]
17. [[#추가 학습 자료]]
18. [[#연결된 노트]]

---

## 시작하기 전에 - 지은이의 하루

### 📖 웹 개발자 지은이의 아침

지은이는 첫 취업에 성공한 주니어 프론트엔드 개발자예요. 오늘은 출근 첫날! 떨리는 마음으로 회사에 도착했습니다.

**오전 10시 - 첫 번째 버그 리포트**

팀장님: "지은아, 사용자들이 로그인이 안 된다고 하는데 확인 좀 해줄래?"

지은이는 브라우저 개발자 도구를 열었어요. 그리고 이런 메시지를 발견했죠:

```
POST https://api.company.com/login
Status: 401 Unauthorized
```

지은이: "401? 이게 뭐지...? 🤔"

**오전 11시 - 두 번째 문제**

이번엔 마이페이지가 로딩되지 않아요.

```
GET https://api.company.com/user/profile
Status: 404 Not Found
```

지은이: "404... 어디서 많이 본 숫자인데...?"

**점심 시간 - 선배의 조언**

선배 개발자 민수: "지은아, HTTP 상태 코드 공부 안 했어? 이거 웹 개발의 기본 중 기본인데!"

지은이: "상태 코드요...? 그게 뭔데요?"

민수: "브라우저와 서버가 나누는 대화의 언어지! 401은 '인증 안 됐어요', 404는 '못 찾았어요'라는 뜻이야."

**오후 3시 - 깨달음의 순간**

지은이는 점심 후 HTTP 상태 코드를 공부했어요. 그리고 발견했습니다!

```javascript
// 로그인 API - 토큰이 만료되어 401 발생
// 해결: 토큰 갱신 로직 추가!

// 프로필 API - 잘못된 URL로 404 발생
// 해결: /user/profile → /users/profile 수정!
```

**오후 5시 - 문제 해결!**

지은이: "팀장님! 두 문제 다 해결했어요! 401은 토큰 문제였고, 404는 URL 오타였어요!"

팀장: "오! 벌써? 빠르네! HTTP 상태 코드 이해하니까 금방 찾았구나!"

### 💡 교훈

> HTTP 상태 코드는 웹 개발자의 **나침반**이에요. 어디가 문제인지, 어떻게 해결해야 하는지 알려주는 친절한 안내판이죠!

---

## HTTP 상태 코드란 무엇인가요?

### 🤔 5살 아이에게 설명한다면...

> "HTTP 상태 코드는 컴퓨터가 우리에게 보내는 **신호등**이에요!
> - 🟢 초록불 (2xx) = 성공! 통과!
> - 🟡 노란불 (3xx) = 잠깐, 다른 곳으로 가세요
> - 🔴 빨간불 (4xx) = 멈춤! 여러분이 뭔가 잘못했어요
> - 🔴🔴 빨간불 깜빡임 (5xx) = 멈춤! 서버가 고장났어요"

### 📚 좀 더 자세히 설명하면...

**HTTP 상태 코드**는 웹 서버가 클라이언트(브라우저, 앱)의 요청에 대해 "어떤 일이 일어났는지" 알려주는 **3자리 숫자**예요.

```
브라우저: "안녕 서버! 사용자 정보 좀 줄래?" 📤
서버: "200 OK - 여기 있어!" 📥

브라우저: "로그인 좀 해줘!" 📤
서버: "401 Unauthorized - 비밀번호가 틀렸어!" ❌
```

### 🎯 핵심 개념

**구조**:
- **첫 자리**: 응답의 카테고리 (1~5)
- **나머지 두 자리**: 구체적인 상황

```
404 Not Found
│││
││└─ 구체적 의미: "찾을 수 없음"
│└── 카테고리: 4 (클라이언트 에러)
└─── HTTP 상태 코드
```

### 🌈 5가지 색깔로 기억하기

```
1xx (파란색 💙) = Information (정보)
   → "잠깐만요, 처리 중이에요"

2xx (초록색 💚) = Success (성공)
   → "완벽해요! 성공했어요!"

3xx (노란색 💛) = Redirect (리다이렉션)
   → "저기로 가세요! 길 안내해줄게요"

4xx (주황색 🧡) = Client Error (클라이언트 에러)
   → "이런, 당신이 뭔가 잘못했어요"

5xx (빨간색 ❤️) = Server Error (서버 에러)
   → "죄송해요, 저희가 고장났어요"
```

---

## 왜 HTTP 상태 코드가 중요한가요?

### 🎯 개발자에게

1. **버그 찾기가 쉬워져요**
   ```javascript
   // 어떤 코드가 더 읽기 쉬울까요?

   // ❌ 나쁜 예
   if (response.data === null) {
     console.log("뭔가 잘못됨");
   }

   // ✅ 좋은 예
   if (response.status === 404) {
     console.log("리소스를 찾을 수 없습니다");
   } else if (response.status === 401) {
     console.log("인증이 필요합니다");
   } else if (response.status === 500) {
     console.log("서버 에러입니다");
   }
   ```

2. **사용자 경험이 좋아져요**
   ```javascript
   // 상태 코드에 따라 다른 메시지 표시
   switch (response.status) {
     case 401:
       showMessage("로그인이 필요합니다");
       redirectToLogin();
       break;
     case 404:
       showMessage("페이지를 찾을 수 없습니다");
       break;
     case 500:
       showMessage("서버 오류입니다. 잠시 후 다시 시도해주세요");
       break;
   }
   ```

3. **API 설계가 명확해져요**
   ```
   POST /api/users
   → 201 Created (사용자 생성 성공)
   → 409 Conflict (이미 존재하는 이메일)
   → 422 Unprocessable Entity (유효성 검사 실패)
   ```

### 💼 비즈니스에게

- **고객 지원 비용 감소**: 정확한 에러 메시지 → 빠른 문제 해결
- **서비스 안정성 모니터링**: 500 에러 증가 → 서버 문제 조기 발견
- **사용자 만족도 향상**: 친절한 에러 안내 → 좋은 사용자 경험

---

## 1xx Information - 정보 응답

### 🤔 이게 뭐예요?

> "잠깐만요! 아직 처리 중이에요. 조금만 기다려주세요!" 👷‍♂️

**1xx 상태 코드**는 서버가 요청을 받았고, 계속 처리 중이라는 뜻이에요. 마치 식당에서 주문을 받은 직원이 "네, 주문 받았어요! 조리 중입니다!"라고 말하는 것과 같아요.

### 📝 주요 1xx 상태 코드

#### **100 Continue** - 계속하세요

**언제 사용하나요?**
- 큰 파일을 업로드할 때
- 서버가 "계속 보내도 돼요!"라고 확인해줄 때

**실생활 비유**:
```
당신: "100MB 영상 파일 보내도 될까요?" 🎥
서버: "100 Continue - 네, 보내세요!" ✅

당신: "그럼 파일 전송 시작합니다!" 📤
서버: "200 OK - 잘 받았어요!" ✅
```

**예제 코드**:
```javascript
// 큰 파일 업로드 전 확인
fetch('/api/upload/large-video', {
  method: 'POST',
  headers: {
    'Expect': '100-continue',
    'Content-Length': '104857600' // 100MB
  }
})
.then(response => {
  if (response.status === 100) {
    console.log("서버가 파일을 받을 준비가 됐어요!");
    // 실제 파일 전송 시작
  }
});
```

#### **101 Switching Protocols** - 프로토콜 전환

**언제 사용하나요?**
- HTTP에서 WebSocket으로 전환할 때
- 실시간 채팅, 게임 등에서 사용

**실생활 비유**:
```
브라우저: "HTTP 말고 WebSocket으로 얘기할래요?" 📞
서버: "101 Switching Protocols - 좋아요, 바꿔요!" 🔄
```

**예제 코드**:
```javascript
// WebSocket 연결 시작
const socket = new WebSocket('ws://chat.example.com');

socket.onopen = () => {
  console.log("101 Switching Protocols 발생!");
  console.log("이제 실시간 통신 가능!");
};

socket.onmessage = (event) => {
  console.log("실시간 메시지:", event.data);
};
```

#### **102 Processing** - 처리 중

**언제 사용하나요?**
- 서버가 요청을 처리하는 데 시간이 오래 걸릴 때
- 타임아웃 방지용

**실생활 비유**:
```
당신: "복잡한 데이터 분석 부탁해요" 📊
서버: "102 Processing - 처리 중이에요, 기다려주세요..." ⏳
(10초 후)
서버: "102 Processing - 아직 처리 중..." ⏳
(20초 후)
서버: "200 OK - 완료했어요!" ✅
```

#### **103 Early Hints** - 초기 힌트

**언제 사용하나요?**
- 페이지 로딩 최적화
- 중요한 리소스를 미리 로드하도록 힌트 제공

**실생활 비유**:
```
브라우저: "index.html 주세요!" 🏠
서버: "103 Early Hints - 본문 만들고 있는데,
       미리 style.css랑 script.js 로드해둬!" 💡
브라우저: "오! 미리 준비할게요!" ⚡
(2초 후)
서버: "200 OK - 여기 HTML이에요!" 📄
브라우저: "CSS/JS는 이미 준비됐으니 바로 렌더링!" 🎨
```

**예제 코드**:
```javascript
// 서버 측 (Node.js Express)
app.get('/dashboard', (req, res) => {
  // 103 Early Hints 전송
  res.writeEarlyHints({
    'link': [
      '</styles/dashboard.css>; rel=preload; as=style',
      '</scripts/charts.js>; rel=preload; as=script'
    ]
  });

  // 복잡한 데이터 처리...
  const data = await fetchComplexData();

  // 최종 응답
  res.status(200).send(renderDashboard(data));
});
```

### 💡 실무 팁

**대부분의 개발자는 1xx를 거의 안 봐요!**
- 브라우저가 자동으로 처리
- 특별한 최적화가 필요한 경우에만 직접 다룸
- WebSocket (101) 정도만 알면 충분!

---

## 2xx Success - 성공 응답

### 🤔 이게 뭐예요?

> "완벽해요! 요청이 성공했어요! 🎉"

**2xx 상태 코드**는 서버가 요청을 성공적으로 처리했다는 뜻이에요. 개발자가 가장 보고 싶어하는 숫자죠! 🌈

### 🎯 개발자의 행복 지수

```
200 OK 확인 → 행복도 100% 😊
201 Created 확인 → 행복도 120% 🎉
204 No Content 확인 → 행복도 80% 😌
400대 에러 발견 → 행복도 30% 😰
500대 에러 발견 → 행복도 5% 😱
```

### 📝 주요 2xx 상태 코드

#### **200 OK** - 성공

**가장 흔하고 기본적인 성공 응답**이에요!

**언제 사용하나요?**
- GET 요청이 성공했을 때
- POST 요청이 성공했을 때 (새 리소스 생성 아닌 경우)
- PUT/PATCH 업데이트가 성공했을 때

**실생활 비유**:
```
손님: "메뉴판 좀 보여주세요" 📋
웨이터: "네, 여기 있습니다!" (200 OK) ✅
```

**🌱 초급 예제**:
```javascript
// 사용자 목록 가져오기
fetch('https://api.example.com/users')
  .then(response => {
    console.log(response.status); // 200
    console.log(response.statusText); // "OK"
    return response.json();
  })
  .then(users => {
    console.log("사용자 목록:", users);
    // 화면에 표시
  });
```

**🌿 중급 예제**:
```javascript
// 프로필 업데이트
async function updateProfile(userData) {
  try {
    const response = await fetch('/api/profile', {
      method: 'PUT',
      headers: {
        'Content-Type': 'application/json',
        'Authorization': `Bearer ${token}`
      },
      body: JSON.stringify(userData)
    });

    if (response.status === 200) {
      const updated = await response.json();
      showSuccess("프로필이 업데이트되었습니다!");
      return updated;
    }
  } catch (error) {
    console.error("업데이트 실패:", error);
  }
}
```

#### **201 Created** - 생성됨

**새로운 리소스가 생성되었을 때** 사용해요!

**언제 사용하나요?**
- POST로 새 사용자 생성
- 새 게시글 작성
- 새 상품 등록

**실생활 비유**:
```
손님: "회원 가입하고 싶어요!" 📝
시스템: "201 Created - 계정이 생성되었습니다!
         새 계정 번호는 #12345입니다!" ✅
```

**🌱 초급 예제**:
```javascript
// 새 게시글 작성
fetch('/api/posts', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({
    title: "내 첫 게시글",
    content: "안녕하세요!"
  })
})
.then(response => {
  if (response.status === 201) {
    console.log("게시글이 생성되었어요! 🎉");
    return response.json();
  }
})
.then(newPost => {
  console.log("새 게시글 ID:", newPost.id);
});
```

**🌳 고급 예제 (REST API 설계)**:
```javascript
// Express.js 서버
app.post('/api/users', async (req, res) => {
  try {
    const newUser = await User.create(req.body);

    // 201 Created + Location 헤더
    res.status(201)
       .location(`/api/users/${newUser.id}`)
       .json({
         message: "User created successfully",
         user: newUser,
         _links: {
           self: `/api/users/${newUser.id}`,
           posts: `/api/users/${newUser.id}/posts`
         }
       });
  } catch (error) {
    res.status(400).json({ error: error.message });
  }
});
```

#### **202 Accepted** - 수락됨

**요청은 받았지만 아직 처리가 완료되지 않았을 때** 사용해요!

**언제 사용하나요?**
- 비동기 작업 시작
- 배치 처리 작업
- 긴 시간이 걸리는 작업

**실생활 비유**:
```
손님: "대용량 영상 파일 변환해주세요" 🎬
서버: "202 Accepted - 작업 접수했어요!
       작업 ID: #789로 나중에 확인하세요" 📋
(10분 후)
손님: "작업 #789 상태 확인 부탁해요"
서버: "200 OK - 완료됐어요! 다운로드하세요" ✅
```

**🌿 중급 예제**:
```javascript
// 긴 작업 시작 (영상 처리)
async function processVideo(videoFile) {
  const response = await fetch('/api/video/process', {
    method: 'POST',
    body: videoFile
  });

  if (response.status === 202) {
    const { jobId, estimatedTime } = await response.json();
    console.log(`작업 접수! Job ID: ${jobId}`);
    console.log(`예상 시간: ${estimatedTime}초`);

    // 상태 확인 함수 호출
    return checkJobStatus(jobId);
  }
}

// 작업 상태 주기적 확인
async function checkJobStatus(jobId) {
  const interval = setInterval(async () => {
    const response = await fetch(`/api/jobs/${jobId}`);
    const job = await response.json();

    if (job.status === 'completed') {
      clearInterval(interval);
      console.log("작업 완료! 결과:", job.result);
    } else if (job.status === 'failed') {
      clearInterval(interval);
      console.error("작업 실패:", job.error);
    } else {
      console.log(`진행 중... ${job.progress}%`);
    }
  }, 3000); // 3초마다 확인
}
```

#### **204 No Content** - 내용 없음

**성공했지만 돌려줄 데이터가 없을 때** 사용해요!

**언제 사용하나요?**
- DELETE 성공 (삭제된 데이터는 돌려줄 필요 없음)
- PUT 업데이트 성공 (업데이트된 데이터를 다시 안 보내도 될 때)
- 즐겨찾기 추가/제거 같은 간단한 액션

**실생활 비유**:
```
손님: "이 게시글 삭제해주세요" 🗑️
서버: "204 No Content - 삭제했어요! (삭제된 건 보여줄 게 없죠?)" ✅
```

**🌱 초급 예제**:
```javascript
// 게시글 삭제
async function deletePost(postId) {
  const response = await fetch(`/api/posts/${postId}`, {
    method: 'DELETE'
  });

  if (response.status === 204) {
    console.log("삭제 성공! 돌려받을 데이터는 없어요");
    removePostFromUI(postId); // UI에서 제거
  }
}
```

**🌿 중급 예제**:
```javascript
// 좋아요 토글
async function toggleLike(postId) {
  const response = await fetch(`/api/posts/${postId}/like`, {
    method: 'PUT'
  });

  if (response.status === 204) {
    // 서버는 새 상태를 안 보내줌
    // 클라이언트가 UI만 토글
    const likeButton = document.querySelector(`#like-${postId}`);
    likeButton.classList.toggle('liked');
    console.log("좋아요 상태 변경됨!");
  }
}
```

#### **206 Partial Content** - 부분 콘텐츠

**큰 파일의 일부만 요청했을 때** 사용해요!

**언제 사용하나요?**
- 동영상 스트리밍 (일부분씩 로드)
- 대용량 파일 다운로드 (이어받기)
- PDF 뷰어 (페이지별 로드)

**실생활 비유**:
```
브라우저: "영화 파일의 10분~20분 부분만 주세요!" 🎬
서버: "206 Partial Content - 요청한 부분만 보냅니다!" 📦
```

**🌳 고급 예제**:
```javascript
// 영상 스트리밍
async function streamVideo(videoUrl) {
  const video = document.querySelector('video');

  video.addEventListener('timeupdate', async () => {
    const currentTime = video.currentTime;
    const bufferSize = 30; // 30초 분량

    // Range 요청으로 필요한 부분만 가져오기
    const response = await fetch(videoUrl, {
      headers: {
        'Range': `bytes=${currentTime * 1000000}-${(currentTime + bufferSize) * 1000000}`
      }
    });

    if (response.status === 206) {
      console.log("부분 콘텐츠 로드 성공!");
      const blob = await response.blob();
      // blob을 video element에 추가
    }
  });
}
```

### 💡 실무 팁: 200 vs 201 vs 204

```javascript
// GET 요청 → 200 OK
GET /api/users
→ 200 OK (사용자 목록 반환)

// POST 생성 → 201 Created
POST /api/users
→ 201 Created (새 사용자 + Location 헤더)

// PUT 업데이트 → 200 OK (데이터 반환) 또는 204 No Content
PUT /api/users/123
→ 200 OK (업데이트된 사용자 반환)
또는
→ 204 No Content (업데이트만 하고 데이터 안 보냄)

// DELETE 삭제 → 204 No Content
DELETE /api/users/123
→ 204 No Content (삭제 완료, 반환할 데이터 없음)
```

---

## 3xx Redirect - 리다이렉션

### 🤔 이게 뭐예요?

> "여기 말고 저기로 가세요! 길 안내해드릴게요!" 🗺️

**3xx 상태 코드**는 요청한 리소스가 다른 곳으로 이동했거나, 다른 곳으로 가야 한다는 뜻이에요. 마치 이사한 친구 집 주소가 바뀌어서 새 주소를 알려주는 것과 같아요!

### 🏠 이사 비유로 이해하기

```
예전 집 (구주소): https://old-site.com
새 집 (신주소): https://new-site.com

배달원 (브라우저): "예전 집으로 소포 가져왔어요!" 📦
이사 안내문 (3xx): "이사했어요! 새 주소는 new-site.com이에요!" 🗺️
배달원: "아, 알겠습니다! 새 주소로 갈게요!" 🚚
```

### 📝 주요 3xx 상태 코드

#### **300 Multiple Choices** - 여러 선택지

**여러 옵션이 있을 때** 사용해요. (실제로는 거의 안 써요!)

**실생활 비유**:
```
당신: "사진 다운로드 할게요" 📸
서버: "300 Multiple Choices - 어떤 버전 원하세요?
       1. 고해상도 (10MB)
       2. 중간 해상도 (2MB)
       3. 저해상도 (500KB)"
```

#### **301 Moved Permanently** - 영구 이동

**리소스가 영원히 다른 곳으로 이동했을 때** 사용해요!

**언제 사용하나요?**
- 도메인 변경 (old-site.com → new-site.com)
- URL 구조 변경
- HTTPS로 영구 전환

**실생활 비유**:
```
브라우저: "http://example.com 주세요" 🏠
서버: "301 Moved Permanently -
       이제 https://example.com 으로 영구 이사했어요!" 🚚
브라우저: "알겠어요! 다음부터는 새 주소로 갈게요!"
          (북마크도 자동 업데이트!)
```

**🌱 초급 예제**:
```javascript
// 서버 측 (Express.js)
app.get('/old-blog/*', (req, res) => {
  // 영구적으로 새 URL로 이동
  const newUrl = req.url.replace('/old-blog', '/blog');
  res.redirect(301, newUrl);
});

// 사용자가 /old-blog/post-1 접속
// → 자동으로 /blog/post-1 로 이동
// → 브라우저는 이 사실을 기억함!
```

**🌿 중급 예제 (HTTP → HTTPS 강제)**:
```javascript
// Express.js 미들웨어
app.use((req, res, next) => {
  // HTTP로 접속한 경우
  if (!req.secure && req.get('x-forwarded-proto') !== 'https') {
    // 301로 HTTPS로 영구 리다이렉트
    return res.redirect(301, `https://${req.headers.host}${req.url}`);
  }
  next();
});
```

**💡 SEO 영향**:
- 301은 검색 엔진에 "주소가 바뀌었어요"라고 알려줌
- 기존 검색 순위가 새 URL로 이전됨
- Google이 새 URL을 인덱싱함

#### **302 Found** (또는 Moved Temporarily) - 임시 이동

**일시적으로 다른 곳으로 갈 때** 사용해요!

**언제 사용하나요?**
- 임시 유지보수 페이지
- A/B 테스트
- 로그인 후 리다이렉트

**실생활 비유**:
```
브라우저: "메인 페이지 주세요" 🏠
서버: "302 Found - 지금은 임시로 유지보수 페이지로 가세요.
       내일이면 다시 정상 페이지 볼 수 있어요!" 🔧
브라우저: "알겠어요! 근데 북마크는 그대로 둘게요!"
```

**🌱 초급 예제**:
```javascript
// 로그인 후 리다이렉트
app.post('/login', async (req, res) => {
  const user = await authenticateUser(req.body);

  if (user) {
    req.session.user = user;
    // 임시로 대시보드로 이동
    res.redirect(302, '/dashboard');
  } else {
    res.status(401).send('로그인 실패');
  }
});
```

**🌿 중급 예제 (A/B 테스트)**:
```javascript
// A/B 테스트: 50% 사용자에게 새 랜딩 페이지
app.get('/', (req, res) => {
  const showNewVersion = Math.random() < 0.5;

  if (showNewVersion) {
    // 임시로 새 버전 보여주기
    res.redirect(302, '/landing-v2');
  } else {
    res.sendFile('index.html');
  }
});
```

#### **303 See Other** - 다른 것을 보세요

**POST 요청 후 GET 페이지로 이동할 때** 사용해요!

**언제 사용하나요?**
- 폼 제출 후 결과 페이지로 이동
- POST-Redirect-GET 패턴

**실생활 비유**:
```
당신: "주문 완료했어요!" (POST) 🛒
서버: "303 See Other - 주문 확인 페이지를 보세요!" (GET) 📄
브라우저: "알겠어요! GET 요청으로 확인 페이지 가져올게요!"
```

**🌿 중급 예제**:
```javascript
// POST-Redirect-GET 패턴
app.post('/checkout', async (req, res) => {
  // 주문 처리
  const order = await processOrder(req.body);

  // 303으로 GET 페이지로 리다이렉트
  // (새로고침해도 중복 주문 안 됨!)
  res.redirect(303, `/orders/${order.id}`);
});

// 주문 확인 페이지 (GET)
app.get('/orders/:id', async (req, res) => {
  const order = await Order.findById(req.params.id);
  res.render('order-confirmation', { order });
});
```

**💡 왜 303을 쓰나요?**
```
POST → 결과 페이지 (302 사용)
→ 새로고침 하면? POST 다시 전송! (중복 주문 위험!) 😱

POST → 결과 페이지 (303 사용)
→ 새로고침 하면? GET만 재전송! (안전!) ✅
```

#### **304 Not Modified** - 수정되지 않음

**캐시된 버전을 사용해도 될 때** 사용해요!

**언제 사용하나요?**
- 브라우저 캐싱
- 이미지/CSS/JS 파일 재사용
- API 응답 캐싱

**실생활 비유**:
```
브라우저: "logo.png 주세요!
           (근데 저 가지고 있는 거 날짜: 2025-01-01)" 📅
서버: "304 Not Modified -
       그거 그대로 써도 돼요. 안 바뀌었어요!" ✅
브라우저: "오예! 다운로드 안 해도 되네! 빠르다!" ⚡
```

**🌳 고급 예제**:
```javascript
// Express.js에서 ETag 사용
app.get('/api/products', async (req, res) => {
  const products = await getProducts();
  const eTag = generateETag(products); // 데이터 해시값

  // 클라이언트가 보낸 ETag와 비교
  if (req.headers['if-none-match'] === eTag) {
    // 데이터 안 바뀜! 304 반환
    res.status(304).end();
  } else {
    // 데이터 바뀜! 새 데이터 전송
    res.set('ETag', eTag);
    res.json(products);
  }
});
```

**실전 시나리오**:
```javascript
// 첫 방문
GET /api/products
→ 200 OK + ETag: "abc123" + 상품 데이터 (1MB 다운로드)

// 5초 후 재방문 (데이터 안 바뀜)
GET /api/products
Headers: If-None-Match: "abc123"
→ 304 Not Modified (데이터 안 보냄, 0KB!)

// 10분 후 재방문 (데이터 바뀜)
GET /api/products
Headers: If-None-Match: "abc123"
→ 200 OK + ETag: "def456" + 새 상품 데이터 (1MB 다운로드)
```

**💡 성능 개선 효과**:
```
304 사용 전:
매 요청마다 1MB 다운로드 × 100회 = 100MB 전송 😰

304 사용 후:
첫 1회만 1MB, 나머지 99회는 0KB = 1MB만 전송! 🚀
99% 대역폭 절약!
```

#### **307 Temporary Redirect** - 임시 리다이렉트

**302와 비슷하지만 더 엄격해요!** HTTP 메서드를 바꾸지 않음.

**실생활 비유**:
```
당신: "POST /api/payment 결제해주세요!" 💳
서버: "307 - 임시로 /api/payment-v2 로 가세요.
       단, POST 그대로 유지하고!" 🔄
브라우저: "알겠어요! POST로 새 URL 호출!" ✅
```

#### **308 Permanent Redirect** - 영구 리다이렉트

**301과 비슷하지만 더 엄격해요!** HTTP 메서드를 바꾸지 않음.

**🌳 고급 예제 (API 버전 관리)**:
```javascript
// v1 API는 영구적으로 v2로 이동
app.all('/api/v1/*', (req, res) => {
  const newUrl = req.url.replace('/api/v1', '/api/v2');
  // 308: POST는 POST로, GET은 GET으로 유지
  res.redirect(308, newUrl);
});
```

### 📊 3xx 빠른 비교표

| 코드 | 이름 | 영구/임시 | 메서드 변경 | 사용 사례 |
|-----|------|----------|-----------|---------|
| 301 | Moved Permanently | 영구 | 가능 (POST→GET) | 도메인 변경, HTTPS 전환 |
| 302 | Found | 임시 | 가능 (POST→GET) | 로그인 리다이렉트 |
| 303 | See Other | 임시 | 강제 GET | POST 후 결과 페이지 |
| 304 | Not Modified | - | - | 캐싱 |
| 307 | Temporary Redirect | 임시 | 불가 | API 임시 이동 |
| 308 | Permanent Redirect | 영구 | 불가 | API 영구 이동 |

---

## 4xx Client Error - 클라이언트 에러

### 🤔 이게 뭐예요?

> "이런, 당신이 뭔가 잘못하셨어요! 다시 확인해주세요!" 🤦‍♂️

**4xx 상태 코드**는 클라이언트(브라우저, 앱)가 잘못된 요청을 보냈다는 뜻이에요. 개발자가 가장 자주 마주치는 에러들이죠!

### 🎯 에러의 주인공은 "당신"

```
5xx = 서버 잘못 → "저희가 죄송합니다" 🙇‍♂️
4xx = 클라이언트 잘못 → "당신이 잘못하셨어요" 🤷‍♂️
```

### 📝 주요 4xx 상태 코드

#### **400 Bad Request** - 잘못된 요청

**요청 형식이 잘못되었을 때** 사용해요!

**언제 발생하나요?**
- JSON 형식이 깨짐
- 필수 파라미터 누락
- 데이터 타입이 틀림

**실생활 비유**:
```
당신: "사용자 등록할게요!
       { name: '김철수', email: '잘못된형식' }" 📝
서버: "400 Bad Request - 이메일 형식이 이상해요!" ❌
```

**🌱 초급 예제 (문제 상황)**:
```javascript
// 잘못된 JSON
fetch('/api/users', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: '{ name: "철수" email: "chul@example.com" }' // ❌ 콤마 누락!
})
.then(response => {
  console.log(response.status); // 400
  console.log("JSON 형식이 잘못되었어요!");
});
```

**🌿 중급 예제 (해결 방법)**:
```javascript
// 올바른 유효성 검사
async function createUser(userData) {
  // 클라이언트 측 유효성 검사
  if (!userData.email || !userData.email.includes('@')) {
    alert("올바른 이메일을 입력하세요!");
    return;
  }

  if (!userData.name || userData.name.length < 2) {
    alert("이름은 2글자 이상이어야 합니다!");
    return;
  }

  try {
    const response = await fetch('/api/users', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify(userData) // ✅ 올바른 JSON
    });

    if (response.status === 400) {
      const error = await response.json();
      alert(`요청 실패: ${error.message}`);
    }
  } catch (error) {
    console.error("네트워크 에러:", error);
  }
}
```

**🌳 고급 예제 (서버 측 검증)**:
```javascript
// Express.js + Joi 유효성 검사
const Joi = require('joi');

const userSchema = Joi.object({
  name: Joi.string().min(2).max(50).required(),
  email: Joi.string().email().required(),
  age: Joi.number().integer().min(0).max(150)
});

app.post('/api/users', async (req, res) => {
  // 유효성 검사
  const { error, value } = userSchema.validate(req.body);

  if (error) {
    return res.status(400).json({
      error: "Bad Request",
      message: error.details[0].message,
      field: error.details[0].path[0]
    });
  }

  // 유효성 검사 통과, 사용자 생성
  const user = await User.create(value);
  res.status(201).json(user);
});
```

#### **401 Unauthorized** - 인증되지 않음

**로그인이 필요할 때** 사용해요!

**언제 발생하나요?**
- 로그인하지 않음
- 토큰이 없음
- 토큰이 만료됨

**실생활 비유**:
```
당신: "내 프로필 정보 주세요!" 👤
경비원: "401 Unauthorized - 신분증 먼저 보여주세요!" 🚫
```

**⚠️ 주의**: 이름은 "Unauthorized"지만 실제로는 "Unauthenticated"(인증 안 됨)를 의미해요!

**🌱 초급 예제**:
```javascript
// 로그인 없이 보호된 페이지 접근
fetch('/api/profile')
  .then(response => {
    if (response.status === 401) {
      console.log("로그인이 필요해요!");
      window.location.href = '/login'; // 로그인 페이지로 이동
    }
  });
```

**🌿 중급 예제 (토큰 갱신)**:
```javascript
// JWT 토큰 자동 갱신
async function fetchWithAuth(url, options = {}) {
  let token = localStorage.getItem('accessToken');

  // 토큰을 헤더에 추가
  options.headers = {
    ...options.headers,
    'Authorization': `Bearer ${token}`
  };

  let response = await fetch(url, options);

  // 401 에러 발생
  if (response.status === 401) {
    console.log("토큰 만료! 갱신 시도...");

    // 리프레시 토큰으로 새 액세스 토큰 받기
    const refreshToken = localStorage.getItem('refreshToken');
    const refreshResponse = await fetch('/api/auth/refresh', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ refreshToken })
    });

    if (refreshResponse.ok) {
      const { accessToken } = await refreshResponse.json();
      localStorage.setItem('accessToken', accessToken);

      // 새 토큰으로 원래 요청 재시도
      options.headers['Authorization'] = `Bearer ${accessToken}`;
      response = await fetch(url, options);
    } else {
      // 리프레시도 실패 → 로그아웃
      console.log("리프레시 실패! 재로그인 필요");
      window.location.href = '/login';
    }
  }

  return response;
}
```

#### **403 Forbidden** - 금지됨

**권한이 없을 때** 사용해요!

**언제 발생하나요?**
- 로그인은 했지만 권한 부족
- 관리자만 접근 가능한 페이지
- 다른 사용자의 데이터 접근 시도

**실생활 비유**:
```
당신 (일반 사용자): "관리자 페이지 주세요!" 👤
경비원: "403 Forbidden - 신분증은 확인했는데,
         당신은 일반 회원이라 못 들어가요!" 🚫
```

**401 vs 403 차이점**:
```
401 = "누구세요? 신분증 보여주세요!" (인증 안 됨)
403 = "신분증 확인했는데, 여긴 못 들어가요!" (권한 없음)
```

**🌱 초급 예제**:
```javascript
// 일반 사용자가 관리자 페이지 접근
fetch('/api/admin/users')
  .then(response => {
    if (response.status === 403) {
      alert("관리자만 접근 가능합니다!");
      window.location.href = '/'; // 메인 페이지로
    }
  });
```

**🌿 중급 예제 (역할 기반 접근 제어)**:
```javascript
// 서버 측 (Express.js)
const requireAdmin = (req, res, next) => {
  // 토큰에서 사용자 정보 확인 (401 체크는 이미 됨)
  const user = req.user; // JWT 미들웨어에서 설정

  if (user.role !== 'admin') {
    return res.status(403).json({
      error: "Forbidden",
      message: "관리자 권한이 필요합니다"
    });
  }

  next();
};

// 관리자 전용 라우트
app.get('/api/admin/users', requireAdmin, async (req, res) => {
  const users = await User.findAll();
  res.json(users);
});
```

**🌳 고급 예제 (리소스 소유권 체크)**:
```javascript
// 본인의 게시글만 수정 가능
app.put('/api/posts/:id', authenticate, async (req, res) => {
  const post = await Post.findById(req.params.id);

  if (!post) {
    return res.status(404).json({ error: "게시글을 찾을 수 없습니다" });
  }

  // 작성자 확인
  if (post.authorId !== req.user.id && req.user.role !== 'admin') {
    return res.status(403).json({
      error: "Forbidden",
      message: "자신의 게시글만 수정할 수 있습니다"
    });
  }

  // 수정 진행
  await post.update(req.body);
  res.json(post);
});
```

#### **404 Not Found** - 찾을 수 없음

**가장 유명한 에러!** 요청한 리소스가 없을 때 사용해요.

**언제 발생하나요?**
- 잘못된 URL
- 삭제된 페이지
- 존재하지 않는 API 엔드포인트

**실생활 비유**:
```
당신: "ID가 999인 사용자 정보 주세요!" 👤
서버: "404 Not Found - 그런 사용자 없어요!" ❌
```

**🌱 초급 예제**:
```javascript
// 존재하지 않는 사용자 조회
fetch('/api/users/99999')
  .then(response => {
    if (response.status === 404) {
      console.log("사용자를 찾을 수 없어요!");
      showErrorPage("사용자가 존재하지 않습니다");
    }
  });
```

**🌿 중급 예제 (친절한 404 페이지)**:
```javascript
// React 컴포넌트
function UserProfile({ userId }) {
  const [user, setUser] = useState(null);
  const [error, setError] = useState(null);

  useEffect(() => {
    fetch(`/api/users/${userId}`)
      .then(response => {
        if (response.status === 404) {
          setError('not-found');
        } else if (response.ok) {
          return response.json();
        }
      })
      .then(data => setUser(data));
  }, [userId]);

  if (error === 'not-found') {
    return (
      <div className="error-page">
        <h1>404 - 사용자를 찾을 수 없습니다</h1>
        <p>요청하신 사용자(ID: {userId})가 존재하지 않습니다.</p>
        <button onClick={() => window.history.back()}>
          돌아가기
        </button>
      </div>
    );
  }

  return user ? <UserDetails user={user} /> : <Loading />;
}
```

**🌳 고급 예제 (Soft Delete)**:
```javascript
// 실제로는 삭제 안 하고 숨김
app.get('/api/posts/:id', async (req, res) => {
  const post = await Post.findById(req.params.id);

  // 게시글이 없거나 삭제됨
  if (!post || post.deletedAt) {
    return res.status(404).json({
      error: "Not Found",
      message: "게시글을 찾을 수 없습니다"
    });
  }

  res.json(post);
});
```

#### **409 Conflict** - 충돌

**리소스 상태 충돌이 있을 때** 사용해요!

**언제 발생하나요?**
- 중복 이메일로 회원가입
- 동시 수정 충돌
- 이미 처리된 요청

**실생활 비유**:
```
당신: "user@example.com으로 가입할게요!" 📧
서버: "409 Conflict - 그 이메일은 이미 사용 중이에요!" ❌
```

**🌿 중급 예제**:
```javascript
// 회원가입 시 이메일 중복 체크
app.post('/api/users', async (req, res) => {
  const { email } = req.body;

  // 이미 존재하는 이메일인지 확인
  const existingUser = await User.findOne({ email });

  if (existingUser) {
    return res.status(409).json({
      error: "Conflict",
      message: "이미 사용 중인 이메일입니다",
      field: "email"
    });
  }

  const user = await User.create(req.body);
  res.status(201).json(user);
});
```

**🌳 고급 예제 (낙관적 잠금)**:
```javascript
// 동시 수정 방지
app.put('/api/posts/:id', async (req, res) => {
  const { version } = req.body; // 클라이언트가 보낸 버전
  const post = await Post.findById(req.params.id);

  // 버전이 다르면 누군가 먼저 수정함
  if (post.version !== version) {
    return res.status(409).json({
      error: "Conflict",
      message: "다른 사용자가 이미 수정했습니다. 새로고침 후 다시 시도하세요",
      currentVersion: post.version,
      yourVersion: version
    });
  }

  // 버전 증가 후 수정
  post.version += 1;
  await post.update(req.body);
  res.json(post);
});
```

#### **422 Unprocessable Entity** - 처리할 수 없는 엔티티

**문법은 맞지만 의미상 오류가 있을 때** 사용해요!

**400 vs 422 차이**:
```
400 = JSON 문법 자체가 틀림
422 = JSON은 맞지만 데이터가 이상함

예시:
{ age: "삼십" } → 400 (숫자여야 함)
{ age: -5 } → 422 (음수는 불가능)
{ age: 200 } → 422 (나이가 너무 많음)
```

**🌳 고급 예제**:
```javascript
// 비즈니스 로직 검증
app.post('/api/orders', async (req, res) => {
  const { productId, quantity } = req.body;

  const product = await Product.findById(productId);

  // 재고 확인
  if (product.stock < quantity) {
    return res.status(422).json({
      error: "Unprocessable Entity",
      message: `재고가 부족합니다. 현재 재고: ${product.stock}개`,
      field: "quantity",
      maxQuantity: product.stock
    });
  }

  // 주문 생성
  const order = await Order.create(req.body);
  res.status(201).json(order);
});
```

### 📊 4xx 빠른 참조 가이드

| 코드 | 의미 | 누구 잘못? | 해결 방법 |
|-----|-----|---------|---------|
| 400 | 잘못된 요청 | 클라이언트 | 요청 형식 확인 |
| 401 | 인증 안 됨 | 클라이언트 | 로그인 필요 |
| 403 | 권한 없음 | 클라이언트 | 권한 필요 |
| 404 | 못 찾음 | 클라이언트 | URL 확인 |
| 409 | 충돌 | 클라이언트 | 중복 데이터 |
| 422 | 처리 불가 | 클라이언트 | 데이터 검증 |

---

## 5xx Server Error - 서버 에러

### 🤔 이게 뭐예요?

> "죄송합니다! 저희 서버가 고장났어요... 🔥😱"

**5xx 상태 코드**는 서버가 요청을 처리하다가 에러가 발생했다는 뜻이에요. 개발자가 가장 보기 싫어하는 숫자들이죠!

### 😰 개발자의 악몽

```
2xx 발견: 😊 "좋아!"
4xx 발견: 😐 "고치면 되지"
5xx 발견: 😱 "안돼!!!"
```

### 📝 주요 5xx 상태 코드

#### **500 Internal Server Error** - 내부 서버 에러

**서버에서 예상치 못한 에러가 발생했을 때** 사용해요. 가장 일반적인 서버 에러!

**언제 발생하나요?**
- 코드에 버그가 있음
- 예외 처리가 안 됨
- 데이터베이스 연결 실패

**실생활 비유**:
```
당신: "사용자 목록 주세요!" 📋
서버: "500 Internal Server Error -
       죄송해요, 뭔가 잘못됐어요... 💥
       (개발자를 불러주세요...)"
```

**🌱 초급 예제 (문제 상황)**:
```javascript
// 서버 코드 (버그 있음)
app.get('/api/users', async (req, res) => {
  const users = await User.findAll();
  res.json(users.data.list); // ❌ users.data가 undefined!
  // → TypeError 발생 → 500 에러!
});
```

**🌿 중급 예제 (에러 처리)**:
```javascript
// 제대로 된 에러 처리
app.get('/api/users', async (req, res) => {
  try {
    const users = await User.findAll();

    if (!users) {
      return res.status(404).json({ error: "사용자 없음" });
    }

    res.json(users); // ✅ 안전
  } catch (error) {
    console.error("사용자 조회 에러:", error);
    res.status(500).json({
      error: "Internal Server Error",
      message: "사용자 목록을 불러오는 데 실패했습니다"
      // 프로덕션에서는 실제 에러 메시지 노출 금지!
    });
  }
});
```

**🌳 고급 예제 (전역 에러 핸들러)**:
```javascript
// Express.js 전역 에러 미들웨어
app.use((error, req, res, next) => {
  // 에러 로깅
  console.error('=== Server Error ===');
  console.error('Time:', new Date().toISOString());
  console.error('URL:', req.url);
  console.error('Method:', req.method);
  console.error('Error:', error.stack);

  // 에러 모니터링 서비스에 전송 (Sentry, LogRocket 등)
  sendToErrorTracker(error, {
    url: req.url,
    user: req.user?.id
  });

  // 클라이언트에게 안전한 메시지만 전송
  res.status(500).json({
    error: "Internal Server Error",
    message: process.env.NODE_ENV === 'development'
      ? error.message  // 개발 환경: 실제 에러
      : "서버 오류가 발생했습니다. 잠시 후 다시 시도해주세요" // 프로덕션: 일반 메시지
  });
});
```

#### **501 Not Implemented** - 구현되지 않음

**서버가 해당 기능을 지원하지 않을 때** 사용해요!

**언제 사용하나요?**
- 아직 개발 안 된 기능
- 지원하지 않는 HTTP 메서드

**실생활 비유**:
```
당신: "DELETE /api/users/123 삭제해주세요!" 🗑️
서버: "501 Not Implemented -
       죄송해요, 삭제 기능은 아직 안 만들었어요!" 🚧
```

**🌿 중급 예제**:
```javascript
// 아직 구현 안 된 기능
app.delete('/api/users/:id', (req, res) => {
  res.status(501).json({
    error: "Not Implemented",
    message: "사용자 삭제 기능은 아직 구현되지 않았습니다",
    plannedRelease: "v2.0"
  });
});
```

#### **502 Bad Gateway** - 잘못된 게이트웨이

**프록시나 게이트웨이가 잘못된 응답을 받았을 때** 사용해요!

**언제 발생하나요?**
- 백엔드 서버가 다운됨
- 로드 밸런서 문제
- API 게이트웨이와 서버 간 통신 실패

**실생활 비유**:
```
브라우저 → Nginx (프록시) → Node.js 서버

브라우저: "API 요청!" 📤
Nginx: "Node.js야, 이 요청 처리해줘!" 📞
Node.js: (응답 없음... 다운됨) 💀
Nginx: "502 Bad Gateway -
        백엔드 서버가 응답 안 해요!" ❌
```

**🌳 고급 예제 (헬스 체크)**:
```javascript
// 백엔드 서버 상태 확인
app.get('/health', (req, res) => {
  // 데이터베이스 연결 확인
  const dbStatus = checkDatabaseConnection();
  // Redis 연결 확인
  const cacheStatus = checkCacheConnection();

  if (dbStatus && cacheStatus) {
    res.status(200).json({ status: 'healthy' });
  } else {
    res.status(503).json({ status: 'unhealthy' });
  }
});

// Nginx 설정에서 헬스 체크 사용
// 502 에러 예방!
```

#### **503 Service Unavailable** - 서비스 이용 불가

**서버가 일시적으로 사용 불가능할 때** 사용해요!

**언제 발생하나요?**
- 유지보수 중
- 서버 과부하
- 일시적 다운타임

**실생활 비유**:
```
당신: "쇼핑몰 접속!" 🛒
서버: "503 Service Unavailable -
       지금 유지보수 중이에요.
       1시간 후에 다시 오세요!" 🔧
```

**🌿 중급 예제 (유지보수 모드)**:
```javascript
// 유지보수 모드 미들웨어
const maintenanceMode = process.env.MAINTENANCE === 'true';

app.use((req, res, next) => {
  if (maintenanceMode) {
    res.status(503)
       .set('Retry-After', '3600') // 1시간 후 재시도
       .json({
         error: "Service Unavailable",
         message: "현재 시스템 유지보수 중입니다",
         retryAfter: "1시간 후"
       });
  } else {
    next();
  }
});
```

**🌳 고급 예제 (Rate Limiting)**:
```javascript
// 요청 제한 (과부하 방지)
const rateLimit = require('express-rate-limit');

const limiter = rateLimit({
  windowMs: 15 * 60 * 1000, // 15분
  max: 100, // 최대 100개 요청
  handler: (req, res) => {
    res.status(503).json({
      error: "Service Unavailable",
      message: "너무 많은 요청입니다. 15분 후 다시 시도해주세요",
      retryAfter: "15분"
    });
  }
});

app.use('/api/', limiter);
```

#### **504 Gateway Timeout** - 게이트웨이 시간 초과

**프록시가 백엔드 서버로부터 응답을 시간 내에 받지 못했을 때** 사용해요!

**언제 발생하나요?**
- 백엔드 서버가 너무 느림
- 복잡한 쿼리 실행 중
- 네트워크 문제

**실생활 비유**:
```
브라우저 → Nginx → 느린 서버

브라우저: "데이터 주세요!" 📤
Nginx: "서버야, 응답해줘!" 📞
(30초 경과...)
Nginx: "504 Gateway Timeout -
        서버가 너무 오래 걸려요!" ⏱️
```

**🌳 고급 예제 (타임아웃 처리)**:
```javascript
// 타임아웃이 긴 작업 처리
app.get('/api/complex-report', async (req, res) => {
  // 타임아웃 설정 (30초)
  req.setTimeout(30000);

  try {
    // Promise.race로 타임아웃 구현
    const result = await Promise.race([
      generateComplexReport(), // 복잡한 작업
      new Promise((_, reject) =>
        setTimeout(() => reject(new Error('Timeout')), 29000)
      )
    ]);

    res.json(result);
  } catch (error) {
    if (error.message === 'Timeout') {
      res.status(504).json({
        error: "Gateway Timeout",
        message: "리포트 생성에 시간이 너무 오래 걸립니다. 나중에 다시 시도하거나 필터를 추가해주세요"
      });
    } else {
      res.status(500).json({ error: error.message });
    }
  }
});
```

### 💡 5xx 에러 예방 체크리스트

```markdown
✅ 모든 비동기 함수에 try-catch
✅ 데이터베이스 연결 에러 처리
✅ 타임아웃 설정
✅ 에러 로깅 (Sentry, LogRocket)
✅ 헬스 체크 엔드포인트
✅ Rate Limiting
✅ 로드 밸런싱
✅ 자동 재시작 (PM2, Docker)
✅ 모니터링 알림
```

---

## 초급 예제 - 첫걸음

### 🌱 예제 1: 브라우저 개발자 도구에서 상태 코드 확인

**단계별 실습**:

```javascript
// 1. 브라우저에서 F12 또는 Cmd+Option+I (Mac)
// 2. Network 탭 클릭
// 3. 웹페이지 새로고침 (Cmd+R 또는 F5)

// 이제 모든 HTTP 요청을 볼 수 있어요!
```

**무엇을 볼 수 있나요?**
```
Name          Status  Type        Size
index.html    200     document    15KB
style.css     200     stylesheet  8KB
logo.png      304     image       (cached)
api/users     200     xhr         2KB
```

**🤔 생각해보기**:
- Q: logo.png가 304인 이유는?
- A: 브라우저 캐시에 이미 있어서 다시 다운로드 안 함!

### 🌱 예제 2: fetch로 상태 코드 확인하기

```javascript
// 가장 기본적인 사용법
fetch('https://api.example.com/users')
  .then(response => {
    // 상태 코드 확인
    console.log('Status:', response.status);        // 200
    console.log('Status Text:', response.statusText); // "OK"
    console.log('OK?:', response.ok);               // true (200-299)

    // 상태 코드에 따라 처리
    if (response.ok) {
      return response.json();
    } else {
      throw new Error(`HTTP Error: ${response.status}`);
    }
  })
  .then(data => {
    console.log('데이터:', data);
  })
  .catch(error => {
    console.error('에러:', error);
  });
```

**response.ok가 뭐예요?**
```javascript
// response.ok는 status가 200-299일 때 true
response.status === 200  → response.ok = true  ✅
response.status === 201  → response.ok = true  ✅
response.status === 404  → response.ok = false ❌
response.status === 500  → response.ok = false ❌
```

### 🌱 예제 3: 간단한 에러 처리

```javascript
async function getUser(userId) {
  try {
    const response = await fetch(`/api/users/${userId}`);

    // 상태 코드별 처리
    if (response.status === 404) {
      alert('사용자를 찾을 수 없습니다');
      return null;
    }

    if (response.status === 500) {
      alert('서버 오류입니다. 잠시 후 다시 시도해주세요');
      return null;
    }

    if (!response.ok) {
      alert(`오류 발생: ${response.status}`);
      return null;
    }

    // 성공!
    const user = await response.json();
    return user;

  } catch (error) {
    alert('네트워크 오류입니다');
    return null;
  }
}

// 사용
const user = await getUser(123);
if (user) {
  console.log('사용자 이름:', user.name);
}
```

---

## 중급 예제 - 실무 적용

### 🌿 예제 4: 재사용 가능한 API 클라이언트

```javascript
// api-client.js
class APIClient {
  constructor(baseURL) {
    this.baseURL = baseURL;
  }

  async request(endpoint, options = {}) {
    const url = `${this.baseURL}${endpoint}`;

    try {
      const response = await fetch(url, {
        ...options,
        headers: {
          'Content-Type': 'application/json',
          ...options.headers
        }
      });

      // 상태 코드별 처리
      return this.handleResponse(response);

    } catch (error) {
      throw new Error('네트워크 오류');
    }
  }

  async handleResponse(response) {
    const data = await response.text();
    const json = data ? JSON.parse(data) : null;

    // 2xx: 성공
    if (response.ok) {
      return { success: true, data: json };
    }

    // 4xx: 클라이언트 에러
    if (response.status >= 400 && response.status < 500) {
      return {
        success: false,
        error: json?.message || '요청 오류',
        status: response.status
      };
    }

    // 5xx: 서버 에러
    if (response.status >= 500) {
      return {
        success: false,
        error: '서버 오류입니다. 잠시 후 다시 시도해주세요',
        status: response.status
      };
    }
  }

  // GET
  get(endpoint) {
    return this.request(endpoint, { method: 'GET' });
  }

  // POST
  post(endpoint, data) {
    return this.request(endpoint, {
      method: 'POST',
      body: JSON.stringify(data)
    });
  }

  // PUT
  put(endpoint, data) {
    return this.request(endpoint, {
      method: 'PUT',
      body: JSON.stringify(data)
    });
  }

  // DELETE
  delete(endpoint) {
    return this.request(endpoint, { method: 'DELETE' });
  }
}

// 사용 예시
const api = new APIClient('https://api.example.com');

// 사용자 목록
const result = await api.get('/users');
if (result.success) {
  console.log('사용자들:', result.data);
} else {
  console.error('에러:', result.error);
}

// 새 사용자 생성
const newUser = await api.post('/users', {
  name: '김철수',
  email: 'chul@example.com'
});
```

### 🌿 예제 5: React에서 상태 코드 처리

```javascript
import { useState, useEffect } from 'react';

function UserList() {
  const [users, setUsers] = useState([]);
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState(null);

  useEffect(() => {
    fetchUsers();
  }, []);

  async function fetchUsers() {
    try {
      const response = await fetch('/api/users');

      // 상태 코드에 따라 다른 UI 표시
      if (response.status === 200) {
        const data = await response.json();
        setUsers(data);
        setError(null);
      } else if (response.status === 401) {
        setError({
          type: 'auth',
          message: '로그인이 필요합니다'
        });
      } else if (response.status === 403) {
        setError({
          type: 'permission',
          message: '권한이 없습니다'
        });
      } else if (response.status === 404) {
        setError({
          type: 'notfound',
          message: '사용자가 없습니다'
        });
      } else if (response.status >= 500) {
        setError({
          type: 'server',
          message: '서버 오류입니다'
        });
      }

    } catch (error) {
      setError({
        type: 'network',
        message: '네트워크 오류입니다'
      });
    } finally {
      setLoading(false);
    }
  }

  // 로딩 중
  if (loading) {
    return <div>로딩 중...</div>;
  }

  // 에러별 다른 UI
  if (error) {
    switch (error.type) {
      case 'auth':
        return (
          <div>
            <p>{error.message}</p>
            <button onClick={() => window.location.href = '/login'}>
              로그인하기
            </button>
          </div>
        );

      case 'permission':
        return (
          <div>
            <p>{error.message}</p>
            <button onClick={() => window.location.href = '/'}>
              홈으로 가기
            </button>
          </div>
        );

      case 'notfound':
        return <div>{error.message}</div>;

      case 'server':
        return (
          <div>
            <p>{error.message}</p>
            <button onClick={fetchUsers}>다시 시도</button>
          </div>
        );

      default:
        return <div>{error.message}</div>;
    }
  }

  // 성공: 사용자 목록 표시
  return (
    <ul>
      {users.map(user => (
        <li key={user.id}>{user.name}</li>
      ))}
    </ul>
  );
}
```

---

## 고급 예제 - 심화 학습

### 🌳 예제 6: Axios 인터셉터로 전역 에러 처리

```javascript
import axios from 'axios';

// Axios 인스턴스 생성
const api = axios.create({
  baseURL: 'https://api.example.com',
  timeout: 10000
});

// 요청 인터셉터 (요청 전에 실행)
api.interceptors.request.use(
  config => {
    // 토큰 자동 추가
    const token = localStorage.getItem('token');
    if (token) {
      config.headers.Authorization = `Bearer ${token}`;
    }
    return config;
  },
  error => Promise.reject(error)
);

// 응답 인터셉터 (응답 후에 실행)
api.interceptors.response.use(
  // 2xx 응답
  response => {
    console.log(`✅ ${response.status} ${response.config.url}`);
    return response;
  },

  // 에러 응답
  async error => {
    const { response, config } = error;

    if (!response) {
      // 네트워크 에러
      showToast('네트워크 오류입니다', 'error');
      return Promise.reject(error);
    }

    // 상태 코드별 처리
    switch (response.status) {
      case 401: // Unauthorized
        // 토큰 만료 시 자동 갱신 시도
        if (!config._retry) {
          config._retry = true;

          try {
            const newToken = await refreshToken();
            localStorage.setItem('token', newToken);
            config.headers.Authorization = `Bearer ${newToken}`;
            return api(config); // 원래 요청 재시도
          } catch (refreshError) {
            // 갱신 실패 → 로그아웃
            localStorage.clear();
            window.location.href = '/login';
            return Promise.reject(refreshError);
          }
        }
        break;

      case 403: // Forbidden
        showToast('권한이 없습니다', 'error');
        break;

      case 404: // Not Found
        showToast('요청한 리소스를 찾을 수 없습니다', 'error');
        break;

      case 409: // Conflict
        showToast(response.data.message || '데이터 충돌이 발생했습니다', 'warning');
        break;

      case 422: // Unprocessable Entity
        // 유효성 검사 에러는 자세히 표시
        const errors = response.data.errors;
        Object.keys(errors).forEach(field => {
          showFieldError(field, errors[field]);
        });
        break;

      case 429: // Too Many Requests
        const retryAfter = response.headers['retry-after'] || 60;
        showToast(`너무 많은 요청입니다. ${retryAfter}초 후 다시 시도하세요`, 'warning');
        break;

      case 500: // Internal Server Error
      case 502: // Bad Gateway
      case 503: // Service Unavailable
      case 504: // Gateway Timeout
        showToast('서버 오류입니다. 잠시 후 다시 시도해주세요', 'error');

        // 에러 추적 서비스에 전송
        sendToErrorTracker(error);
        break;

      default:
        showToast(`오류 발생: ${response.status}`, 'error');
    }

    return Promise.reject(error);
  }
);

// 사용 예시
async function getUserProfile() {
  try {
    const { data } = await api.get('/profile');
    return data;
  } catch (error) {
    // 인터셉터에서 이미 처리했으므로
    // 여기서는 추가 처리만 필요
    console.error('프로필 로드 실패');
    return null;
  }
}
```

### 🌳 예제 7: Express.js 백엔드 완벽 에러 처리

```javascript
const express = require('express');
const app = express();

// === 커스텀 에러 클래스 ===
class AppError extends Error {
  constructor(message, statusCode) {
    super(message);
    this.statusCode = statusCode;
    this.isOperational = true; // 예상된 에러
    Error.captureStackTrace(this, this.constructor);
  }
}

// === 라우트 ===
app.get('/api/users/:id', async (req, res, next) => {
  try {
    const user = await User.findById(req.params.id);

    if (!user) {
      // 404 에러 발생
      throw new AppError('사용자를 찾을 수 없습니다', 404);
    }

    res.json(user);
  } catch (error) {
    next(error); // 에러 미들웨어로 전달
  }
});

app.post('/api/users', async (req, res, next) => {
  try {
    const { email } = req.body;

    // 중복 체크
    const existing = await User.findOne({ email });
    if (existing) {
      throw new AppError('이미 사용 중인 이메일입니다', 409);
    }

    const user = await User.create(req.body);
    res.status(201).json(user);
  } catch (error) {
    next(error);
  }
});

// === 404 핸들러 ===
app.use('*', (req, res, next) => {
  next(new AppError(`경로를 찾을 수 없습니다: ${req.originalUrl}`, 404));
});

// === 전역 에러 핸들러 ===
app.use((err, req, res, next) => {
  err.statusCode = err.statusCode || 500;
  err.status = err.status || 'error';

  // 개발 환경
  if (process.env.NODE_ENV === 'development') {
    res.status(err.statusCode).json({
      status: err.status,
      error: err,
      message: err.message,
      stack: err.stack
    });
  }
  // 프로덕션 환경
  else {
    // 예상된 에러 (operational)
    if (err.isOperational) {
      res.status(err.statusCode).json({
        status: err.status,
        message: err.message
      });
    }
    // 예상하지 못한 에러 (programming)
    else {
      console.error('💥 예상치 못한 에러:', err);

      res.status(500).json({
        status: 'error',
        message: '서버 오류가 발생했습니다'
      });
    }
  }
});

app.listen(3000);
```

---

## 실전 시나리오 - 에러 해결 스토리

### 📖 시나리오 1: 로그인이 안 돼요!

**상황**:
```
사용자 민지: "로그인 버튼 눌렀는데 아무 일도 안 일어나요!"
```

**디버깅 과정**:

```javascript
// 1단계: 개발자 도구 Network 탭 확인
POST /api/login
Status: 401 Unauthorized
Response: { error: "Invalid credentials" }

// 2단계: 코드 확인
async function login(email, password) {
  const response = await fetch('/api/login', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({ email, password })
  });

  // ❌ 문제: 401을 처리하지 않음!
  const data = await response.json();
  return data;
}

// 3단계: 해결
async function login(email, password) {
  const response = await fetch('/api/login', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({ email, password })
  });

  // ✅ 상태 코드 확인 추가
  if (response.status === 401) {
    alert('이메일 또는 비밀번호가 틀렸습니다');
    return null;
  }

  if (response.status === 429) {
    alert('너무 많은 로그인 시도입니다. 잠시 후 다시 시도하세요');
    return null;
  }

  if (!response.ok) {
    alert('로그인 중 오류가 발생했습니다');
    return null;
  }

  const data = await response.json();
  return data;
}
```

**교훈**: 항상 상태 코드를 확인하고 사용자에게 피드백을 주세요!

### 📖 시나리오 2: 이미지가 안 보여요!

**상황**:
```
사용자 철수: "프로필 사진이 안 나와요!"
```

**디버깅**:

```javascript
// Network 탭 확인
GET /api/users/123/avatar
Status: 404 Not Found

// 원인 파악
// 1. 사용자가 아바타를 업로드 안 함 → 404
// 2. 또는 잘못된 URL

// 해결책 1: 기본 이미지 표시
function Avatar({ userId }) {
  const [imageUrl, setImageUrl] = useState(null);

  useEffect(() => {
    fetch(`/api/users/${userId}/avatar`)
      .then(response => {
        if (response.status === 404) {
          // 기본 이미지 사용
          setImageUrl('/images/default-avatar.png');
        } else if (response.ok) {
          return response.blob();
        }
      })
      .then(blob => {
        if (blob) {
          setImageUrl(URL.createObjectURL(blob));
        }
      });
  }, [userId]);

  return <img src={imageUrl} alt="Avatar" />;
}
```

### 📖 시나리오 3: 장바구니에 담을 수 없어요!

**상황**:
```
사용자 영희: "이 상품 장바구니에 담으려는데 안 돼요!"
```

**디버깅**:

```javascript
// Network 탭
POST /api/cart
Status: 422 Unprocessable Entity
Response: {
  error: "Validation Error",
  details: {
    quantity: "재고가 부족합니다. 최대 5개"
  }
}

// 해결: 사용자에게 명확한 피드백
async function addToCart(productId, quantity) {
  const response = await fetch('/api/cart', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({ productId, quantity })
  });

  if (response.status === 422) {
    const error = await response.json();

    // 필드별 에러 표시
    if (error.details.quantity) {
      showFieldError('quantity', error.details.quantity);
    }

    return false;
  }

  if (response.status === 409) {
    alert('이미 장바구니에 있는 상품입니다');
    return false;
  }

  if (response.ok) {
    showSuccess('장바구니에 추가되었습니다!');
    return true;
  }
}
```

---

## 자주 발생하는 에러 TOP 10

### 1. 401 Unauthorized - 토큰 만료

**문제**:
```javascript
// 30분 전에 로그인했는데 갑자기 401
GET /api/profile
Status: 401
```

**해결**:
```javascript
// 토큰 자동 갱신
// (예제 5의 Axios 인터셉터 참고)
```

### 2. 404 Not Found - URL 오타

**문제**:
```javascript
// 오타!
fetch('/api/usres/123') // ❌ usres
fetch('/api/users/123') // ✅ users
```

**예방**:
```javascript
// API 엔드포인트를 상수로 관리
const API_ENDPOINTS = {
  USERS: '/api/users',
  PROFILE: '/api/profile',
  POSTS: '/api/posts'
};

fetch(`${API_ENDPOINTS.USERS}/123`); // ✅ 오타 방지
```

### 3. 500 Internal Server Error - try-catch 누락

**문제**:
```javascript
// 서버 코드
app.get('/api/users/:id', async (req, res) => {
  const user = await User.findById(req.params.id);
  res.json(user.profile.name); // ❌ user가 null이면 폭발!
});
```

**해결**:
```javascript
app.get('/api/users/:id', async (req, res) => {
  try {
    const user = await User.findById(req.params.id);

    if (!user) {
      return res.status(404).json({ error: '사용자 없음' });
    }

    res.json(user.profile?.name || 'Unknown'); // ✅ 안전
  } catch (error) {
    console.error(error);
    res.status(500).json({ error: '서버 오류' });
  }
});
```

### 4. 403 Forbidden - 권한 체크 누락

**문제**:
```javascript
// 일반 사용자가 다른 사용자 삭제 시도
DELETE /api/users/456
Status: 200 ❌ 이러면 안 됨!
```

**해결**:
```javascript
app.delete('/api/users/:id', authenticate, async (req, res) => {
  // 본인 또는 관리자만 삭제 가능
  if (req.user.id !== req.params.id && req.user.role !== 'admin') {
    return res.status(403).json({
      error: '권한이 없습니다'
    });
  }

  await User.delete(req.params.id);
  res.status(204).send();
});
```

### 5. CORS 에러 (보통 0 또는 네트워크 에러로 표시)

**문제**:
```
Access to fetch at 'http://api.example.com' from origin 'http://localhost:3000'
has been blocked by CORS policy
```

**해결**:
```javascript
// 서버 측 (Express.js)
const cors = require('cors');

app.use(cors({
  origin: 'http://localhost:3000',
  credentials: true
}));
```

---

## 디버깅 체크리스트

### 🔍 에러 발생 시 확인 사항

```markdown
1. ✅ Network 탭에서 실제 요청/응답 확인
2. ✅ 상태 코드 확인 (2xx? 4xx? 5xx?)
3. ✅ 응답 본문 확인 (에러 메시지 읽기)
4. ✅ 요청 헤더 확인 (Authorization, Content-Type)
5. ✅ 요청 본문 확인 (JSON 형식 올바른지)
6. ✅ 서버 로그 확인
7. ✅ 환경 변수 확인 (API URL, 토큰 등)
```

### 🛠️ 빠른 디버깅 도구

```javascript
// 콘솔에 상태 코드 자동 로깅
const originalFetch = window.fetch;
window.fetch = function(...args) {
  return originalFetch(...args).then(response => {
    console.log(`${args[0]} → ${response.status}`);
    return response;
  });
};
```

---

## 상태 코드 치트시트

### 📊 빠른 참조표

| 범위 | 의미 | 대표 예시 | 행동 |
|-----|------|----------|------|
| **1xx** | 정보 | 101 (WebSocket) | 계속 대기 |
| **2xx** | 성공 | 200, 201, 204 | 정상 처리 |
| **3xx** | 리다이렉트 | 301, 302, 304 | 다른 곳으로 |
| **4xx** | 클라이언트 에러 | 400, 401, 403, 404 | 요청 수정 |
| **5xx** | 서버 에러 | 500, 502, 503 | 재시도 또는 대기 |

### 🎯 상황별 사용 가이드

#### 사용자 등록 API
```
POST /api/users
→ 201 Created (성공)
→ 400 Bad Request (필수 필드 누락)
→ 409 Conflict (이메일 중복)
→ 422 Unprocessable Entity (형식 오류)
```

#### 로그인 API
```
POST /api/login
→ 200 OK (성공)
→ 401 Unauthorized (비밀번호 틀림)
→ 429 Too Many Requests (시도 횟수 초과)
```

#### 리소스 조회 API
```
GET /api/users/123
→ 200 OK (성공)
→ 304 Not Modified (캐시 사용)
→ 401 Unauthorized (로그인 필요)
→ 404 Not Found (사용자 없음)
```

#### 리소스 삭제 API
```
DELETE /api/posts/456
→ 204 No Content (성공)
→ 403 Forbidden (권한 없음)
→ 404 Not Found (게시글 없음)
→ 409 Conflict (삭제 불가 상태)
```

---

## 용어 설명

### HTTP 관련

**HTTP (HyperText Transfer Protocol)**
> 웹 브라우저와 서버가 대화하는 규칙이에요. 마치 외국인과 대화할 때 영어를 쓰는 것처럼, 컴퓨터들이 서로 이해하려면 HTTP라는 공통 언어를 사용해요.

**Status Code (상태 코드)**
> 서버가 브라우저에게 보내는 "결과 번호표"예요.
> - 200번: "성공했어요!"
> - 404번: "못 찾았어요!"
> - 500번: "고장났어요!"

**Request (요청)**
> 브라우저가 서버에게 "이거 주세요!"라고 부탁하는 것
> - 예: "ID 123번 사용자 정보 주세요"

**Response (응답)**
> 서버가 브라우저에게 "여기 있어요!" 또는 "없어요!"라고 대답하는 것
> - 상태 코드 + 데이터

**Endpoint (엔드포인트)**
> API의 "주소"예요.
> - `/api/users` = 사용자 목록 주소
> - `/api/posts/123` = 123번 게시글 주소

### 인증/권한 관련

**Authentication (인증)**
> "당신이 누구인지" 확인하는 것
> - 로그인 = 인증
> - 비유: 신분증 제시

**Authorization (권한)**
> "당신이 이것을 할 수 있는지" 확인하는 것
> - 관리자 권한 확인 = 권한
> - 비유: VIP 카드 확인

**Token (토큰)**
> 로그인 후 받는 "입장권" 같은 것
> - JWT (JSON Web Token)가 가장 흔함
> - 매 요청마다 토큰을 함께 보냄

**Bearer Token**
> 토큰 앞에 "Bearer"를 붙여서 보내는 방식
> ```
> Authorization: Bearer eyJhbGci0iJIUzI1NiIs...
> ```

### 에러 처리 관련

**try-catch**
> 에러가 날 수 있는 코드를 안전하게 감싸는 것
> ```javascript
> try {
>   // 시도
>   위험한코드();
> } catch (error) {
>   // 에러 발생 시 처리
>   console.error("에러:", error);
> }
> ```

**Exception (예외)**
> 프로그램이 예상하지 못한 상황
> - 예: 파일이 없음, 네트워크 끊김

**Error Handling (에러 처리)**
> 에러가 발생했을 때 프로그램이 죽지 않고 적절히 대응하는 것

### API 설계 관련

**REST API**
> 웹 서비스를 만드는 일종의 "규칙"
> - URL로 자원을 표현
> - HTTP 메서드로 행동을 표현

**CRUD**
> 데이터의 4가지 기본 작업
> - Create (생성) → POST
> - Read (읽기) → GET
> - Update (수정) → PUT/PATCH
> - Delete (삭제) → DELETE

**Idempotent (멱등성)**
> 같은 요청을 여러 번 해도 결과가 같은 것
> - GET: 멱등 (여러 번 조회해도 같음)
> - DELETE: 멱등 (이미 삭제되면 계속 없음)
> - POST: 비멱등 (매번 새로 생성됨)

### 네트워크 관련

**CORS (Cross-Origin Resource Sharing)**
> 다른 도메인의 리소스를 안전하게 사용하는 방법
> - 브라우저의 보안 정책
> - 서버에서 허용해줘야 함

**Cache (캐시)**
> 자주 쓰는 데이터를 미리 저장해두는 것
> - 304 Not Modified가 캐시 관련

**Proxy (프록시)**
> 중간에서 요청을 대신 전달해주는 서버
> - 502, 504가 프록시 관련 에러

**Timeout (타임아웃)**
> 너무 오래 기다리면 포기하는 것
> - 기본: 30초~60초

### 보안 관련

**Rate Limiting (속도 제한)**
> 너무 많은 요청을 막는 것
> - 429 Too Many Requests
> - DDoS 공격 방어

**HTTPS**
> HTTP의 보안 버전
> - 데이터 암호화
> - 중간에서 훔쳐보기 불가능

**XSS (Cross-Site Scripting)**
> 악의적인 스크립트를 주입하는 공격
> - 입력 값 검증으로 방어

**SQL Injection**
> 데이터베이스에 악의적인 쿼리를 주입하는 공격
> - 파라미터 바인딩으로 방어

---

## 추가 학습 자료

### 📚 공식 문서
- [MDN - HTTP Status Codes](https://developer.mozilla.org/ko/docs/Web/HTTP/Status)
- [RFC 7231 - HTTP/1.1 Semantics and Content](https://tools.ietf.org/html/rfc7231)

### 🎥 동영상 강의
- "HTTP 완벽 가이드" - 우아한테크 유튜브
- "REST API 설계 가이드" - 노마드 코더

### 🛠️ 실습 도구
- [HTTPBin](https://httpbin.org/) - HTTP 요청 테스트
- [Postman](https://www.postman.com/) - API 테스팅 도구
- [HTTPie](https://httpie.io/) - CLI HTTP 클라이언트

### 📖 추천 도서
- "HTTP 완벽 가이드" - David Gourley
- "RESTful Web API Patterns" - Mike Amundsen

---

## 연결된 노트

### 관련 개발 개념
- [[RESTful API 설계 가이드]]
- [[인증과 인가의 차이]]
- [[JWT 토큰 완벽 가이드]]
- [[API 보안 베스트 프랙티스]]

### 프론트엔드
- [[Fetch API 사용법]]
- [[Axios 심화 가이드]]
- [[React에서의 에러 처리]]
- [[사용자 친화적인 에러 메시지]]

### 백엔드
- [[Express.js 에러 미들웨어]]
- [[Node.js 예외 처리]]
- [[API Rate Limiting 구현]]
- [[로깅과 모니터링]]

### 디버깅
- [[브라우저 개발자 도구 마스터]]
- [[네트워크 탭 활용법]]
- [[에러 추적 도구 (Sentry)]]

---

## 📝 마무리하며

### 🎉 축하합니다!

이 가이드를 완독하셨다면 이제 HTTP 상태 코드 마스터입니다! 🎓

**여러분은 이제 할 수 있어요**:
- ✅ 모든 상태 코드의 의미 이해
- ✅ 적절한 상태 코드 선택
- ✅ 에러를 빠르게 디버깅
- ✅ 사용자 친화적인 에러 처리
- ✅ 프로덕션급 API 설계

### 🚀 다음 단계

1. **실습**: 직접 API를 만들어보세요
2. **리뷰**: 기존 코드의 상태 코드 점검
3. **공유**: 팀원들과 이 지식 나누기
4. **심화**: RESTful API 설계 공부

### 💬 기억하세요

> "상태 코드는 단순한 숫자가 아닙니다. 서버와 클라이언트가 나누는 대화이고, 개발자와 사용자를 연결하는 다리입니다!" 🌉

**핵심 요약 3줄**:
1. 2xx = 성공 😊
2. 4xx = 당신 잘못 🤷‍♂️
3. 5xx = 서버 잘못 🙇‍♂️

---

**Happy Coding! 💻✨**

**문서 정보**
- 작성일: 2025-10-12
- 대상: 초보 웹 개발자
- 난이도: 입문 → 중급 → 고급
- 예상 학습 시간: 3-4시간
- 실습 포함: 7개 예제 + 3개 실전 시나리오

**태그**: #HTTP #상태코드 #웹개발 #API #에러처리 #디버깅 #REST #초보자가이드