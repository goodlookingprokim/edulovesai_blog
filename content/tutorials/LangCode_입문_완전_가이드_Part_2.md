---
title: "LangCode 입문 완전 가이드 - Part 2: 핵심 명령어 마스터"
created: '2025-11-16'
last_modified: '2025-11-16'
tags:
  - LangCode
  - CLI/명령어
  - 실습/예제
  - AI/도구
  - 초급자/교육
status: "진행중"
type: "입문가이드"
priority: "high"
---

# LangCode 입문 완전 가이드 - Part 2: 핵심 명령어 마스터

## 📋 목차

1. [[#3가지 주요 명령어 개요]]
2. [[#Chat 명령어 완전 가이드]]
3. [[#Feature 명령어 완전 가이드]]
4. [[#Fix 명령어 완전 가이드]]
5. [[#명령어 선택 결정 트리]]
6. [[#실전 연습 문제]]
7. [[#자주 하는 실수]]

---

## 3가지 주요 명령어 개요

### 명령어의 세 가지 종류

LangCode의 핵심은 단 **3가지 명령어**입니다.

```
┌─────────────────────────────────────────┐
│         LangCode 3가지 명령어            │
├─────────────────────────────────────────┤
│ 1. langcode chat    → 대화형 질문        │
│ 2. langcode feature → 새 기능 자동 구현   │
│ 3. langcode fix     → 버그 자동 수정      │
└─────────────────────────────────────────┘
```

### 비유로 이해하기

**음식점에 비유하면:**

| 명령어 | 상황 | 예시 |
|-------|------|------|
| **chat** | 메뉴에 대해 물어보기 | "이 요리에 알레르기 있나요?" |
| **feature** | 새로운 요리 추가 요청 | "돈까스를 메뉴에 추가해줄래?" |
| **fix** | 음식이 잘못됨 | "이 음식이 너무 맵아요. 다시 해줄래?" |

---

## Chat 명령어 완전 가이드

### 💬 Chat이란?

**정의**: AI와 대화하듯이 질문하고 답변받는 방식

**특징:**
- 🔄 계속 대화 가능 (대화 이어짐)
- ❓ 어떤 질문이든 OK
- 💭 깊게 생각할 시간 있음
- 🎬 인터랙티브 (사람이 개입)

### 🚀 기본 사용법

**가장 간단한 명령어:**

```bash
langcode chat
```

**이러면 뭐가 되나요?**

1️⃣ LangCode가 시작됨
2️⃣ 메뉴가 나타남
3️⃣ 옵션을 선택
4️⃣ 질문을 입력
5️⃣ AI가 대답

### 📝 실전 예제

#### 예제 1: 기본 질문

**상황:** Python 코드를 작성 중인데 list가 뭔지 모름

```bash
$ langcode chat

# 메뉴에서 "1. Chat with AI" 선택

# 질문 입력:
Python의 list 자료형이 뭔가요?

# 대답:
Python의 list는 여러 개의 값을 순서대로 저장하는 컨테이너입니다.
예시:
fruits = ["apple", "banana", "cherry"]
print(fruits[0])  # "apple" 출력
```

**학습 포인트:** Chat은 **질문에 답변하는 도구**입니다.

#### 예제 2: 코드 설명 요청

**상황:** 이 코드가 뭐 하는 건지 모름

```bash
$ langcode chat

# 다음 코드를 설명해줄래?
def calculate_average(numbers):
    total = sum(numbers)
    average = total / len(numbers)
    return average

# AI의 답변:
이 함수는 숫자 목록의 평균을 구합니다.
단계:
1. sum() - 모든 숫자를 더함
2. len() - 숫자의 개수를 셈
3. 나누기 - 평균 계산
4. 반환 - 결과를 돌려줌

result = calculate_average([10, 20, 30])  # 20
```

### 🎯 Chat 옵션 분석

#### LLM 선택하기

```bash
langcode chat --llm claude

# vs

langcode chat --llm gemini
```

**어떨 때 누구를 쓸까?**

| 상황 | 추천 |
|-----|------|
| 깐깐한 코드 리뷰 | Claude |
| 빠른 답변 | Gemini |
| 창의적인 아이디어 | Claude |
| 빠른 정보 검색 | Gemini |

#### Deep Mode vs ReAct Mode

```bash
# 빠른 답변 (기본값)
langcode chat --mode react

# vs

# 깊게 생각하는 답변
langcode chat --mode deep
```

**비유:**
- **ReAct**: 빠르게 달리기 (🏃)
- **Deep**: 천천히 깊게 생각하기 (🧘)

### ⚙️ Chat 옵션 테이블

```bash
langcode chat [옵션들]
```

| 옵션 | 예시 | 의미 |
|-----|------|------|
| `--llm` | `--llm claude` | AI 선택 |
| `--mode` | `--mode deep` | 생각하는 깊이 |
| `--router` | `--router` | 자동 AI 선택 |
| `--priority` | `--priority cost` | 중점: cost/speed/quality |

---

## Feature 명령어 완전 가이드

### 🆕 Feature란?

**정의**: 새로운 기능을 구현하도록 AI에게 지시하는 방식

**특징:**
- 🤖 자동 구현 (사람이 덜 개입)
- 📋 계획 → 구현 → 테스트 자동화
- ✅ 코드 자동 생성
- 🔍 결과물 미리보기 가능

### 📝 기본 구조

```bash
langcode feature "구현할 기능 설명"
```

### 🎬 실전 예제

#### 예제 1: 간단한 기능 (초급)

**상황:** Python으로 간단한 계산기 만들기

```bash
langcode feature "두 숫자를 더하는 함수 만들어"

# 뒤에서 벌어지는 일:
# 1. AI가 계획을 세움 (5초)
# 2. 코드를 작성 (10초)
# 3. 코드를 보여줌
#    더하기(a, b)
#      return a + b
# 4. 파일에 저장할지 물어봄
```

#### 예제 2: 중급 기능

**상황:** 사용자 인증 기능 추가

```bash
langcode feature "사용자 로그인 시스템 구현" --test-cmd "pytest -q"

# 뒤에서 벌어지는 일:
# 1. 로그인 기능 계획
# 2. 데이터베이스 코드 작성
# 3. 로그인 함수 구현
# 4. 테스트 코드 자동 실행 (pytest)
# 5. 모든 테스트 통과 여부 확인
```

**--test-cmd 설명:**
- `pytest -q` = 테스트 실행 후 간단하게 결과만 보기
- 기능이 제대로 작동하는지 확인!

#### 예제 3: 실제 상황 (중급)

**시나리오:** 웹 프로젝트에 새로운 API 엔드포인트 추가

```bash
langcode feature "POST /users 엔드포인트 추가: 새 사용자 생성" \
  --test-cmd "npm test" \
  --apply

# 명령어 해석:
# feature: 새 기능 구현
# "POST /users...": 뭘 구현할 건지
# --test-cmd "npm test": 테스트 실행 명령어
# --apply: 자동으로 변경 적용 (물어보지 말고)
```

### 🎯 Feature 옵션

```bash
langcode feature "기능 설명" [옵션]
```

| 옵션 | 예시 | 의미 |
|-----|------|------|
| `--test-cmd` | `--test-cmd "pytest -q"` | 테스트 실행 명령어 |
| `--apply` | `--apply` | 자동 적용 (묻지 말고) |
| `--llm` | `--llm claude` | 사용할 AI |
| `--router` | `--router` | 상황 맞게 AI 선택 |

### ⚠️ Feature 사용할 때 주의

**체크리스트:**

```
✅ 기능을 명확하게 설명했는가?
   나쁜 예: "로그인 만들어"
   좋은 예: "사용자 이메일/패스워드로 로그인 하는 기능"

✅ 테스트 명령어를 알고 있는가?
   예: npm test, pytest -q, cargo test

✅ --apply를 썼으면 백업이 있는가?
   git commit을 먼저 하자!
```

---

## Fix 명령어 완전 가이드

### 🐛 Fix란?

**정의**: 버그(오류)를 찾아서 자동으로 수정하는 명령어

**특징:**
- 🔍 자동으로 문제 찾기
- 🔧 원인을 파악하고 수정
- ✅ 테스트로 검증
- 📝 수정 사항 설명

### 📝 기본 구조

```bash
langcode fix "버그 설명"
langcode fix --log error.log "버그 설명"
```

### 🎬 실전 예제

#### 예제 1: 간단한 버그

**상황:** 코드를 실행하니 오류가 남

```bash
langcode fix "이 함수가 오류를 낸다:
def divide(a, b):
    return a / b

divide(10, 0)  # ZeroDivisionError 발생"

# AI의 분석:
# 문제: 0으로 나누려고 함
# 해결: 사전에 체크
# 수정된 코드:
def divide(a, b):
    if b == 0:
        return "0으로 나눌 수 없습니다"
    return a / b
```

#### 예제 2: 로그 파일로 버그 찾기

**상황:** 웹 서버가 자꾸 다운됨

```bash
langcode fix --log server_crash.log "서버가 매일 오전 10시에 다운된다"

# 로그 파일 내용:
# [ERROR] Memory leak detected
# [ERROR] Database connection timeout
# [ERROR] Unhandled exception in worker thread

# AI의 분석:
# 원인: 메모리 누수 + 데이터베이스 연결 문제
# 해결책:
# 1. 메모리 릭 고치기
# 2. 데이터베이스 연결 풀 개선
# 3. 타임아웃 늘리기
```

#### 예제 3: 테스트 실패 수정

**상황:** 테스트가 실패해서 배포 안 됨

```bash
langcode fix "테스트 실패" --test-cmd "npm test" --apply

# 순서:
# 1. 테스트 실행 → 실패 원인 파악
# 2. 원인 찾아서 수정
# 3. 테스트 다시 실행 → 성공 확인
# 4. 코드 자동 저장
```

### 🎯 Fix 옵션

```bash
langcode fix "버그 설명" [옵션]
```

| 옵션 | 예시 | 의미 |
|-----|------|------|
| `--log` | `--log crash.log` | 에러 로그 파일 |
| `--test-cmd` | `--test-cmd "npm test"` | 테스트 명령어 |
| `--apply` | `--apply` | 자동 적용 |
| `--llm` | `--llm claude` | 사용할 AI |

---

## 명령어 선택 결정 트리

### 🌳 어떤 명령어를 쓸까?

```
질문이 있어?
│
├─ YES → Chat을 사용하세요
│   예: "이 코드가 뭐 하는 거야?"
│
└─ NO → 다음 질문으로

    실제로 코드를 만들어야 해?
    │
    ├─ 새로운 기능 → Feature를 사용하세요
    │   예: "로그인 기능 만들어"
    │
    └─ 버그를 고쳐야 해? → Fix를 사용하세요
        예: "이 함수가 오류를 낸다"
```

### 📋 빠른 선택표

| 상황 | 명령어 | 예시 |
|-----|-------|------|
| **이해가 안 돼요** | chat | "Python list 뭐야?" |
| **새 기능 만들기** | feature | "로그인 추가해" |
| **버그 고치기** | fix | "로그인 버튼이 안 눌려" |
| **코드 최적화** | chat | "이 코드 더 빠르게" |
| **자동화하기** | feature | "매일 오전 9시에 리포트 보내" |

---

## 실전 연습 문제

### 초급 문제 (따라 하기)

#### 문제 1: Chat 사용해보기

```bash
# 터미널에 입력
langcode chat

# 질문 입력
"Python의 for 루프가 뭐예요?"

# 예상 결과: for 루프 설명 + 예제
```

**확인:**
- [ ] 메뉴가 나타났는가?
- [ ] 질문을 입력했는가?
- [ ] 답변이 나왔는가?

#### 문제 2: Feature 사용해보기

```bash
# 프로젝트 폴더로 이동
cd ~/my_project

# Feature 실행
langcode feature "더하기 함수 만들어"

# 예상: 함수 코드 자동 생성
```

### 중급 문제 (응용)

#### 문제 3: Fix 사용해보기

```bash
# 버그가 있는 코드 작성
cat > buggy.py << 'EOF'
def multiply(a, b):
    return a + b  # 버그: +를 * 로 해야 함

print(multiply(3, 4))  # 출력: 7 (틀림, 12여야 함)
EOF

# Fix로 버그 고치기
langcode fix "multiply 함수가 나누기 대신 더하기를 한다"
```

### 고급 문제 (실전)

#### 문제 4: 복합 작업

**상황:** 웹 프로젝트에 사용자 등록 기능 추가

```bash
# 1단계: 설계 확인 (chat)
langcode chat --llm claude
"사용자 등록 API 설계에서 꼭 필요한 항목이 뭐야?"

# 2단계: 기능 구현 (feature)
langcode feature "회원가입 API 엔드포인트 만들기" \
  --test-cmd "npm test" \
  --apply

# 3단계: 버그 수정 (fix)
langcode fix "이메일 검증이 작동 안 한다" \
  --test-cmd "npm test"
```

---

## 자주 하는 실수

### ❌ 실수 1: 명확하지 않은 지시

```bash
# 나쁜 예
langcode feature "기능 추가"  # 뭔 기능?

# 좋은 예
langcode feature "사용자 이름을 저장하는 input 필드 추가"
```

**해결책:** "누가 뭘 하는 건지" 명확하게!

### ❌ 실수 2: --apply 무분별하게 사용

```bash
# 위험 ⚠️
langcode feature "뭔가 추가" --apply

# 안전 ✅
langcode feature "뭔가 추가"
# 먼저 결과를 보고
# 괜찮으면 그 다음 apply
```

**해결책:** 변경 사항을 먼저 확인하세요!

### ❌ 실수 3: 환경 변수 설정 안 함

```bash
# 하면
langcode chat
# 오류: "API 키가 없습니다"

# 먼저 하기
export ANTHROPIC_API_KEY="키값"
export GOOGLE_API_KEY="키값"
```

**해결책:** Part 1의 "환경 변수 설정"을 다시 보세요.

### ❌ 실수 4: 로그 파일 없이 fix 사용

```bash
# 약함
langcode fix "이상한 오류가 난다"

# 강함
langcode fix --log error.log "이상한 오류가 난다"
```

**해결책:** 오류 메시지를 파일로 남겨서 같이 제공하세요!

### ❌ 실수 5: 한국어로 지시해도 괜찮은데 영어가 더 잘 작동함

```bash
# 한국어 (작동)
langcode feature "사용자 로그인 추가"

# 영어 (더 잘 작동)
langcode feature "Add user login functionality"
```

**해결책:** 영어로 쓰는 게 더 정확한 결과를 얻습니다!

---

## 💡 팁과 꿀팁

### 팁 1: 체이닝으로 연속 작업하기

```bash
# Step 1: Chat으로 설계 확인
langcode chat

# Step 2: Feature로 구현
langcode feature "설계에 맞게 구현"

# Step 3: Fix로 테스트 오류 해결
langcode fix --test-cmd "npm test"
```

### 팁 2: 프로젝트 폴더에서 작업하기

```bash
# 항상 프로젝트 폴더에서
cd ~/my_project
langcode feature "뭔가 추가"

# 이러면 파일 위치를 자동으로 찾음
```

### 팁 3: 이전 대화 이어받기

```bash
# 첫 번째
langcode chat --llm claude

# 같은 AI로 다시 시작 (설정이 유지됨)
langcode chat --llm claude
```

---

## 📚 다음 학습

이제 기본을 배웠습니다!

다음 단계:
- **Part 3**: 고급 기능 (Router, Deep Mode, 자동화)
- **Part 4**: 실제 프로젝트에서 사용하기
- **Part 5**: MCP 통합 및 커스터마이징

---

## 🎯 체크리스트: Part 2 마무리

```
✅ Chat 명령어 이해했나?
✅ Feature 명령어 이해했나?
✅ Fix 명령어 이해했나?
✅ 3개 중 어느 걸 쓸지 판단할 수 있나?
✅ 실전 예제를 따라 해봤나?
```

모두 체크했다면 Part 3로 진행해보세요!

