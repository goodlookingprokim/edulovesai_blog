---
title: 초보 개발자를 위한 MCP 기반 AI 챗봇 완전 가이드
created: 2025-10-12
last_modified: 2025-10-12
tags:
  - AI/챗봇
  - MCP/프로토콜
  - ESP32/하드웨어
  - 임베디드/시스템
  - 초보자/가이드
  - 음성인식/기술
  - IoT/제어
  - 샤오즈/XiaoZhi
status: 완료
type: 교육자료
priority: high
source: https://github.com/78/xiaozhi-esp32
license: MIT
share_link: https://share.note.sx/12wz5wpt#GQgcModSE/Fxa0othlw1mnBliYLK+YBU7Tt4gMZtEH8
share_updated: 2025-10-12T12:36:47+09:00
---

# 초보 개발자를 위한 MCP 기반 AI 챗봇 완전 가이드

> 📌 **원본 프로젝트**: [XiaoZhi ESP32 (小智)](https://github.com/78/xiaozhi-esp32)
> 👨‍💻 **개발자**: GitHub @78
> 📜 **라이선스**: MIT License (상업적 이용 가능)
> 🌐 **공식 사이트**: [xiaozhi.me](https://xiaozhi.me)

## 📋 목차 (클릭하여 이동)

1. [[#프로젝트 소개 및 배경]]
2. [[#이야기로 시작하는 MCP 챗봇의 세계]]
3. [[#MCP 챗봇이란 무엇인가요]]
4. [[#핵심 구성 요소 이해하기]]
5. [[#하드웨어 선택 가이드]]
6. [[#소프트웨어 개발 환경 준비]]
7. [[#단계별 구현 가이드]]
8. [[#실전 예제 3가지 난이도별]]
9. [[#문제 해결 및 디버깅]]
10. [[#확장 및 응용 아이디어]]
11. [[#커뮤니티 및 학습 자료]]
12. [[#용어 사전]]

---

## 프로젝트 소개 및 배경

### 🎯 XiaoZhi (小智) 프로젝트란?

**XiaoZhi ESP32**는 오픈소스 음성 AI 챗봇 프로젝트로, 2025년 불과 2개월 만에 **10만 대 이상의 AI 기기 보급**을 이끈 혁신적인 프로젝트입니다.

#### 프로젝트 주요 특징
- 🌟 **GitHub Stars**: 6,000+ (2025년 10월 기준)
- 🤖 **지원 하드웨어**: 70개 이상의 ESP32 보드
- 🌍 **다국어 지원**: 한국어, 영어, 중국어, 일본어
- 🆓 **무료 사용**: MIT 라이선스 (상업적 이용 가능)
- ☁️ **클라우드 지원**: xiaozhi.me에서 무료 Qwen AI 모델 제공

#### 왜 이 프로젝트가 특별한가?

1. **진입 장벽 제거**: $20대 저가 하드웨어로 AI 비서 구축 가능
2. **교육 친화적**: 브레드보드 DIY부터 PCB 제작까지 단계별 학습
3. **확장성**: MCP 프로토콜로 스마트홈, PC 제어 등 무한 확장
4. **활발한 커뮤니티**: QQ 그룹 1만+ 회원, 다양한 튜토리얼

### 📊 프로젝트 발전 히스토리

| 버전 | 출시일 | 주요 변경사항 |
|------|--------|---------------|
| **v1.0** | 2024년 8월 | 초기 릴리즈, 기본 음성 대화 |
| **v1.9.2** | 2025년 1월 | 안정화 버전 (v1 최종) |
| **v2.0** | 2025년 3월 | 파티션 재설계, MCP 고도화 |
| **v2.x** | 2025년 6월+ | ESP32-P4 지원, 다국어 확대 |

> ⚠️ **주의**: v1과 v2는 파티션 테이블이 달라 OTA 업그레이드 불가. v1 사용자는 수동 플래시 필요.

### 🎥 공식 튜토리얼 영상

- **B站 원본 영상**: [AI 여자친구 만들기 초보자 가이드](https://www.bilibili.com/video/BV1XnmFYLEJN/)
- **사용 데모**: [AI에게 카메라를 주면 생기는 일](https://www.bilibili.com/video/BV1bpjgzKEhd/)
- **백엔드 콘솔 사용법**: [xiaozhi.me 설정 가이드](https://www.bilibili.com/video/BV1jUCUY2EKM/)

### 📚 공식 문서 및 리소스

- **프로젝트 위키**: [小智 AI 챗봇 백과사전 (飞书)](https://ccnphfhqs21z.feishu.cn/wiki/F5krwD16viZoF0kKkvDcrZNYnhb)
- **초보자 펌웨어 가이드**: [新手烧录固件教程](https://ccnphfhqs21z.feishu.cn/wiki/Zpz4wXBtdimBrLk25WdcXzxcnNS)
- **GitHub 저장소**: https://github.com/78/xiaozhi-esp32
- **공식 서버**: https://xiaozhi.me (무료 회원가입 후 Qwen 모델 사용 가능)

---

## 이야기로 시작하는 MCP 챗봇의 세계

### 🎭 상황극: 민수의 첫 AI 프로젝트

**민수**: "엄마, 나 오늘 AI 챗봇 만들 거야!"

**엄마**: "AI? 그게 뭔데? 컴퓨터 프로그램?"

**민수**: "응! 내가 말하면 알아듣고 대답도 해주는 거야. 마치 시리나 알렉사처럼!"

**엄마**: "아, 그런 거? 근데 그거 어렵지 않아?"

**민수**: "처음엔 나도 그렇게 생각했어. 근데 이 가이드를 보니까 레고 조립하는 것처럼 하나씩 따라하면 되더라고!"

이 가이드는 바로 민수처럼 AI 챗봇을 만들고 싶지만 어디서부터 시작해야 할지 막막한 여러분을 위한 것입니다.

### 🎯 이 가이드로 당신이 할 수 있게 되는 것들

- 🗣️ **음성으로 대화하는 AI 비서** 만들기
- 💡 **집 안의 전등을 목소리로 제어**하기
- 🤖 **나만의 맞춤형 AI 친구** 프로그래밍하기
- 📱 **스마트폰처럼 동작하는 IoT 기기** 제작하기

---

## MCP 챗봇이란 무엇인가요

### 🎓 파인만식 설명: 5살 아이에게 설명하듯이

**질문**: "MCP 기반 AI 챗봇이 뭐예요?"

**파인만 답변**:
> 여러분이 레고 블록으로 자동차를 만든다고 생각해보세요.
> - 바퀴 블록 = **마이크** (듣는 부분)
> - 운전대 블록 = **ESP32 칩** (생각하는 두뇌)
> - 자동차 몸체 = **스피커** (말하는 부분)
> - 조립 설명서 = **MCP 프로토콜** (부품들이 서로 대화하는 방법)
>
> MCP 챗봇은 이 모든 블록들이 "MCP"라는 특별한 언어로 서로 대화하면서
> 사람의 말을 듣고, 생각하고, 대답하는 똑똑한 로봇입니다!

### 📊 일반인용 설명 (10살 수준)

MCP 챗봇은 세 가지 마법을 가진 친구입니다:

1. **듣는 마법** 👂
   - 당신이 "안녕?"이라고 말하면 소리를 컴퓨터 언어로 바꿔요
   - 마치 한국어를 영어로 통역하는 것처럼!

2. **생각하는 마법** 🧠
   - 큰 AI 두뇌(Qwen, DeepSeek)에게 물어봐요
   - "이 친구가 '안녕?'이라고 했는데 뭐라고 대답할까?"

3. **말하는 마법** 🗣️
   - AI가 생각한 답을 다시 소리로 바꿔서 들려줘요
   - "안녕! 오늘 기분이 어때?" 이런 식으로요!

### 🎓 개발자용 설명 (기술적 정의)

**MCP(Model Context Protocol) 기반 AI 챗봇**은:

```
음성 입력 → ASR(음성→텍스트) → LLM(AI 추론) → TTS(텍스트→음성) → 음성 출력
         ↑                                                              ↓
         └──────────────── MCP 프로토콜로 통신 ──────────────────────┘
```

**핵심 특징**:
- **실시간 스트리밍 처리**: 대화가 끊기지 않고 자연스럽게 흐름
- **프로토콜 기반 확장성**: 새로운 기능을 레고 블록처럼 추가 가능
- **멀티 플랫폼 지원**: ESP32, 안드로이드, 리눅스 등 다양한 기기에서 작동

---

## 핵심 구성 요소 이해하기

### 🧩 시스템 아키텍처: 레고 조립도처럼 이해하기

#### 레벨 1: 초보자용 블록 다이어그램

```
┌─────────────────────────────────────────────────────┐
│               📱 당신의 MCP 챗봇                    │
├─────────────────────────────────────────────────────┤
│                                                       │
│  🎤 마이크          →  듣기 모듈                     │
│  (소리 수집)           (ESP-SR 음성인식)             │
│                                                       │
│  🧠 두뇌 칩         →  생각 모듈                     │
│  (ESP32-S3)            (Wi-Fi/통신)                  │
│                                                       │
│  🔊 스피커          →  말하기 모듈                   │
│  (소리 출력)           (OPUS 오디오)                 │
│                                                       │
│  📺 화면            →  보여주기 모듈                 │
│  (OLED/LCD)            (이모지 표시)                 │
│                                                       │
└─────────────────────────────────────────────────────┘
           ↕️ MCP 프로토콜 (부품 간 대화 언어)
┌─────────────────────────────────────────────────────┐
│               ☁️ 클라우드 AI 서버                   │
│  (Qwen / DeepSeek 같은 똑똑한 AI)                   │
└─────────────────────────────────────────────────────┘
```

#### 레벨 2: 중급자용 상세 흐름도

```
사용자: "오늘 날씨 어때?"
         ↓
┌────────────────────────────────────────────────────┐
│ 1️⃣ 음성 입력 단계                                  │
│   - MEMS 마이크가 소리 감지                        │
│   - ESP-SR이 깨우기 단어("샤오즈") 인식            │
│   - 녹음 시작: PCM 오디오 → OPUS 압축              │
└────────────────────────────────────────────────────┘
         ↓ WebSocket/MQTT 통신
┌────────────────────────────────────────────────────┐
│ 2️⃣ 클라우드 처리 단계                              │
│   - ASR: OPUS → "오늘 날씨 어때?" (텍스트)         │
│   - LLM: Qwen AI가 답변 생성                       │
│         "오늘은 맑고 따뜻해요! 🌞"                 │
│   - TTS: 텍스트 → OPUS 음성 파일                  │
└────────────────────────────────────────────────────┘
         ↓ 스트리밍 전송
┌────────────────────────────────────────────────────┐
│ 3️⃣ 음성 출력 단계                                  │
│   - OPUS 디코딩 → PCM 오디오                       │
│   - I2S 프로토콜로 스피커 재생                     │
│   - OLED에 "😊" 이모지 표시                        │
└────────────────────────────────────────────────────┘
```

#### 레벨 3: 전문가용 기술 스택

| 계층 | 구성 요소 | 기술 스택 |
|------|-----------|-----------|
| **하드웨어** | 메인 MCU | ESP32-S3 (Xtensa Dual-Core 240MHz) |
|  | 네트워크 | Wi-Fi 802.11b/g/n / ML307 Cat.1 4G |
|  | 오디오 입력 | MEMS 마이크 (I2S 인터페이스) |
|  | 오디오 출력 | MAX98357A DAC + 스피커 (I2S) |
|  | 디스플레이 | SSD1306 OLED 128×64 / ST7789 LCD |
| **펌웨어** | 개발 환경 | ESP-IDF 5.4+ (FreeRTOS 기반) |
|  | 음성 인식 | ESP-SR (WakeNet, MultiNet) |
|  | 오디오 코덱 | OPUS (저대역폭 고품질) |
|  | 통신 프로토콜 | WebSocket / MQTT+UDP |
| **클라우드** | ASR 엔진 | 실시간 스트리밍 음성→텍스트 |
|  | LLM | Qwen-2.5 / DeepSeek-V3 |
|  | TTS 엔진 | 한국어/영어/일본어 지원 |
|  | MCP 서버 | 스마트홈, PC제어, 이메일 등 |

---

## 하드웨어 선택 가이드

### 🎯 지원 하드웨어 현황 (70개+ 보드)

XiaoZhi 프로젝트는 다양한 ESP32 하드웨어를 지원합니다. 아래는 공식 지원 보드 목록입니다.

#### 📊 하드웨어 호환성 매트릭스

| 카테고리 | 제품명 | 가격대 | 난이도 | 특징 |
|----------|--------|--------|--------|------|
| **공식 보드** | ESP32-S3-BOX3 | $25-30 | ⭐ | 터치스크린, 공식 문서 |
| **올인원** | M5Stack CoreS3 | $40-50 | ⭐ | 완전 통합, 케이스 포함 |
| **올인원** | M5Stack AtomS3R + Echo Base | $35-45 | ⭐⭐ | 컴팩트, 좋은 음질 |
| **디스플레이 강화** | LILYGO T-Circle-S3 | $20-30 | ⭐⭐ | 원형 디스플레이 |
| **고품질 화면** | Waveshare ESP32-S3-Touch-AMOLED-1.8 | $25-35 | ⭐⭐ | AMOLED 터치 |
| **DIY 친화** | 立创实战派 ESP32-S3 | $15-20 | ⭐⭐⭐ | 저렴, 교육용 |
| **커스텀** | 神奇按钮 2.4 (Magic Button) | $30-40 | ⭐⭐ | 독특한 디자인 |
| **웨어러블** | 璀璨·AI 吊坠 (AI Pendant) | $25-35 | ⭐⭐ | 착용 가능 |
| **고급** | SenseCAP Watcher | $80-100 | ⭐⭐⭐⭐ | 카메라 비전 |
| **로봇** | ESP-HI 로봇 강아지 | $50-70 | ⭐⭐⭐⭐ | 4족 보행 |

### 🌱 초급: 완제품 개발보드 (바로 시작 가능)

#### 추천 1: M5Stack CoreS3
- **가격**: 약 $40-50
- **구매처**: [M5Stack 공식](https://docs.m5stack.com/zh_CN/core/CoreS3)
- **장점**:
  - ✅ 마이크, 스피커, 2.0인치 터치스크린 내장
  - ✅ USB-C로 바로 연결 가능
  - ✅ 케이스와 배터리 포함
  - ✅ 확장 포트로 센서 추가 가능
- **단점**:
  - ❌ 가격이 다른 보드 대비 높음
  - ❌ 커스터마이징 제한적
- **추천 대상**: "일단 만들어보고 싶어!" 하는 입문자
- **펌웨어**: `m5cores3` 타입 선택

#### 추천 2: ESP32-S3-BOX3 (Espressif 공식)
- **가격**: 약 $25-30
- **구매처**: [Espressif 공식](https://github.com/espressif/esp-box)
- **장점**:
  - ✅ Espressif 공식 제품, 문서 풍부
  - ✅ 2.4인치 터치스크린
  - ✅ 가성비 최고
  - ✅ 공식 지원으로 안정성 우수
- **단점**:
  - ❌ 스피커 품질이 평범함
  - ❌ 배터리 미포함
- **추천 대상**: "저렴하게 실험하고 싶어!" 하는 학생
- **펌웨어**: `esp-box-3` 타입 선택

#### 추천 3: 立创实战派 ESP32-S3 (중국 학습자용 최고 가성비)
- **가격**: 약 $15-20
- **구매처**: [立创商城](https://oshwhub.com/li-chuang-kai-fa-ban/li-chuang-shi-zhan-pai-esp32-s3-kai-fa-ban)
- **장점**:
  - ✅ 초저가 진입
  - ✅ 중국어 튜토리얼 풍부
  - ✅ 확장성 우수
- **단점**:
  - ❌ 별도 부품 연결 필요
- **추천 대상**: 중국 커뮤니티 활용하는 학습자
- **펌웨어**: `lichuang-s3` 타입 선택

#### 🌿 중급: 브레드보드 DIY (학습에 최적)

**필요한 부품 리스트**:

| 부품명 | 개수 | 예상 가격 | 구매처 |
|--------|------|-----------|---------|
| ESP32-S3-DevKitC | 1개 | $8 | AliExpress |
| MEMS 마이크 (SPH0645) | 1개 | $3 | AliExpress |
| I2S 앰프 (MAX98357A) | 1개 | $2 | AliExpress |
| 3W 스피커 | 1개 | $1 | AliExpress |
| OLED 128×64 (I2C) | 1개 | $3 | AliExpress |
| 브레드보드 | 1개 | $2 | AliExpress |
| 점퍼 와이어 세트 | 1세트 | $2 | AliExpress |
| USB-C 케이블 | 1개 | $2 | 다이소 |
| **총합** | - | **약 $23** | - |

**회로도 (초보자용 단순화)**:

```
ESP32-S3-DevKitC
┌─────────────────────────────────────┐
│                                       │
│  GPIO 1 ───────────── 마이크 DATA    │
│  GPIO 2 ───────────── 마이크 CLK     │
│                                       │
│  GPIO 12 ──────────── 앰프 DIN       │
│  GPIO 13 ──────────── 앰프 BCLK      │
│  GPIO 14 ──────────── 앰프 LRC       │
│                                       │
│  GPIO 21 ──────────── OLED SDA       │
│  GPIO 22 ──────────── OLED SCL       │
│                                       │
│  3.3V ────────────── 모든 부품 VCC   │
│  GND ─────────────── 모든 부품 GND   │
└─────────────────────────────────────┘
```

**배선 팁**:
1. 🔴 **빨간 선**: 전원(3.3V)에 연결
2. ⚫ **검은 선**: 접지(GND)에 연결
3. 🔵 **파란/기타**: 신호선 (GPIO)

#### 🌳 고급: PCB 제작 (나만의 제품 만들기)

**추천 제작 서비스**:
- **JLCPCB**: 중국, 저렴함 ($2부터)
- **PCBWay**: 중국, 품질 좋음 ($5부터)
- **입체회로**: 한국, 빠름 (₩10,000부터)

**PCB 설계 도구**:
- **KiCad**: 무료, 오픈소스
- **EasyEDA**: 웹 기반, 초보자 친화적
- **Altium Designer**: 전문가용 (비쌈)

---

## 소프트웨어 개발 환경 준비

### 🖥️ Step 1: 필수 프로그램 설치

#### Windows 사용자

**1-1. VSCode 설치**
1. https://code.visualstudio.com/ 접속
2. "Download for Windows" 클릭
3. 다운로드한 파일 실행
4. 쭉 "다음" 클릭하면 끝!

**1-2. ESP-IDF 플러그인 설치**
1. VSCode 실행
2. 왼쪽 메뉴에서 확장 프로그램 아이콘 클릭 (네모 4개 모양)
3. 검색창에 "ESP-IDF" 입력
4. "ESP-IDF" (Espressif Systems 제작) 설치
5. 설치 완료 후 팝업창에서 "Express 설치" 선택
6. ESP-IDF 5.4 버전 선택
7. 설치 완료까지 10-20분 대기 (☕ 커피 한잔!)

#### macOS 사용자

**터미널에서 한 줄 명령어로 설치**:

```bash
# Homebrew 설치 (이미 있으면 건너뛰기)
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"

# 필요한 도구 설치
brew install cmake ninja dfu-util python3

# ESP-IDF 설치
mkdir -p ~/esp
cd ~/esp
git clone -b v5.4 --recursive https://github.com/espressif/esp-idf.git
cd esp-idf
./install.sh esp32s3

# 환경 변수 설정
echo "alias get_idf='. $HOME/esp/esp-idf/export.sh'" >> ~/.zshrc
source ~/.zshrc
```

#### Linux 사용자 (Ubuntu 기준)

```bash
# 필수 패키지 설치
sudo apt-get update
sudo apt-get install git wget flex bison gperf python3 python3-pip python3-venv cmake ninja-build ccache libffi-dev libssl-dev dfu-util libusb-1.0-0

# ESP-IDF 설치
mkdir -p ~/esp
cd ~/esp
git clone -b v5.4 --recursive https://github.com/espressif/esp-idf.git
cd esp-idf
./install.sh esp32s3

# 환경 변수 설정
echo "alias get_idf='. $HOME/esp/esp-idf/export.sh'" >> ~/.bashrc
source ~/.bashrc
```

### 🚀 Step 2: 프로젝트 다운로드 및 빌드

#### 초보자용: 웹 플래시 도구 사용 (코딩 불필요!)

1. **펌웨어 다운로드 페이지 접속**:
   - https://github.com/78/xiaozhi-esp32/releases
   - 최신 버전의 `xiaozhi-esp32-firmware.zip` 다운로드

2. **웹 플래시 도구 접속**:
   - https://espressif.github.io/esp-launchpad/
   - USB로 ESP32 보드 연결
   - "Connect" 버튼 클릭
   - 다운받은 펌웨어 파일 선택
   - "Program" 클릭!

3. **Wi-Fi 설정**:
   - 시리얼 모니터에서 Wi-Fi 설정 지시 따라하기
   - 또는 스마트폰 앱으로 설정 가능

**완료! 🎉 이제 "샤오즈"라고 불러보세요!**

#### 중급자용: 소스코드에서 빌드하기

```bash
# 1. 프로젝트 클론
git clone https://github.com/78/xiaozhi-esp32.git
cd xiaozhi-esp32

# 2. ESP-IDF 환경 활성화
get_idf

# 3. 프로젝트 설정
idf.py set-target esp32s3
idf.py menuconfig
# → "Xiaozhi Configuration" 메뉴에서 설정 변경 가능

# 4. 빌드
idf.py build

# 5. 플래시 및 모니터링
idf.py -p /dev/ttyUSB0 flash monitor
# Windows: -p COM3 (포트 번호는 장치 관리자에서 확인)
# macOS: -p /dev/tty.usbserial-*
```

**빌드 소요 시간**:
- 첫 번째: 10-15분 (모든 라이브러리 컴파일)
- 이후: 1-2분 (변경된 파일만 재컴파일)

---

## 단계별 구현 가이드

### 🎯 Phase 1: Hello World - LED 깜빡이기 (30분)

**목표**: ESP32 보드가 정상 작동하는지 확인

**코드 (초보자용 주석 버전)**:

```c
// 필요한 라이브러리 불러오기 (책에서 페이지 펼치는 것과 같음)
#include "freertos/FreeRTOS.h"  // 운영체제 같은 것
#include "freertos/task.h"       // 작업(Task)을 만들 수 있게 해줌
#include "driver/gpio.h"         // GPIO 핀을 제어할 수 있게 해줌

// LED가 연결된 핀 번호 정의 (보드마다 다를 수 있음!)
#define LED_PIN 2  // GPIO 2번에 LED가 있다는 뜻

// 프로그램 시작점 (main 함수)
void app_main(void)
{
    // LED 핀을 "출력 모드"로 설정 (빛을 내보내는 모드)
    gpio_set_direction(LED_PIN, GPIO_MODE_OUTPUT);

    // 무한 반복 (계속계속 실행)
    while (1) {
        // LED 켜기 (HIGH = 높은 전압 = 불 켜짐)
        gpio_set_level(LED_PIN, 1);
        printf("LED ON!\n");  // 화면에 메시지 출력

        // 1초 대기 (1000 밀리초 = 1초)
        vTaskDelay(1000 / portTICK_PERIOD_MS);

        // LED 끄기 (LOW = 낮은 전압 = 불 꺼짐)
        gpio_set_level(LED_PIN, 0);
        printf("LED OFF!\n");

        // 1초 대기
        vTaskDelay(1000 / portTICK_PERIOD_MS);
    }
}
```

**실행 방법**:
1. 위 코드를 `main/blink.c`에 저장
2. `idf.py build flash monitor` 실행
3. LED가 깜빡이면 성공! 🎉

**문제 해결**:
- LED가 안 깜빡여요? → LED_PIN 번호가 맞는지 확인 (보드 핀맵 참조)
- 에러가 나요? → USB 케이블이 제대로 연결되었는지 확인

---

### 🎤 Phase 2: 음성 입력 받기 (2시간)

**목표**: 마이크로 소리를 녹음해서 파일로 저장

**필요한 개념 이해**:

#### 🎓 I2S 프로토콜이 뭐예요?

**파인만식 설명**:
> 악기에서 나오는 소리를 악보로 적는다고 상상해보세요.
> - **I2S = Inter-IC Sound** = 칩과 칩 사이에 소리 데이터를 주고받는 방법
> - 마치 "도레미파솔"을 숫자로 바꿔서 보내는 거예요!
>
> **세 개의 선으로 통신**:
> 1. **BCLK (비트 클락)**: "박자" - 딱딱딱 소리 데이터 타이밍
> 2. **WS (워드 선택)**: "왼쪽? 오른쪽?" - 스테레오 구분
> 3. **SD (시리얼 데이터)**: "실제 소리 숫자" - 소리 정보 전송

**실전 코드**:

```c
#include "driver/i2s.h"

// I2S 설정 구조체 (마이크 설정 명세서)
i2s_config_t i2s_config = {
    .mode = I2S_MODE_MASTER | I2S_MODE_RX,  // 주인 모드, 받기 전용
    .sample_rate = 16000,                    // 16kHz (전화 품질)
    .bits_per_sample = I2S_BITS_PER_SAMPLE_32BIT,  // 32비트 정밀도
    .channel_format = I2S_CHANNEL_FMT_ONLY_LEFT,   // 모노 (한쪽만)
    .communication_format = I2S_COMM_FORMAT_I2S,
    .dma_buf_count = 8,                      // DMA 버퍼 개수
    .dma_buf_len = 1024,                     // 버퍼 크기
};

// 핀 설정 (하드웨어 연결 정보)
i2s_pin_config_t pin_config = {
    .bck_io_num = 2,     // BCLK → GPIO 2
    .ws_io_num = 3,      // WS → GPIO 3
    .data_in_num = 1,    // DATA → GPIO 1
    .data_out_num = -1,  // 출력 없음 (마이크만 사용)
};

void init_microphone() {
    // I2S 드라이버 설치
    i2s_driver_install(I2S_NUM_0, &i2s_config, 0, NULL);
    i2s_set_pin(I2S_NUM_0, &pin_config);

    printf("마이크 초기화 완료!\n");
}

void record_audio() {
    const int RECORD_TIME_SECONDS = 5;  // 5초 녹음
    const int SAMPLE_RATE = 16000;
    const int BUFFER_SIZE = 1024;

    int16_t *buffer = malloc(BUFFER_SIZE * sizeof(int16_t));
    size_t bytes_read = 0;

    printf("녹음 시작! (5초간 말해주세요)\n");

    for (int i = 0; i < RECORD_TIME_SECONDS * SAMPLE_RATE / BUFFER_SIZE; i++) {
        // 마이크에서 데이터 읽기
        i2s_read(I2S_NUM_0, buffer, BUFFER_SIZE * sizeof(int16_t),
                 &bytes_read, portMAX_DELAY);

        // 여기서 buffer를 파일에 저장하거나 전송
        // (실제로는 OPUS로 압축해서 보냄)
    }

    printf("녹음 완료!\n");
    free(buffer);
}
```

**이해를 돕는 비유**:
- `sample_rate = 16000`: 1초에 16,000번 "스냅샷" 찍기 (카메라 프레임처럼)
- `bits_per_sample = 32`: 각 스냅샷을 32비트 숫자로 표현 (해상도)
- `dma_buf`: 고속도로 톨게이트 같은 중간 저장소 (효율적 전송)

---

### 🧠 Phase 3: AI와 대화하기 (3시간)

**목표**: 녹음한 음성을 AI에게 보내고 답변 받기

#### WebSocket 통신 이해하기

**파인만식 설명**:
> 전화 통화를 생각해보세요.
> - **HTTP**: 편지 주고받기 (보내고 → 기다리고 → 받고)
> - **WebSocket**: 전화 통화 (실시간으로 계속 연결)
>
> AI 챗봇은 "실시간 대화"가 필요하니까 WebSocket을 써요!

**WebSocket 연결 코드**:

```c
#include "esp_websocket_client.h"

// WebSocket 이벤트 처리 함수
void websocket_event_handler(void *handler_args, esp_event_base_t base,
                              int32_t event_id, void *event_data) {
    esp_websocket_event_data_t *data = (esp_websocket_event_data_t *)event_data;

    switch (event_id) {
        case WEBSOCKET_EVENT_CONNECTED:
            printf("🎉 AI 서버 연결 완료!\n");
            // 인증 메시지 전송
            send_auth_message();
            break;

        case WEBSOCKET_EVENT_DATA:
            printf("📩 AI 답변 받음: %.*s\n", data->data_len, (char*)data->data_ptr);
            // 받은 데이터 처리 (TTS 음성 재생)
            process_ai_response(data->data_ptr, data->data_len);
            break;

        case WEBSOCKET_EVENT_ERROR:
            printf("❌ 오류 발생!\n");
            break;
    }
}

void connect_to_ai_server() {
    esp_websocket_client_config_t ws_cfg = {
        .uri = "wss://xiaozhi.me/v1/ws",  // AI 서버 주소
        .cert_pem = NULL,  // HTTPS 인증서 (필요시)
    };

    esp_websocket_client_handle_t client = esp_websocket_client_init(&ws_cfg);
    esp_websocket_register_events(client, WEBSOCKET_EVENT_ANY,
                                   websocket_event_handler, NULL);

    esp_websocket_client_start(client);
    printf("🚀 AI 서버 연결 시도 중...\n");
}
```

**메시지 포맷 (JSON 예시)**:

```json
// 사용자 → AI 서버
{
  "type": "audio",
  "format": "opus",
  "sample_rate": 16000,
  "data": "T3B1c0hlYWQBAgA..."  // Base64 인코딩된 음성 데이터
}

// AI 서버 → 사용자
{
  "type": "response",
  "text": "안녕하세요! 무엇을 도와드릴까요?",
  "audio": "T3B1c0hlYWQBAgA...",  // TTS 음성
  "emotion": "happy"  // 이모지 표시용
}
```

---

### 🔊 Phase 4: 음성 출력하기 (1시간)

**목표**: AI의 답변을 스피커로 재생

**I2S 출력 설정**:

```c
// 스피커용 I2S 설정
i2s_config_t i2s_speaker_config = {
    .mode = I2S_MODE_MASTER | I2S_MODE_TX,  // 전송 모드
    .sample_rate = 16000,
    .bits_per_sample = I2S_BITS_PER_SAMPLE_16BIT,
    .channel_format = I2S_CHANNEL_FMT_ONLY_LEFT,
    .communication_format = I2S_COMM_FORMAT_I2S,
    .dma_buf_count = 8,
    .dma_buf_len = 1024,
};

i2s_pin_config_t speaker_pin_config = {
    .bck_io_num = 13,     // BCLK
    .ws_io_num = 14,      // LRCLK
    .data_out_num = 12,   // DOUT
    .data_in_num = -1,    // 입력 없음
};

void init_speaker() {
    i2s_driver_install(I2S_NUM_1, &i2s_speaker_config, 0, NULL);
    i2s_set_pin(I2S_NUM_1, &speaker_pin_config);
    printf("스피커 초기화 완료!\n");
}

void play_audio(uint8_t *audio_data, size_t length) {
    size_t bytes_written = 0;

    // 오디오 데이터를 스피커로 전송
    i2s_write(I2S_NUM_1, audio_data, length, &bytes_written, portMAX_DELAY);

    printf("재생 완료: %d 바이트\n", bytes_written);
}
```

---

## 실전 예제: 3가지 난이도별

### 🌱 초급 예제: 음성 타이머

**기능**: "3분 타이머 시작"이라고 말하면 3분 후 알람

```c
#include <string.h>

void process_timer_command(char *text) {
    int minutes = 0;

    // "3분" 같은 단어에서 숫자 추출
    if (strstr(text, "분") != NULL) {
        sscanf(text, "%d분", &minutes);

        if (minutes > 0) {
            printf("⏰ %d분 타이머 시작!\n", minutes);

            // 타이머 Task 생성
            xTaskCreate(timer_task, "timer", 2048, (void*)minutes, 5, NULL);
        }
    }
}

void timer_task(void *param) {
    int minutes = (int)param;

    // 분을 밀리초로 변환
    int delay_ms = minutes * 60 * 1000;

    vTaskDelay(delay_ms / portTICK_PERIOD_MS);

    // 알람 울리기
    printf("⏰ 타이머 완료!\n");
    play_alarm_sound();

    vTaskDelete(NULL);  // Task 종료
}
```

---

### 🌿 중급 예제: 스마트 전등 제어

**기능**: "불 켜줘" / "불 꺼줘" 음성 명령으로 릴레이 제어

```c
#define RELAY_PIN 4  // 릴레이 모듈 연결 핀

typedef struct {
    char *keyword;
    int light_state;
} LightCommand;

LightCommand commands[] = {
    {"불 켜", 1},
    {"전등 켜", 1},
    {"라이트 온", 1},
    {"불 꺼", 0},
    {"전등 꺼", 0},
    {"라이트 오프", 0},
};

void init_smart_light() {
    gpio_set_direction(RELAY_PIN, GPIO_MODE_OUTPUT);
    gpio_set_level(RELAY_PIN, 0);  // 초기: 꺼짐
}

void process_light_command(char *text) {
    int num_commands = sizeof(commands) / sizeof(LightCommand);

    for (int i = 0; i < num_commands; i++) {
        if (strstr(text, commands[i].keyword) != NULL) {
            // 명령 실행
            gpio_set_level(RELAY_PIN, commands[i].light_state);

            if (commands[i].light_state == 1) {
                printf("💡 전등 켜졌습니다!\n");
            } else {
                printf("🌙 전등 꺼졌습니다!\n");
            }
            return;
        }
    }

    printf("❓ 전등 명령을 이해하지 못했어요.\n");
}
```

**실제 회로 연결**:
```
ESP32 GPIO 4 → 릴레이 모듈 IN
릴레이 COM → 전등 Live 선
릴레이 NO → 콘센트 Live
```

⚠️ **안전 주의사항**:
- 220V 전기는 위험합니다!
- 전기 공사는 전문가에게 맡기세요
- 초보자는 LED 전등으로 테스트하세요

---

### 🌳 고급 예제: 화자 인식 시스템

**기능**: 여러 사람 목소리를 구별하여 맞춤형 응답

```c
#include "esp_http_client.h"

typedef struct {
    int user_id;
    char name[32];
    float voice_embedding[128];  // 음성 특징 벡터
} VoiceProfile;

VoiceProfile users[5];  // 최대 5명
int user_count = 0;

// 3D-Speaker 모델로 화자 임베딩 추출
float* extract_voice_embedding(int16_t *audio, size_t length) {
    // 실제로는 클라우드 API 호출 또는 온디바이스 모델 실행
    // 여기서는 간략화

    esp_http_client_config_t config = {
        .url = "https://xiaozhi.me/api/speaker/embed",
        .method = HTTP_METHOD_POST,
    };

    esp_http_client_handle_t client = esp_http_client_init(&config);

    // 오디오 데이터 전송 (생략)

    float *embedding = malloc(128 * sizeof(float));
    // 응답 파싱 (생략)

    return embedding;
}

// 코사인 유사도 계산
float cosine_similarity(float *a, float *b, int dim) {
    float dot = 0.0, norm_a = 0.0, norm_b = 0.0;

    for (int i = 0; i < dim; i++) {
        dot += a[i] * b[i];
        norm_a += a[i] * a[i];
        norm_b += b[i] * b[i];
    }

    return dot / (sqrt(norm_a) * sqrt(norm_b));
}

// 화자 인식
int identify_speaker(float *embedding) {
    float max_similarity = -1.0;
    int best_match = -1;

    for (int i = 0; i < user_count; i++) {
        float sim = cosine_similarity(embedding, users[i].voice_embedding, 128);

        if (sim > max_similarity) {
            max_similarity = sim;
            best_match = i;
        }
    }

    // 유사도가 0.8 이상이면 인식 성공
    if (max_similarity > 0.8) {
        printf("👤 %s님 인식됨! (유사도: %.2f)\n",
               users[best_match].name, max_similarity);
        return best_match;
    } else {
        printf("❓ 등록되지 않은 사용자\n");
        return -1;
    }
}

// 사용 예시
void handle_voice_command(int16_t *audio, size_t length) {
    float *embedding = extract_voice_embedding(audio, length);
    int user_id = identify_speaker(embedding);

    if (user_id >= 0) {
        // 개인화된 응답
        char *greeting = malloc(128);
        sprintf(greeting, "%s님, 어떻게 도와드릴까요?", users[user_id].name);
        speak_text(greeting);
        free(greeting);
    } else {
        speak_text("안녕하세요! 누구신가요?");
    }

    free(embedding);
}
```

---

## 문제 해결 및 디버깅

### 🐛 자주 발생하는 문제 TOP 10

#### 1. "포트를 찾을 수 없습니다" (Port not found)

**증상**:
```
Error: could not open port 'COM3': PermissionError
```

**원인과 해결**:
| 원인 | 해결 방법 |
|------|-----------|
| USB 드라이버 미설치 | CP210x 드라이버 설치 (Silicon Labs 홈페이지) |
| 다른 프로그램이 포트 사용 중 | 시리얼 모니터 모두 닫기 |
| 불량 케이블 | 데이터 전송 가능한 케이블로 교체 (충전 전용 케이블 안 됨!) |

**빠른 진단**:
```bash
# Windows
mode  # 사용 가능한 포트 목록

# macOS/Linux
ls /dev/tty.*
```

---

#### 2. "마이크가 소리를 안 잡아요"

**체크리스트**:
- [ ] 마이크 VCC가 3.3V에 연결되었나요? (5V 아님!)
- [ ] I2S 핀 번호가 코드와 일치하나요?
- [ ] 마이크에 불량이 없나요? (다른 마이크로 테스트)
- [ ] `sample_rate` 설정이 올바른가요?

**디버깅 코드**:
```c
void test_microphone() {
    int16_t buffer[1024];
    size_t bytes_read = 0;

    for (int i = 0; i < 10; i++) {
        i2s_read(I2S_NUM_0, buffer, sizeof(buffer), &bytes_read, portMAX_DELAY);

        // 진폭 계산 (얼마나 큰 소리인지)
        int32_t sum = 0;
        for (int j = 0; j < 1024; j++) {
            sum += abs(buffer[j]);
        }
        int32_t avg = sum / 1024;

        printf("진폭: %d (조용: <100, 보통: 100-500, 큼: >500)\n", avg);
        vTaskDelay(100 / portTICK_PERIOD_MS);
    }
}
```

---

#### 3. "스피커에서 잡음만 나요"

**원인**:
1. **샘플레이트 불일치**: TTS가 보낸 16kHz 오디오를 8kHz로 재생
2. **비트 깊이 불일치**: 16비트 데이터를 32비트로 해석
3. **접지(GND) 불량**: 노이즈 발생

**해결**:
```c
// TTS 응답 헤더 파싱
typedef struct {
    uint32_t sample_rate;
    uint16_t bits_per_sample;
    uint16_t channels;
} AudioHeader;

void play_tts_audio(uint8_t *data, size_t length) {
    AudioHeader *header = (AudioHeader*)data;

    // I2S 재설정
    i2s_set_sample_rates(I2S_NUM_1, header->sample_rate);

    // 실제 오디오 데이터는 헤더 다음부터
    uint8_t *audio = data + sizeof(AudioHeader);
    size_t audio_len = length - sizeof(AudioHeader);

    i2s_write(I2S_NUM_1, audio, audio_len, NULL, portMAX_DELAY);
}
```

---

#### 4. "Wi-Fi 연결이 안 돼요"

**단계별 진단**:

```c
void diagnose_wifi() {
    wifi_ap_record_t ap_info;
    esp_err_t ret = esp_wifi_sta_get_ap_info(&ap_info);

    if (ret == ESP_OK) {
        printf("연결됨! SSID: %s\n", ap_info.ssid);
        printf("신호 강도: %d dBm\n", ap_info.rssi);

        if (ap_info.rssi < -80) {
            printf("⚠️ 신호가 약합니다. 라우터 가까이 가보세요!\n");
        }
    } else if (ret == ESP_ERR_WIFI_NOT_CONNECT) {
        printf("❌ Wi-Fi 연결 실패\n");
        printf("가능한 원인:\n");
        printf("1. SSID 오타\n");
        printf("2. 비밀번호 오류\n");
        printf("3. 2.4GHz가 아닌 5GHz 대역 (ESP32는 2.4GHz만 지원)\n");
        printf("4. 공유기가 너무 멀리 있음\n");
    }
}
```

---

#### 5. "메모리 부족 에러" (Out of Memory)

**증상**:
```
E (1234) heap: alloc failed, size 32768
```

**원인**: ESP32-S3는 RAM이 제한적 (512KB 내외)

**해결 전략**:

1. **PSRAM 활성화**:
```bash
idf.py menuconfig
→ Component config → ESP32S3-Specific → Support for external, SPI-connected RAM
→ [*] Enable
```

2. **메모리 사용 최적화**:
```c
// ❌ 나쁜 예: 큰 배열 스택에 할당
void bad_example() {
    uint8_t huge_buffer[100000];  // 스택 오버플로우!
}

// ✅ 좋은 예: 힙에 동적 할당
void good_example() {
    uint8_t *buffer = heap_caps_malloc(100000, MALLOC_CAP_SPIRAM);
    if (buffer == NULL) {
        printf("메모리 부족!\n");
        return;
    }

    // 사용 후 반드시 해제!
    free(buffer);
}
```

3. **메모리 사용량 모니터링**:
```c
void print_memory_info() {
    printf("=== 메모리 상태 ===\n");
    printf("남은 힙: %d bytes\n", esp_get_free_heap_size());
    printf("최소 힙: %d bytes\n", esp_get_minimum_free_heap_size());
    printf("PSRAM: %d bytes\n", heap_caps_get_free_size(MALLOC_CAP_SPIRAM));
}
```

---

### 🔍 시리얼 모니터 활용법

**레벨 1: 기본 로그 읽기**
```c
// 간단한 디버깅
printf("현재 상태: %d\n", state);

// 더 자세한 로그 (파일:라인 번호 포함)
ESP_LOGI("TAG", "값: %d", value);  // [INFO]
ESP_LOGW("TAG", "경고!");           // [WARN]
ESP_LOGE("TAG", "에러 발생!");      // [ERROR]
```

**레벨 2: 로그 레벨 조정**
```bash
# 빌드 시 로그 레벨 설정
idf.py menuconfig
→ Component config → Log output
→ Default log verbosity: Debug
```

**레벨 3: 고급 필터링**
```bash
# 특정 태그만 보기
idf.py monitor --print-filter "AUDIO:I WIFI:W"

# 정규식 매칭
idf.py monitor --print-filter "*:E"  # 에러만 표시
```

---

## 확장 및 응용 아이디어

### 💡 프로젝트 아이디어 10선

#### 🏠 스마트홈 연동
1. **HomeKit 통합**: iPhone Siri로 제어
2. **MQTT 브릿지**: Home Assistant 연동
3. **Zigbee 게이트웨이**: 스마트 전구 제어

#### 🎮 재미있는 프로젝트
4. **AI 친구 로봇**: 서보모터로 움직이는 귀여운 로봇
5. **음성 게임**: "가위바위보" 같은 음성 게임
6. **노래방 점수 시스템**: 음정 분석

#### 🎓 교육용
7. **언어 학습 도우미**: 발음 교정 피드백
8. **스토리텔링 로봇**: 동화책 읽어주기
9. **퀴즈 챗봇**: 음성으로 문제 내고 채점

#### 🚗 IoT 응용
10. **차량용 보조 시스템**: 운전 중 핸즈프리 제어

---

### 🔌 MCP 프로토콜 심화 학습

#### MCP란 무엇인가? (JSON-RPC 2.0 기반)

**MCP (Model Context Protocol)**은 AI 모델과 IoT 기기 간 상호작용을 표준화한 프로토콜입니다.

**핵심 개념**:
```json
{
  "session_id": "device-unique-id",
  "type": "mcp",
  "payload": {
    "jsonrpc": "2.0",
    "method": "tools/call",
    "params": {
      "name": "set_led",
      "arguments": {
        "brightness": 80,
        "color": "blue"
      }
    },
    "id": 123
  }
}
```

#### MCP 통신 흐름 (Device-side)

```
1️⃣ 초기화 (Initialization)
   Device → Server: "hello" 메시지 + 기기 정보
   ├─ 기기 ID, 모델명
   ├─ 지원 기능 목록
   └─ 오디오 설정 (16kHz, OPUS 등)

2️⃣ 세션 생성
   Server → Device: "initialize" 요청
   Device → Server: 세션 정보 응답

3️⃣ 도구 목록 발견 (Tool Discovery)
   Server → Device: "tools/list" 요청
   Device → Server: 사용 가능한 도구 목록
   ├─ set_volume (볼륨 제어)
   ├─ set_led (LED 제어)
   ├─ gpio_control (GPIO 제어)
   └─ play_sound (소리 재생)

4️⃣ 도구 실행 (Tool Invocation)
   User: "불 켜줘" (음성)
   ↓
   AI: "불 켜기 명령 인식" → MCP 호출
   ↓
   Server → Device: "tools/call" 메시지
   {
     "name": "gpio_control",
     "arguments": {"pin": 4, "state": "HIGH"}
   }
   ↓
   Device: GPIO 4번 HIGH 실행
   ↓
   Device → Server: 성공 응답

5️⃣ 알림 (Notifications, 선택사항)
   Device → Server: "notifications/button_pressed"
   - 기기에서 이벤트 발생 시 서버에 알림
```

#### Device-side MCP Tool 예제

**1. 볼륨 제어**
```c
// main/mcp_tools/volume_tool.c
#include "mcp_tool.h"

esp_err_t set_volume_handler(cJSON *params, cJSON **result) {
    int volume = cJSON_GetObjectItem(params, "volume")->valueint;

    if (volume < 0 || volume > 100) {
        return ESP_ERR_INVALID_ARG;
    }

    // 실제 볼륨 조절 (I2S 또는 오디오 코덱)
    audio_hal_set_volume(volume);

    // 결과 JSON 생성
    *result = cJSON_CreateObject();
    cJSON_AddStringToObject(*result, "status", "success");
    cJSON_AddNumberToObject(*result, "volume", volume);

    return ESP_OK;
}

// Tool 등록
MCP_TOOL_REGISTER(
    "set_volume",                           // Tool 이름
    "스피커 볼륨 조절",                     // 설명
    "{\"volume\": {\"type\": \"number\"}}",  // 파라미터 스키마
    set_volume_handler                      // 핸들러 함수
);
```

**2. LED 제어 (RGB)**
```c
esp_err_t set_led_handler(cJSON *params, cJSON **result) {
    int brightness = cJSON_GetObjectItem(params, "brightness")->valueint;
    const char *color = cJSON_GetObjectItem(params, "color")->valuestring;

    uint32_t rgb = 0;
    if (strcmp(color, "red") == 0) {
        rgb = 0xFF0000;
    } else if (strcmp(color, "green") == 0) {
        rgb = 0x00FF00;
    } else if (strcmp(color, "blue") == 0) {
        rgb = 0x0000FF;
    }

    // RGB LED 제어 (WS2812 등)
    led_strip_set_rgb(rgb, brightness);

    *result = cJSON_CreateObject();
    cJSON_AddStringToObject(*result, "status", "LED 설정 완료");

    return ESP_OK;
}
```

**3. 서보모터 제어**
```c
esp_err_t move_servo_handler(cJSON *params, cJSON **result) {
    int angle = cJSON_GetObjectItem(params, "angle")->valueint;

    if (angle < 0 || angle > 180) {
        return ESP_ERR_INVALID_ARG;
    }

    // LEDC PWM으로 서보 제어
    uint32_t duty = angle_to_duty(angle);
    ledc_set_duty(LEDC_HIGH_SPEED_MODE, LEDC_CHANNEL_0, duty);
    ledc_update_duty(LEDC_HIGH_SPEED_MODE, LEDC_CHANNEL_0);

    *result = cJSON_CreateObject();
    cJSON_AddNumberToObject(*result, "angle", angle);

    return ESP_OK;
}

MCP_TOOL_REGISTER(
    "move_servo",
    "서보모터 각도 조절 (0-180도)",
    "{\"angle\": {\"type\": \"number\", \"min\": 0, \"max\": 180}}",
    move_servo_handler
);
```

#### Cloud-side MCP Tool 예제

**백엔드 서버에서 실행되는 MCP Tools:**

1. **스마트홈 연동** (SmartThings, Home Assistant)
```python
# server/mcp_tools/smart_home.py
@mcp_tool("control_light")
async def control_light(device_id: str, state: str):
    """스마트 전등 제어"""
    async with aiohttp.ClientSession() as session:
        await session.post(
            f"https://api.smartthings.com/v1/devices/{device_id}/commands",
            json={"commands": [{"component": "main", "capability": "switch", "command": state}]},
            headers={"Authorization": f"Bearer {SMARTTHINGS_TOKEN}"}
        )
    return {"status": "success", "device": device_id, "state": state}
```

2. **날씨 정보 조회**
```python
@mcp_tool("get_weather")
async def get_weather(city: str):
    """날씨 정보 조회"""
    async with aiohttp.ClientSession() as session:
        response = await session.get(
            f"https://api.openweathermap.org/data/2.5/weather?q={city}&appid={API_KEY}&units=metric"
        )
        data = await response.json()

    return {
        "temperature": data["main"]["temp"],
        "humidity": data["main"]["humidity"],
        "description": data["weather"][0]["description"]
    }
```

3. **이메일 전송**
```python
@mcp_tool("send_email")
async def send_email(to: str, subject: str, body: str):
    """이메일 전송"""
    import smtplib
    from email.mime.text import MIMEText

    msg = MIMEText(body)
    msg['Subject'] = subject
    msg['To'] = to

    with smtplib.SMTP('smtp.gmail.com', 587) as server:
        server.starttls()
        server.login(GMAIL_USER, GMAIL_PASSWORD)
        server.send_message(msg)

    return {"status": "sent", "to": to}
```

#### MCP 실전 활용 시나리오

**시나리오 1: 스마트 알람**
```
사용자: "내일 아침 7시에 깨워줘"
↓
LLM: 알람 설정 필요 인식
↓
MCP Tool Call:
{
  "name": "set_alarm",
  "arguments": {
    "time": "07:00",
    "date": "2025-10-13"
  }
}
↓
ESP32: RTC 알람 설정
↓
다음 날 07:00 → 알람 울림 + "좋은 아침입니다!" TTS
```

**시나리오 2: 스마트홈 자동화**
```
사용자: "잘 준비해줘"
↓
LLM: 취침 루틴 실행
↓
병렬 MCP Tool Calls:
1. control_light("bedroom", "off")     # 침실 불 끄기
2. control_light("living_room", "off")  # 거실 불 끄기
3. set_temperature(20)                  # 온도 조절
4. play_sound("white_noise")            # 백색 소음 재생
↓
ESP32 + 스마트홈: 모든 명령 실행
```

**시나리오 3: 정보 조회 + 동작**
```
사용자: "오늘 날씨 어때? 비 오면 우산 챙기라고 알려줘"
↓
Cloud MCP: get_weather("Seoul")
→ 결과: 비 예보
↓
LLM: "오늘 서울에 비가 옵니다"
↓
Device MCP: set_reminder("우산 챙기기", time="08:00")
→ ESP32에 알림 설정
```

#### MCP Tool 디버깅

**로그 활성화**:
```c
// menuconfig에서 설정
Component config → XiaoZhi Configuration → MCP
→ [*] Enable MCP debug logs
```

**시리얼 모니터 출력**:
```
I (12345) MCP: Received tool call: set_led
I (12346) MCP: Parameters: {"brightness":80,"color":"blue"}
I (12347) LED: Setting RGB to 0x0000FF at 80% brightness
I (12350) MCP: Tool execution successful
I (12351) MCP: Sending result to server
```

---

## 용어 사전

### 🔤 알파벳 순

#### A
- **ASR (Automatic Speech Recognition)**: 자동 음성 인식. 사람의 말을 텍스트로 바꾸는 기술.
  - *쉬운 설명*: 음성을 글자로 받아쓰기하는 프로그램
  - *예시*: "안녕하세요" (음성) → "안녕하세요" (텍스트)

#### B
- **BCLK (Bit Clock)**: I2S에서 데이터 전송 타이밍을 맞추는 클락 신호.
  - *쉬운 설명*: 악보의 박자처럼 데이터를 보내는 타이밍

#### C
- **Cat.1 4G**: 4세대 LTE 통신 기술의 한 종류. IoT 기기용으로 저렴하고 전력 효율적.
  - *쉬운 설명*: 스마트폰보다 느리지만 IoT 기기에 딱 맞는 인터넷

#### D
- **DMA (Direct Memory Access)**: CPU 개입 없이 메모리 간 데이터를 직접 전송하는 기술.
  - *쉬운 설명*: CPU가 직접 나르지 않고 자동 컨베이어 벨트로 데이터 이동
  - *왜 중요한가*: 오디오처럼 대용량 데이터를 빠르게 처리할 때 필수

#### E
- **ESP-IDF (Espressif IoT Development Framework)**: ESP32 칩 프로그래밍용 공식 개발 도구.
  - *쉬운 설명*: ESP32 칩을 위한 "개발 도구 세트" (컴파일러, 라이브러리 등 포함)
- **ESP-SR (Espressif Speech Recognition)**: Espressif가 만든 오프라인 음성 인식 라이브러리.
  - *기능*: 깨우기 단어 인식 (WakeNet), 간단한 명령어 인식 (MultiNet)

#### F
- **FreeRTOS**: 임베디드 시스템용 실시간 운영체제(RTOS).
  - *쉬운 설명*: ESP32에서 여러 작업을 동시에 실행하게 해주는 "교통 정리 시스템"
  - *예시*: 마이크 녹음 + 스피커 재생 + LED 깜빡이기 동시 실행

#### G
- **GPIO (General Purpose Input/Output)**: 범용 입출력 핀.
  - *쉬운 설명*: 마치 레고의 "연결 구멍"처럼 다양한 부품을 연결할 수 있는 핀

#### I
- **I2C (Inter-Integrated Circuit)**: 두 개의 선(SDA, SCL)으로 여러 칩이 통신하는 프로토콜.
  - *쉬운 설명*: 전화 파티 라인처럼 여러 기기가 두 개의 선으로 대화
  - *용도*: OLED, 센서 등 저속 장치
- **I2S (Inter-IC Sound)**: 오디오 데이터 전송 전용 프로토콜.
  - *I2C와 차이*: I2C는 저속 범용, I2S는 고속 오디오 전용

#### L
- **LLM (Large Language Model)**: 대규모 언어 모델. GPT, Qwen 같은 AI.
  - *쉬운 설명*: 수천억 개의 단어로 학습한 초똑똑한 AI 두뇌

#### M
- **MCP (Model Context Protocol)**: AI 모델이 외부 도구를 사용할 수 있게 하는 프로토콜.
  - *쉬운 설명*: AI에게 "전등 켜줘" 도구, "이메일 보내줘" 도구처럼 기능을 추가하는 방법
  - *비유*: 스마트폰에 앱을 설치하는 것과 비슷
- **MEMS 마이크 (Micro-Electro-Mechanical Systems)**: 초소형 반도체 마이크.
  - *장점*: 작고, 싸고, 전력 적게 먹음

#### O
- **OLED (Organic Light-Emitting Diode)**: 자체 발광하는 디스플레이.
  - *LCD와 차이*: 백라이트 불필요, 검은색이 진짜 검정
- **OPUS**: 고품질 저대역폭 오디오 코덱.
  - *쉬운 설명*: 음성을 압축해서 작은 크기로 만들지만 품질은 좋게 유지

#### P
- **PCM (Pulse Code Modulation)**: 압축하지 않은 원본 오디오 포맷.
  - *쉬운 설명*: 마이크가 녹음한 "날것" 그대로의 소리
  - *OPUS와 차이*: PCM은 원본(큼), OPUS는 압축본(작음)
- **PSRAM (Pseudo Static RAM)**: ESP32에 추가로 연결할 수 있는 외부 RAM.
  - *용도*: 메모리 부족할 때 확장 (최대 8MB)

#### T
- **TTS (Text-To-Speech)**: 텍스트를 음성으로 변환.
  - *쉬운 설명*: 글자를 읽어주는 "낭독 프로그램"
  - *예시*: "안녕하세요" (텍스트) → 음성 재생

#### W
- **WebSocket**: 실시간 양방향 통신 프로토콜.
  - *HTTP와 차이*: HTTP는 요청→응답 반복, WebSocket은 한 번 연결 후 계속 통신
  - *용도*: 채팅, 실시간 알림, 음성 스트리밍

---

### 📊 개념별 분류

#### 하드웨어 관련
- ESP32-S3, GPIO, I2C, I2S, MEMS, OLED, PSRAM

#### 소프트웨어 관련
- ESP-IDF, FreeRTOS, DMA, PCM, OPUS

#### 네트워크 관련
- Wi-Fi, Cat.1 4G, WebSocket, MQTT

#### AI 관련
- ASR, TTS, LLM, MCP

---

## 🎓 학습 로드맵

### Week 1: 기초 다지기
- [ ] ESP32 보드 구매 및 개발 환경 설치
- [ ] LED 깜빡이기 (Hello World)
- [ ] 버튼 입력 읽기
- [ ] Wi-Fi 연결 테스트

### Week 2: 오디오 입출력
- [ ] 마이크 연결 및 녹음
- [ ] 스피커 연결 및 재생
- [ ] WAV 파일 재생
- [ ] OPUS 코덱 실습

### Week 3: 네트워크 통신
- [ ] HTTP 요청 보내기
- [ ] WebSocket 연결
- [ ] JSON 파싱
- [ ] 클라우드 API 호출

### Week 4: AI 챗봇 완성
- [ ] ASR 연동
- [ ] LLM 대화
- [ ] TTS 음성 재생
- [ ] 전체 파이프라인 통합

### Week 5-6: 고급 기능
- [ ] 화자 인식
- [ ] MCP 도구 추가
- [ ] 스마트홈 제어
- [ ] 나만의 프로젝트!

---

## 커뮤니티 및 학습 자료

### 🌐 공식 커뮤니티

#### 중국 커뮤니티 (주 커뮤니티)
- **QQ 그룹**: 1011329060 (1만+ 회원)
  - 실시간 기술 지원
  - 하드웨어 구매 정보 공유
  - 프로젝트 쇼케이스
- **Bilibili**: XiaoZhi 공식 채널
  - 튜토리얼 영상
  - 라이브 코딩 세션
  - 사용자 제작 콘텐츠

#### 글로벌 커뮤니티
- **GitHub Issues**: [버그 리포트 및 기능 요청](https://github.com/78/xiaozhi-esp32/issues)
- **GitHub Discussions**: 일반 토론 및 Q&A
- **Adafruit Blog**: [영문 소개 기사](https://blog.adafruit.com/2025/06/09/xiaozhi-esp32-is-an-mcp-based-chatbot/)
- **HelloGitHub**: [프로젝트 큐레이션](https://hellogithub.com/en/repository/78/xiaozhi-esp32)

### 📚 학습 자료

#### 공식 문서
- **XiaoZhi 프로젝트 위키**: [飞书文档](https://ccnphfhqs21z.feishu.cn/wiki/F5krwD16viZoF0kKkvDcrZNYnhb)
  - 하드웨어 선택 가이드
  - 펌웨어 플래시 방법
  - 문제 해결 FAQ
- **개발자 문서**:
  - [MCP 프로토콜 상세](https://github.com/78/xiaozhi-esp32/blob/main/docs/mcp-protocol.md)
  - [WebSocket 통신](https://github.com/78/xiaozhi-esp32/blob/main/docs/websocket.md)
  - [커스텀 보드 제작](https://github.com/78/xiaozhi-esp32/blob/main/docs/custom-board.md)
  - [MCP 사용 가이드](https://github.com/78/xiaozhi-esp32/blob/main/docs/mcp-usage.md)
- **ESP-IDF 공식**: [프로그래밍 가이드](https://docs.espressif.com/projects/esp-idf/en/latest/)

#### 커뮤니티 튜토리얼
- **CSDN 블로그**: [ESP32 小智 AI 입문 튜토리얼](https://blog.csdn.net/h050210/article/details/146120433)
  - 원리부터 실전까지
  - 클라우드 자체 배포 가이드
- **DF创客社区**: [ESP32-S3 AI CAM 이식 튜토리얼](https://mc.dfrobot.com.cn/thread-326416-1-1.html)
- **知乎**: [XiaoZhi 프로젝트 심층 분석](https://zhuanlan.zhihu.com/p/17299115609)
- **kingcos 블로그**: [小智 기술 해부](https://kingcos.me/posts/2025/dive_in_xiaozhi_1/)

#### 동영상 강좌
- **B站 공식 채널**:
  - [AI 여자친구 DIY 초보 가이드](https://www.bilibili.com/video/BV1XnmFYLEJN/)
  - [AI 카메라 데모](https://www.bilibili.com/video/BV1bpjgzKEhd/)
  - [백엔드 콘솔 설정](https://www.bilibili.com/video/BV1jUCUY2EKM/)
- **커뮤니티 제작**:
  - ESP32 펌웨어 플래시 방법
  - 브레드보드 DIY 실전
  - 스마트홈 연동 사례

### 🔧 관련 오픈소스 프로젝트

#### 서버 구현체
다양한 언어로 구현된 자체 호스팅 서버:

1. **Python 서버**: [xinnan-tech/xiaozhi-esp32-server](https://github.com/xinnan-tech/xiaozhi-esp32-server)
   - 가장 많이 사용됨
   - Docker 지원
   - 풍부한 문서

2. **Java 서버**: [joey-zhou/xiaozhi-esp32-server-java](https://github.com/joey-zhou/xiaozhi-esp32-server-java)
   - Spring Boot 기반
   - 엔터프라이즈 환경

3. **Go 서버**: [AnimeAIChat/xiaozhi-server-go](https://github.com/AnimeAIChat/xiaozhi-server-go)
   - 고성능
   - 저메모리

#### 클라이언트 포팅
- **Python 클라이언트**: [huangjunsen0406/py-xiaozhi](https://github.com/huangjunsen0406/py-xiaozhi)
  - PC에서 테스트
  - 프로토타이핑용
- **Android 앱**: [TOM88812/xiaozhi-android-client](https://github.com/TOM88812/xiaozhi-android-client)
  - 스마트폰에서 사용
- **Linux 클라이언트**: [100askTeam/xiaozhi-linux](http://github.com/100askTeam/xiaozhi-linux)
  - 라즈베리파이 등
- **SF32 블루투스**: [78/xiaozhi-sf32](https://github.com/78/xiaozhi-sf32)
  - 블루투스 칩 펌웨어
- **QuecPython**: [QuecPython/solution-xiaozhiAI](https://github.com/QuecPython/solution-xiaozhiAI)
  - Quectel 모듈용

#### 커스터마이징 도구
- **Assets Generator**: [78/xiaozhi-assets-generator](https://github.com/78/xiaozhi-assets-generator)
  - 웹 기반 에디터
  - 깨우기 단어 커스텀
  - 폰트, 이모지, 배경 변경
  - 온라인 바로 적용

### 📖 추천 도서

#### 한국어 도서
- 《ESP32로 시작하는 사물인터넷》
- 《아두이노와 ESP32로 만드는 IoT 프로젝트》
- 《임베디드 C 프로그래밍》

#### 영문 도서
- *Getting Started with ESP32* (Kolban)
- *ESP32 Development Workshop* (Agus Kurniawan)
- *IoT Projects with ESP32* (Vedat Ozan Oner)

#### 중국어 도서 (원문 자료)
- 《ESP32开发实战》
- 《乐鑫ESP32官方教程》
- 《物联网嵌入式开发》

### 💡 학습 팁

1. **초보자 경로**:
   - 공식 위키부터 시작
   - 웹 플래시로 빠른 체험
   - QQ 그룹 가입 (중국어 가능 시)
   - 커뮤니티 프로젝트 따라하기

2. **개발자 경로**:
   - GitHub 저장소 클론
   - 문서(docs/) 폴더 정독
   - 간단한 MCP Tool 추가해보기
   - 자신만의 보드 포팅

3. **문제 해결**:
   - GitHub Issues 검색 먼저
   - 에러 메시지로 Google 검색
   - QQ 그룹에 질문 (중국어)
   - StackOverflow ESP32 태그

---

## 🙏 마치며

이 가이드가 여러분의 AI 챗봇 개발 여정에 도움이 되길 바랍니다!

### 📈 다음 단계 추천

#### 초보자 (1-2주 차)
1. ✅ ESP32-S3-BOX3 또는 M5Stack CoreS3 구매
2. ✅ 웹 플래시 도구로 펌웨어 설치
3. ✅ xiaozhi.me 회원가입 및 설정
4. ✅ "샤오즈, 안녕"으로 첫 대화 성공!
5. ✅ 공식 위키 읽으며 기본 개념 익히기

#### 중급자 (3-4주 차)
1. ✅ ESP-IDF 개발 환경 구축
2. ✅ GitHub 저장소 클론하여 빌드
3. ✅ 간단한 MCP Tool 추가 (LED 제어 등)
4. ✅ 브레드보드로 커스텀 회로 구성
5. ✅ 커뮤니티 프로젝트에 기여

#### 고급자 (5-6주 차)
1. ✅ 자신만의 보드 포팅 및 PCB 설계
2. ✅ Python 서버 자체 호스팅
3. ✅ Cloud-side MCP로 스마트홈 연동
4. ✅ 화자 인식 시스템 구현
5. ✅ 완성된 프로젝트를 커뮤니티에 공유

### 🎯 성공을 위한 팁

**기억하세요**:
- 🚀 **완벽보다 완성**: 일단 작동하게 만든 후 개선하세요
- 🐛 **에러는 배움**: 에러 메시지를 두려워하지 마세요
- 🤝 **커뮤니티 활용**: QQ 그룹, GitHub Issues에서 도움받기
- 🎉 **작은 성공**: LED 하나 깜빡인 것도 축하할 일입니다
- 📚 **문서 정독**: docs/ 폴더는 보물창고입니다
- 🔍 **코드 탐험**: 다른 보드의 구현을 참고하세요

**"복잡한 것은 단순한 것들의 조합일 뿐이다"** - 리처드 파인만

### 🌟 프로젝트 쇼케이스 아이디어

여러분만의 XiaoZhi를 만들어보세요:

- 🏡 **스마트홈 허브**: 집안의 모든 IoT 기기 음성 제어
- 🤖 **반려 로봇**: 서보모터로 움직이는 귀여운 AI 친구
- 🎓 **언어 학습기**: 발음 교정 및 대화 연습
- 🚗 **차량용 AI**: 운전 중 안전한 음성 비서
- 👴 **시니어 케어**: 어르신을 위한 간편한 AI 도우미
- 🧒 **어린이 교육**: 동화 읽어주고 질문 답하는 AI
- 🏥 **건강 모니터**: 약 복용 알림 및 건강 상담
- 🎵 **음악 DJ**: 분위기에 맞는 음악 자동 재생

### 📢 커뮤니티 참여 방법

**기여 환영!**
- 🐛 버그 발견 → GitHub Issues 리포트
- 💡 기능 아이디어 → Discussions에 제안
- 📝 튜토리얼 작성 → 블로그나 YouTube 공유
- 🔧 코드 개선 → Pull Request 제출
- 🌐 번역 → 다른 언어 문서 번역
- 🎨 디자인 → 케이스 3D 모델 공유

### 🏆 프로젝트 성과 (2025년 10월 기준)

- ⭐ **GitHub Stars**: 6,000+
- 👥 **커뮤니티**: QQ 그룹 10,000+ 회원
- 🤖 **보급 대수**: 100,000+ 기기
- 🌍 **지원 언어**: 한/중/영/일 4개 언어
- 🔧 **지원 보드**: 70개 이상
- 📦 **파생 프로젝트**: 10개 이상 서버/클라이언트

### 📜 라이선스 및 저작권

**원본 프로젝트**: [XiaoZhi ESP32](https://github.com/78/xiaozhi-esp32)
- **라이선스**: MIT License
- **상업적 이용**: ✅ 가능 (크레딧 표시 권장)
- **수정 및 배포**: ✅ 자유롭게 가능
- **특허 사용**: ✅ 제한 없음

**이 가이드**:
- **원저작자**: GitHub @78 (XiaoZhi 프로젝트)
- **가이드 편집**: AI 교육자를 위한 한국어 버전
- **사용 목적**: 교육 및 학습 자료

---

## 연결된 노트
- [[ESP32 개발 환경 구축 가이드]]
- [[음성 인식 기술 깊이 이해하기]]
- [[MCP 프로토콜 상세 분석]]
- [[스마트홈 IoT 통합 가이드]]
- [[임베디드 C 프로그래밍 기초]]
- [[FreeRTOS 실시간 운영체제 입문]]
- [[I2S 오디오 프로그래밍 완벽 가이드]]

---

## 📊 학습 체크리스트

### 이론 학습 (20%)
- [ ] MCP 프로토콜 개념 이해
- [ ] ESP32 아키텍처 파악
- [ ] 음성 처리 파이프라인 (ASR→LLM→TTS)
- [ ] I2S/I2C 통신 프로토콜
- [ ] FreeRTOS 멀티태스킹

### 실습 (60%)
- [ ] 개발 환경 설치 완료
- [ ] LED 깜빡이기 성공
- [ ] 마이크 녹음 테스트
- [ ] 스피커 재생 테스트
- [ ] Wi-Fi 연결 성공
- [ ] AI 서버 연동 완료
- [ ] 음성 대화 성공
- [ ] 첫 MCP Tool 구현

### 응용 프로젝트 (20%)
- [ ] 스마트홈 기기 제어
- [ ] 커스텀 보드 제작
- [ ] 자체 서버 호스팅
- [ ] 오리지널 프로젝트 완성
- [ ] 커뮤니티 공유

---

**📝 문서 정보**
- **원본 프로젝트**: XiaoZhi ESP32 by GitHub @78
- **문서 버전**: v2.0
- **대상 독자**: 초보 개발자 (프로그래밍 경험 1년 미만)
- **예상 학습 시간**: 4-6주 (주당 10시간)
- **난이도**: ⭐⭐☆☆☆ (5점 만점)
- **작성일**: 2025-10-12
- **마지막 업데이트**: 2025-10-12
- **문서 언어**: 한국어

**🔗 참고 자료**
- [GitHub 원본 저장소](https://github.com/78/xiaozhi-esp32)
- [공식 위키 (飞书)](https://ccnphfhqs21z.feishu.cn/wiki/F5krwD16viZoF0kKkvDcrZNYnhb)
- [공식 서버](https://xiaozhi.me)
- [QQ 커뮤니티 그룹](https://qm.qq.com/q/1011329060)

**📢 피드백**
이 가이드가 도움이 되었나요? 개선 제안이나 오류 발견 시:
- GitHub Issues에 리포트
- QQ 그룹에서 피드백
- 이메일로 연락

---

**감사의 말**: 이 프로젝트는 오픈소스 커뮤니티의 협력으로 만들어졌습니다. 모든 기여자분들께 감사드립니다. 🙏